# Bridge Kit 0.9.0 Low-Friction Operations Convergence — Kickoff Draft v0.1

Status: **DRAFT FOR EXECUTION-READY CRITIC REVIEW / NOT YET USER AUTHORIZATION**

This Kickoff is bound to:

- Plan: `docs/design/0.9.0_low_friction_operations_convergence_plan_v0.3_2026-09-23.md`
- Plan commit: `11f770fdb8a8d928aefbfda5f42527270a19f8b4`
- Goal: `docs/design/0.9.0_low_friction_operations_convergence_goal_v0.1_2026-09-23.md`
- Task key: `bridge-core--low-friction-operations-convergence`

The user sending the Critic-approved version of the text below is the execution authorization.

---

## Copy to Codex after execution-ready Critic PASS

你现在执行 Bridge Kit 0.9.0 的已批准任务：

```text
Repository:
YuukiAS/GPT_Codex_AI_Bridge_Kit

Task key:
bridge-core--low-friction-operations-convergence

Branch:
main

Worktree:
仅使用我发送本消息时你当前所在的该仓库 canonical checkout。
启动时用 git rev-parse --show-toplevel 解析并记录绝对路径；
不得创建或切换到新的 branch/worktree。
```

权威设计与完整执行合同：

```text
Plan:
docs/design/0.9.0_low_friction_operations_convergence_plan_v0.3_2026-09-23.md

Plan commit:
11f770fdb8a8d928aefbfda5f42527270a19f8b4

Canonical Goal:
docs/design/0.9.0_low_friction_operations_convergence_goal_v0.1_2026-09-23.md
```

先读取最新 `main`、根 `AGENTS.md`、上述 Plan 与 Goal。不要重新设计架构。若 latest-main drift 实质改变了 approved production/version/release surfaces，停止并返回 Planner/Critic。

### 我现在授权的执行范围

我明确授权你在当前这一个 Bridge Kit repository / `main` / canonical checkout 内，完成 Goal 中批准的 0.9.0 implementation、tests、normal-entry gates、evidence、README/CHANGELOG/version closure。

允许修改的 production/source area 仅限 Goal 明确列出的：

- Machine Policy / host rules 与直接 CLI；
- existing Reviewed Handoff bounded worktree materialization；
- Persistent Run progress/ETA；
- Notifications 最小 operational-progress transport；
- 直接相关 tests；
- README / QUICKSTART / AGENTS / CHANGELOG / version metadata；
- 本 task 的 results/evidence。

禁止扩展到 unrelated Overleaf、Visual Review、Text Review/Transform、Controlled Mode/agent-flow、paid-review 或其他产品模块。

### Git 授权

我授权：

- 当前 `main` 上 task-owned ordinary commits；
- 向 `origin/main` 做本任务需要的 ordinary non-force publication；
- implementation 完成后使用已批准的唯一 bounded `ai-bridge host publish-current-branch` 路径做对应正常验证；
- GitHub 的正常 fetch/read/check 操作；
- 本地 disposable Git repo、bare remote、临时 config/worktree fixture 用于 tests。

不授权：

- force / force-with-lease；
- reset/clean/restore 用户工作；
- remote/tag 删除；
- 作为 workaround 创建新的 remote branch；
- upstream/remotes remap；
- PR create/merge；
- GitHub release/tag 创建；
- unrelated repository write。

如果当前 checkout 不是 `main`，origin 不是 `YuukiAS/GPT_Codex_AI_Bridge_Kit`，或者 working tree 有不属于本任务的 dirty work，先停止并报告，不要 stash/reset 猜 ownership。

### Machine Policy 真实安装/升级授权

本消息授权 **一次** bounded real Machine Policy install/upgrade，但只能满足以下全部条件后进行：

1. implementation 已完成；
2. focused/full relevant tests 已通过；
3. temporary candidate execpolicy/config probes 已通过；
4. README / CHANGELOG / version 已冻结为 final 0.9.0 candidate；
5. final candidate commit 已记录。

任务开始时先用 Bridge canonical resolution 解析并记录当前 `CODEX_HOME` 绝对路径。真实 install/upgrade 只能作用于这个 **同一个 current Codex identity**。

允许：

- Bridge 标准 managed-file backup；
- 一次 final-candidate `ai-bridge host install`；
- `ai-bridge host validate`；
- 同一 final candidate 的真实 Machine Policy normal-entry G1-G4/G13 验证。

如果 live install/validation 暴露 candidate regression：

- 先保留 evidence；
- 恢复本次 install 前的 exact managed-file backup；
- 不去安装另一个 `CODEX_HOME`；
- 不靠 broad allow / approval bypass 修测试。

若全部 required live gates PASS，可以保留这个已验证的 0.9.0 Machine Policy 安装。

### 临时探针与测试授权

允许使用：

- temporary rules/config；
- disposable local Git repos / bare remotes；
- temporary CODEX_HOME；
- temporary sibling/`/tmp` worktree fixtures；
- bounded local tmux test session；
- public-safe/repository-local fixture；
- current installed Git/Codex/GitHub CLI；
- 当前机器已经存在的正常 HTTPS credential manager、SSH agent、`~/.ssh/config`。

B2 transport fence 的 deterministic regressions必须包括：

```text
GIT_CONFIG_COUNT / GIT_CONFIG_KEY_* / GIT_CONFIG_VALUE_*
GIT_CONFIG_GLOBAL
GIT_CONFIG_SYSTEM
GIT_CONFIG_NOSYSTEM
core.sshCommand
GIT_SSH
GIT_SSH_COMMAND
credential.helper at local/worktree/command scope
GIT_ASKPASS
SSH_ASKPASS
core.askPass
```

custom transport 失败时回 ordinary raw `git push` approval；不得再造 wrapper。

### Notifications / 外部发送

如果当前 repo/host **已经**配置并验证过 Notifications，我授权最多 4 条本任务的 operational progress test notification 发往现有已配置 recipient，用于 G10-G12。

不得：

- 修改 recipient；
- 新增账号/provider；
- 新增/迁移 credential；
- 打印 secret；
- 将 progress notification 写成 semantic PASS/release decision。

如果没有现成有效 Notifications config，不要静默配置新的 credential/provider；记录 gate unavailable，并按 Goal 停止 full-release claim。

### Plugin 边界

- production `plugin-replay` 只保持 KEEP_NARROW；
- 不复活 `candidate-plugin-replay`；
- ordinary plugin development/install/reinstall/fresh-session 使用 official Codex / AI_Skills path；
- AI_Skills 如需用于 G8/G9，只允许 read-only evidence，不允许本任务写 AI_Skills repo；
- 不调用 paid API。

### Capability Gates

必须完整执行 Goal 中 G1-G15。

不能用：

- tests/CI PASS；
- schema/file existence；
- synthetic-only fixture；
- wrapper-only path；
- stale installed Machine Policy；
- 不同 candidate 拼接 evidence

替代 normal-entry capability evidence。

G10/G11 的 progress/ETA 必须来自真实 progress evidence；没有 defensible ETA 就输出 `UNKNOWN`，不能编百分比或完成时间。

### 明确禁止

本授权不允许：

- generic shell/Python Host-wide allow；
- danger-full-access；
- generic/Host-wide `approval_policy=never`；
- auth DB / token store；
- wrapper registry；
- new daemon/controller/watcher hierarchy；
- new workflow class；
- broad destructive Git allow；
- new provider/data/credential scope；
- paid API；
- unrelated external messages；
- arbitrary deployment/resource mutation。

现有 narrow `plugin-replay` child 内冻结的 isolated `approval_policy="never"` contract 不属于上述禁止项，不要误伤它。

### 完成与停止

你只有在：

- one final 0.9.0 candidate；
- G1-G15 全部按 Goal 满足；
- live Machine Policy identity 验证完成；
- display taxonomy 与 compatibility identifiers 正确；
- required final evidence 完整；
- independent pre-final/final review 所需 package 已准备好

之后，才可以报告 release-ready candidate。

如果需要改变 architecture、Gate、权限、provider/data/credential scope、恢复语义，或引入第二个 wrapper/new subsystem，立即停止并回 Planner/Critic，不要自行扩 scope。
