# Bridge Kit 0.9.0 Low-Friction Operations Convergence — Recovery Critic Handoff v0.2

Date: **2026-09-24**  
Task key: **bridge-core--low-friction-operations-convergence**  
Review stage: **RECOVERY_EXECUTION_READY**  
Reviewed Plan: **docs/design/0.9.0_low_friction_operations_convergence_plan_v0.4_2026-09-24.md**  
Plan commit: **bf20c4ff4f2a72f2ef2b5a57cd236519d91ef4cd**  
Recovery Package: **docs/design/0.9.0_low_friction_operations_convergence_recovery_execution_package_v0.2_2026-09-24.md**  
Recovery Package commit: **b2a659e3fa49cc391467226bc941457d79edbc69**

以下内容可完整复制到独立 Critic thread。

~~~text
你是 AI Research Stack 的独立 Critic。

target_repo:
YuukiAS/GPT_Codex_AI_Bridge_Kit

target_domain:
Bridge Kit core

design_topic_or_task_key:
bridge-core--low-friction-operations-convergence

review_stage:
RECOVERY_EXECUTION_READY

source_branch/ref:
main

Critic-approved amended Plan:
docs/design/0.9.0_low_friction_operations_convergence_plan_v0.4_2026-09-24.md

Plan version:
0.4

Plan commit:
bf20c4ff4f2a72f2ef2b5a57cd236519d91ef4cd

Amended Recovery Goal:
docs/design/0.9.0_low_friction_operations_convergence_recovery_goal_v0.2_2026-09-24.md

Recovery Goal version:
0.2

Recovery Goal first commit:
71f39b51b53ad3bff4e89567293eae38dd88aa82

Amended Recovery Kickoff:
docs/design/0.9.0_low_friction_operations_convergence_recovery_kickoff_v0.2_2026-09-24.md

Recovery Kickoff version:
0.2

Recovery Kickoff first commit:
d49a7fa2d4e23f58cf2ab4196587c55d55a9f123

Amended Recovery Package:
docs/design/0.9.0_low_friction_operations_convergence_recovery_execution_package_v0.2_2026-09-24.md

Recovery Package version:
0.2

Recovery Package commit:
b2a659e3fa49cc391467226bc941457d79edbc69

Same task:
bridge-core--low-friction-operations-convergence

Current recovery source candidate before this amendment:
37a773cd83971d64f194a7d82c55a24889bd8164

This is READ-ONLY recovery execution-ready review.

Do not:
- modify Git;
- implement production;
- install/refresh Machine Policy;
- configure SSH;
- create branch/worktree;
- launch Executor;
- call paid API;
- start automation;
- create successor task;
- reopen unrelated Plan v0.4-approved architecture.

==================================================
0. GOVERNANCE / SOURCE
==================================================

Read latest AI_Skills_Collection main:
- docs/workflows/PLANNER_ROLE_CONTRACT.md
- docs/workflows/CRITIC_ROLE_CONTRACT.md
- docs/workflows/PLUGIN_CAPABILITY_GATE_POLICY.md

Then read Bridge latest main:
- AGENTS.md
- Plan v0.4
- Recovery Goal v0.2
- Recovery Kickoff v0.2
- Recovery Package v0.2
- current ai_bridge_kit/host.py
- current recovery EVIDENCE.md / FINAL_REPORT.md only as needed

Do not re-review Persistent Run, Reviewed materializer, plugin-replay,
Notifications, taxonomy, wrapper count, or one-release architecture unless
this amended package directly changes them.

==================================================
1. PACKAGE FIDELITY TO PLAN V0.4
==================================================

The package must implement exactly this product scope:

trusted low-friction publisher positive:
https://github.com/<owner>/<repo>.git

using existing system/global/gh credential helper.

Not trusted-positive:
- git@github.com:<owner>/<repo>.git
- ssh://git@github.com/<owner>/<repo>.git
- other SSH/scp-style/custom transport

Those must fail closed before the first remote/network Git operation and return
to ordinary raw git push approval.

No SSH setup is part of the task.

Check that the Goal/Kickoff/package do not reintroduce real SSH positive
publication as a release requirement.

==================================================
2. EFFECTIVE URL REQUIREMENT
==================================================

Verify the package requires classification of EFFECTIVE fetch/push URLs after
Git URL rewriting.

Required negatives include:

- literal SSH/scp-style origin;
- ssh:// origin;
- literal HTTPS rewritten via insteadOf/pushInsteadOf to SSH/custom;
- effective fetch HTTPS + effective push SSH/custom.

All must reject before ls-remote/fetch/push.

Check that this is publisher-specific and does not globally narrow
shared _canonical_repo_identity(), because reviewed_handoff/local test identity
must remain compatible.

==================================================
3. SECURITY NEGATIVES PRESERVED
==================================================

The amendment must retain the existing fences/canaries:

- GIT_SSH
- GIT_SSH_COMMAND
- GIT_SSH_VARIANT
- core.sshCommand
- local/worktree/command credential.helper
- GIT_ASKPASS
- SSH_ASKPASS
- core.askPass
- GIT_CONFIG_COUNT
- GIT_CONFIG_KEY_*
- GIT_CONFIG_VALUE_*
- GIT_CONFIG_GLOBAL
- GIT_CONFIG_SYSTEM
- GIT_CONFIG_NOSYSTEM
- multiple destination / pushurl mismatch
- pre-push hook
- reviewed_runner guard
- mirror/followTags/submodule/push-option/signing expansion
- final branch/HEAD/destination/config drift

Rejected cases must prove:
- no injected executable runs;
- no remote/network publication occurs through trusted helper.

Removing the SSH positive gate must not weaken these negatives.

==================================================
4. AFFECTED GATES ONLY
==================================================

Review that the package reruns only the affected release claims plus required
should-not-change regression.

G3:
- real existing-branch GitHub HTTPS bounded publication;
- no repeated approval;
- exact non-force ref effect.

G4:
- GitHub HTTPS positive;
- existing normal credential-manager path;
- all retained exact-effect/security negatives;
- explicit effective SSH/custom fail-closed;
- no real SSH positive requirement.

G14:
- one final candidate owns source/runtime/docs/evidence;
- documentation says:
  trusted low-friction = GitHub HTTPS
  SSH/custom = ordinary approval.

All other gates are should-not-change unless directly affected.

Do not require re-running unrelated architecture work for ceremony.

==================================================
5. NO SSH MACHINE CONFIGURATION
==================================================

The Kickoff must explicitly prohibit:

- create/upload SSH key;
- start/manage ssh-agent;
- modify ~/.ssh/config;
- modify known_hosts;
- mutate SSH_AUTH_SOCK;
- switch remotes to SSH;
- add SSH wrapper.

Check that this is unambiguous.

==================================================
6. MACHINE POLICY REFRESH / INSTALL AUTHORIZATION
==================================================

Plan v0.4 itself gave no new install authorization.

Kickoff v0.2 now requests a conditional maximum-one refresh/install only for:

/home/yuukias/.codex

Required semantics:

1. freeze new final candidate first;
2. inspect trusted ai-bridge executable + imported ai_bridge_kit source identity;
3. if current editable install already loads the final candidate and managed
   Machine Policy files have no drift:
   - do not do redundant install;
   - run host validate + affected normal-entry gates;
4. if package identity is stale:
   - at most one local-only editable refresh from:
     /home/yuukias/GPT_Codex_AI_Bridge_Kit
   - same existing Python environment;
   - no new venv/external package source/provider;
5. if managed Machine Policy files/binding need refresh:
   - at most one host install for /home/yuukias/.codex;
   - standard backup before writes;
6. on regression:
   - preserve evidence;
   - restore exact managed-file backup;
   - no second CODEX_HOME/install loop.

Review whether this is sufficiently bounded and whether the user sending the
approved Kickoff would constitute valid current-user authorization for this one
conditional refresh.

Do not require an install if identity is already current.

==================================================
7. AUTHORIZATION BOUNDARY
==================================================

The Kickoff may authorize only:

- same repo/task/main/canonical worktree;
- publisher-specific HTTPS-only scope correction;
- ordinary task-owned commits;
- ordinary non-force origin/main publication;
- affected G3/G4/G14 normal-entry evidence;
- disposable local Git/config fixtures;
- same existing GitHub HTTPS credential/account/provider scope;
- the conditional same-CODEX_HOME refresh described above.

It must NOT authorize:

- paid API;
- new provider/account/credential;
- secret display;
- force/destructive Git;
- new remote branch;
- PR/release/tag;
- upstream/remote remap;
- another CODEX_HOME;
- second wrapper;
- generic Git facade;
- auth DB/token store;
- daemon/watcher hierarchy;
- new workflow;
- candidate-plugin-replay;
- unrelated external effect.

==================================================
8. EVIDENCE HANDLING
==================================================

Historical SSH-positive failure must remain recorded as real provenance.

Final evidence may say:

- under Plan v0.3 the SSH-positive gate was attempted and unavailable;
- Plan v0.4 deliberately removed trusted SSH positive publication from the
  0.9.0 product claim;
- SSH/custom fail-closed security negatives remain required and passed.

It must not rewrite history or pretend the previous SSH failure never occurred.

==================================================
9. OUTPUT
==================================================

Return:

CRITIC_RESULT=PASS | REVISE
REVIEW_STAGE=RECOVERY_EXECUTION_READY
REVIEWED_PLAN_VERSION=0.4
RECOVERY_GOAL_PATH=docs/design/0.9.0_low_friction_operations_convergence_recovery_goal_v0.2_2026-09-24.md
RECOVERY_GOAL_VERSION=0.2
RECOVERY_KICKOFF_PATH=docs/design/0.9.0_low_friction_operations_convergence_recovery_kickoff_v0.2_2026-09-24.md
RECOVERY_KICKOFF_VERSION=0.2
RECOVERY_PACKAGE_PATH=docs/design/0.9.0_low_friction_operations_convergence_recovery_execution_package_v0.2_2026-09-24.md
RECOVERY_PACKAGE_COMMIT=b2a659e3fa49cc391467226bc941457d79edbc69
HTTPS_ONLY_FIDELITY=PASS | REVISE
EFFECTIVE_URL_FENCE=PASS | REVISE
SSH_NEGATIVE_FENCE_PRESERVED=YES | NO
SHARED_HELPER_COMPATIBILITY=PASS | REVISE
G3_G4_G14_SCOPE=PASS | REVISE
MACHINE_POLICY_REFRESH_AUTH=PASS | REVISE
AUTHORIZATION_BOUNDARY=PASS | REVISE
READY_FOR_CODEX=YES | NO
BLOCKERS=
NON_BLOCKING_NOTES=
NEXT_HANDOFF=CODEX | PLANNER

If REVISE:
block only on a concrete mismatch with Plan v0.4 or a real authorization/recovery
gap in this amended package.
Do not reopen unrelated approved architecture.

If PASS:

1. explain briefly in Chinese that this recovery now targets only the
   GitHub-HTTPS publisher scope correction and affected final gates;
2. state that SSH machine setup is intentionally out of scope;
3. state:
   READY_FOR_CODEX=YES
4. reproduce verbatim the exact text under:

## Copy to Codex after recovery execution-ready Critic PASS

from:

docs/design/0.9.0_low_friction_operations_convergence_recovery_kickoff_v0.2_2026-09-24.md

without broadening or rephrasing authorization.

That exact text is the only amended Recovery Kickoff the user should send to Codex.
~~~
