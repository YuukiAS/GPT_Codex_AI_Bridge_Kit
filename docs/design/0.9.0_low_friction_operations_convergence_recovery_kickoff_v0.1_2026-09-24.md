# Bridge Kit 0.9.0 Low-Friction Operations Convergence — Recovery Kickoff Draft v0.1

Status: **DRAFT FOR RECOVERY EXECUTION-READY CRITIC REVIEW / NOT YET USER AUTHORIZATION**

Bound objects:

- Approved design:
  `docs/design/0.9.0_low_friction_operations_convergence_plan_v0.3_2026-09-23.md`
- Approved Plan commit:
  `11f770fdb8a8d928aefbfda5f42527270a19f8b4`
- Canonical Goal:
  `docs/design/0.9.0_low_friction_operations_convergence_goal_v0.1_2026-09-23.md`
- Recovery Goal:
  `docs/design/0.9.0_low_friction_operations_convergence_recovery_goal_v0.1_2026-09-24.md`
- Recovery Goal first commit:
  `411abef0040ebc33e5b831abd4e74bc7d50ef4db`
- Current implementation baseline:
  `88fa67c44e6e491ed548f033ce0d3c33f63525c4`
- Task:
  `bridge-core--low-friction-operations-convergence`

Only the user sending the Critic-approved text below constitutes recovery execution authorization.

---

## Copy to Codex after recovery execution-ready Critic PASS

你现在恢复并返修同一个 Bridge Kit 0.9.0 任务。不要开 successor，不要重新设计 Plan v0.3。

```text
Repository:
YuukiAS/GPT_Codex_AI_Bridge_Kit

Task:
bridge-core--low-friction-operations-convergence

Branch:
main

Canonical worktree:
/home/yuukias/GPT_Codex_AI_Bridge_Kit

Current implementation baseline:
88fa67c44e6e491ed548f033ce0d3c33f63525c4

Current Machine Policy identity:
CODEX_HOME=/home/yuukias/.codex
```

权威合同：

```text
Approved Plan:
docs/design/0.9.0_low_friction_operations_convergence_plan_v0.3_2026-09-23.md

Canonical Goal:
docs/design/0.9.0_low_friction_operations_convergence_goal_v0.1_2026-09-23.md

Recovery Goal:
docs/design/0.9.0_low_friction_operations_convergence_recovery_goal_v0.1_2026-09-24.md
```

先读取 latest `main`、根 `AGENTS.md`、上述三个合同以及当前
`results/bridge-core--low-friction-operations-convergence/EVIDENCE.md` /
`FINAL_REPORT.md`。

当前旧 `EVIDENCE.md` / `FINAL_REPORT.md` 不是最终完成证据。只有真实修复并重新跑完 Recovery Goal 要求的 gates 后才能更新它们。

### 我现在授权的恢复范围

我授权你在**同一个 repo / task / main / canonical worktree**内完成以下四项：

```text
B1-PUBLISH-PREFLIGHT-TRANSPORT-FENCE
B2-PERSISTENT-RUN-REPORTER-INCOMPLETE
B3-WORKTREE-FROZEN-SCOPE-FIDELITY
B4-FINAL-GATE-EVIDENCE-INCOMPLETE
```

保留 88fa67c 未受影响实现；只修 Recovery Goal 指定的 source/tests/templates/evidence owner。

不得重新设计：

- Machine Policy native read architecture；
- single publish-current-branch helper；
- single reviewed-handoff materializer；
- plugin-replay KEEP_NARROW；
- candidate-plugin-replay removal；
- Persistent Run producer-owns-truth；
- Notifications transport-only authority；
- display taxonomy；
- one 0.9.0 release。

### B1 授权

允许修改 publisher implementation/tests，使 transport/config/credential/askpass rejection 在**第一次 remote/network Git command 之前**完成，或使任何 remote preflight 已经使用被证明安全的 sanitized environment。

必须加入 executable canary regressions并证明 canary 不执行：

- GIT_SSH / GIT_SSH_COMMAND；
- GIT_ASKPASS；
- SSH_ASKPASS；
- local/worktree/command credential.helper；
- core.sshCommand。

同时覆盖：

- GIT_CONFIG_COUNT / KEY / VALUE；
- GIT_CONFIG_GLOBAL；
- GIT_CONFIG_SYSTEM；
- GIT_CONFIG_NOSYSTEM。

不能加第二个 wrapper。custom transport 继续回 ordinary raw push approval。

### B2 授权

允许在现有 Persistent Run 内补齐 approved event-driven reporter：

- normalized concise report；
- machine-local latest；
- bounded recent history；
- reconnect query；
- progress/ETA movement comparison；
- dedupe / bounded cadence；
- contract fields：
  - Progress source locator
  - Report interval
  - Delivery mode
  - Stall threshold
- optional existing Notifications one-shot projection；
- no daemon/controller/watcher；
- no auto restart/cancel/resource mutation。

允许保留当前 `persistent-run progress --evidence` 作为 diagnostic/compatibility surface，但它不能继续替代 reporter。

### B3 授权

允许修 Reviewed materializer 的 frozen scope fidelity：

- frozen worktree identity不能在 invocation 时随着 symlink target一起漂移；
- changed symlink target/path redirection必须 fail closed；
- bootstrap必须验证 frozen base_commit 对 frozen `origin/<base_branch>` 的 lineage；
- 保持 sibling 和 `/tmp` 正常 case；
- 实现已批准的 invocation-owned partial rollback/evidence；
- 不建 auth DB/schema family/workflow。

### Git / publication 授权

我授权：

- 当前 `main` 上 task-owned ordinary commits；
- 本任务 ordinary non-force publication 到 `origin/main`；
- 使用已存在的 single bounded publisher 做 required normal-entry验证；
- GitHub fetch/read/check；
- disposable local Git repo / bare remote / config / symlink / worktree fixtures。

不授权：

- force / force-with-lease；
- reset/clean/restore用户工作；
- remote/tag delete；
- workaround new remote branch；
- upstream/remote remap；
- PR create/merge；
- GitHub release/tag；
- unrelated repository write。

### Final-candidate Machine Policy replacement授权

之前的 Machine Policy live install authorization 已经消费完毕。

这次我**重新授权且只授权一次** replacement/final-candidate Machine Policy install/validate，目标固定为：

```text
/home/yuukias/.codex
```

在 B1-B3 修复、focused/full tests、临时 probes 完成并冻结一个新的 final candidate **之前，禁止执行这次 live replacement install**。

冻结 final candidate 后允许：

1. 对 `/home/yuukias/.codex` 做标准 Bridge managed-file backup；
2. 从该 frozen final candidate 执行一次 replacement `ai-bridge host install`；
3. 执行 `ai-bridge host validate`；
4. 在这个已安装 final candidate 上跑 Recovery Goal 要求的真实 Machine Policy normal-entry gates。

如果 replacement candidate 引入回归：

- 保存 evidence；
- 恢复这次 replacement 前 exact managed-file backup；
- 不安装第二个 CODEX_HOME；
- 不立即再消耗第二次 live install；
- 回 Planner/recovery。

如果全部 gate 通过，可保留这个 final 0.9.0 Machine Policy。

### B4 / normal-entry gates授权

冻结一个新的 final candidate 后，直接按 Recovery Goal 重跑真实 evidence，至少包括：

- G1/G2：installed Machine Policy 下的真实 normal Codex entry；
- G3/G4：真实 bounded publisher；HTTPS 正常 credential manager；标准 SSH agent/config；所有 transport executable negative canary；
- G5/G6/G7：trusted materializer sibling bootstrap + `/tmp` rematerialization + fail-closed negatives，并确认没有重复 Auto-review interruption；
- G9：official Codex/AI_Skills plugin install/reinstall/fresh-session route；
- G10/G11：multi-event Persistent Run reporter + 独立 UNKNOWN/stall case；
- G13：real reviewed_runner guard environment 中 generic publisher必须拒绝；
- G14：所有 source/install/evidence/docs 都绑定同一个 final candidate。

G8/G12/G15继续做 same-candidate should-not-change确认。

不能用 unit tests/CI/static JSON normalization 替代上述 normal-entry evidence。

### Official plugin probe授权边界

为 G9，我授权使用**现有 Codex/OpenAI account/provider/credential scope**做 bounded official plugin normal-entry验证。

允许：

- official Codex Marketplace/plugin add/reinstall/fresh-session path；
- read AI_Skills current source/evidence；
- local/personal/repo-local test marketplace only if already within现有 official flow；
- 验证结束后恢复/保留原本合法 production plugin identity，不新增 Bridge candidate wrapper。

不授权：

- 新 provider/account；
- 新 credential；
- paid API；
- 写 AI_Skills repository；
- candidate-plugin-replay。

### Persistent Run / Notifications gate授权

允许使用 task-owned bounded local multi-event process和独立 UNKNOWN/stall process做 G10/G11。

如果当前机器已经配置好同一个 Notifications provider/recipient，我授权本 recovery gate 使用现有配置发送最多 **4 条** operational-progress test Notifications。

不得：

- 改 recipient；
- 改 provider/account；
- 新 credential；
- 打印 secret；
- 把 operational progress写成 semantic PASS/READY/release completion。

如果当前 Notifications 不可用，不要新配 provider/credential；记录不可用证据，并停止声称完整 release closure。

### 明确禁止

本 recovery authorization 不允许：

- paid API；
- new provider/account/data/credential scope；
- destructive/force Git；
- PR/release/tag；
- new wrapper；
- generic Git facade；
- auth DB/token store；
- daemon/watcher hierarchy；
- new workflow class；
- candidate-plugin-replay；
- unrelated external side effect。

现有 narrow `plugin-replay` child 固定 isolated `approval_policy="never"` contract 保持不变，不要误伤。

### EVIDENCE / FINAL_REPORT 规则

在 B1-B3 修复后先冻结一个新 final candidate，再跑真实 gates。

只有真实 gates 完成后才更新：

```text
results/bridge-core--low-friction-operations-convergence/EVIDENCE.md
results/bridge-core--low-friction-operations-convergence/FINAL_REPORT.md
```

最终报告必须明确旧 candidate `88fa67c...` 是被 pre-final Critic 返修的 baseline，不得把旧的 `complete` marker 或旧 gate summary 当最终 PASS。

如果修复暴露 Plan v0.3 架构、权限、Gate 或恢复语义必须改变，立即停止回 Planner/Critic，不要自行扩大 scope。
