# Bridge Kit 0.9.1 Reviewed First-Bootstrap — Kickoff Draft v0.2

Status: **DRAFT FOR EXECUTION_READY CRITIC RE-REVIEW / NOT AUTHORIZED**

Bound design:

- Plan: `docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_plan_v0.2_2026-09-24.md`
- Plan commit: `eac789a35c4a83ce015a7744c722527580e79dc8`
- Canonical Goal: `docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_goal_v0.2_2026-09-24.md`
- Goal first commit: `399db248613948a9f4ed02555d456418ad9a37ee`
- Task: `reviewed-handoff--first-bootstrap-normal-entry`

Only the user sending the Critic-approved text below constitutes execution authorization.

---

## Copy to Codex after execution-ready Critic PASS

执行 Bridge Kit 0.9.1 Reviewed first-bootstrap normal-entry 任务。严格按已 PASS 的 Plan v0.2 和 Canonical Goal v0.2，不要重新设计。

权威合同：

```text
Plan:
docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_plan_v0.2_2026-09-24.md

Canonical Goal:
docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_goal_v0.2_2026-09-24.md
```

本次授权同时覆盖 Bridge 0.9.1 implementation、candidate Machine Policy activation、使用**新 candidate task-bootstrap**创建真实 AI_Skills sibling implementation worktree、workflow-core 小型 consumer routing refinement，以及 FB-G1–FB-G5 的 bounded evidence。

不要 raw bootstrap AI_Skills worktree。

==================================================
1. PHASE 0 — PRECHECK ONLY
==================================================

### Bridge identity

```text
Repository:
YuukiAS/GPT_Codex_AI_Bridge_Kit

Task:
reviewed-handoff--first-bootstrap-normal-entry

Branch:
main

Canonical worktree:
/home/yuukias/GPT_Codex_AI_Bridge_Kit
```

核验 repo/origin/branch/worktree/current HEAD/dirty ownership。

Bridge implementation只在这个 canonical main checkout进行；不得新建 Bridge implementation branch/worktree。

### AI_Skills base discovery

```text
Repository:
YuukiAS/AI_Skills_Collection

Canonical checkout:
/home/yuukias/AI_Skills_Collection

Implementation task:
workflow-core--reviewed-first-bootstrap-normal-entry

Derived branch:
reviewed/workflow-core--reviewed-first-bootstrap-normal-entry

Exact future worktree:
/home/yuukias/AI_Skills_Collection-workflow-core--reviewed-first-bootstrap-normal-entry
```

我授权：

- 对 canonical AI_Skills checkout做一次普通 `git fetch origin main`；
- 读取并记录当时 exact `origin/main` OID；
- 只读检查 branch/worktree/path conflict；
- 读取 latest AGENTS、version policy、VERSION、workflow-core source/version。

**此时禁止创建 AI branch/worktree。**

如果 workflow-core source/version/release policy有 material overlap，或 exact sibling path/derived branch已经被不相关状态占用，停止并报告，不要换路径/branch/clone。

==================================================
2. PHASE 1 — BRIDGE 0.9.1 IMPLEMENTATION ONLY
==================================================

我授权在 Bridge canonical main完成 Goal允许的 0.9.1 source/tests/templates/docs/version/evidence修改。

必须实现：

- `ai-bridge reviewed-handoff task bootstrap`;
- branch固定由 `reviewed/<task_key>` 派生，无 `--branch`;
- first bootstrap Machine Policy = `prompt`；
- existing `materialize-worktree` = artifact-bound `allow`；
- raw `git worktree add` = `prompt`；
- one shared REQUEST/CURRENT initializer；
- first REQUEST/CURRENT only in new reviewed worktree；
- remote-only resume先从 exact remote tree读取/校验 REQUEST/CURRENT，再 checkout/materialize；
- `LOCAL_TASK_METADATA_PARTIAL` fail closed；
- current-invocation-only conservative rollback；
- Bridge 0.9.1 version/README/CHANGELOG closure。

运行 focused/full Bridge tests。

随后创建一个 exact Bridge candidate commit并冻结它。

我授权 Bridge task-owned ordinary commits和 ordinary non-force publication到现有 `origin/main`。

不授权 force、reset/clean/restore用户工作、PR/tag/release、remote remap或 unrelated source changes。

==================================================
3. PHASE 2 — ACTIVATE EXACT BRIDGE CANDIDATE
==================================================

Machine Policy target固定：

```text
CODEX_HOME=/home/yuukias/.codex
```

只有 Bridge candidate冻结且 tests通过后才允许本 Phase。

步骤：

1. 检查实际 `ai-bridge` executable 与 imported `ai_bridge_kit` source；
2. 若 current editable install 已加载 exact candidate，不做冗余 reinstall；
3. 若 runtime stale，我授权最多一次 local-only：
   ```text
   pip install -e /home/yuukias/GPT_Codex_AI_Bridge_Kit
   ```
   仅更新当前 existing Python environment，不创建新环境、不从外部 package source升级；
4. 对 managed Machine Policy写入前创建标准 Bridge backup；
5. 我授权最多一次：
   ```text
   ai-bridge host install --codex-home /home/yuukias/.codex
   ```
6. 运行 `ai-bridge host validate`；
7. 必须真实验证：
   - `ai-bridge reviewed-handoff task bootstrap ...` => `prompt`;
   - `ai-bridge reviewed-handoff materialize-worktree ...` => `allow`;
   - raw `git worktree add ...` => `prompt`；
8. 若 candidate导致 Machine Policy regression，保存 evidence、恢复 exact pre-install managed-file backup并停止；
9. 不安装第二个 CODEX_HOME，不做 install loop。

==================================================
4. PHASE 3 — REAL SIBLING FB-G1 / CREATE AI IMPLEMENTATION WORKTREE
==================================================

现在才允许创建 AI_Skills implementation branch/worktree，而且必须通过刚安装/验证的 **Bridge 0.9.1 candidate command**。

使用 Phase 0 已记录的 exact AI `origin/main` OID。

Canonical invocation effect：

```text
target:
/home/yuukias/AI_Skills_Collection

task-key:
workflow-core--reviewed-first-bootstrap-normal-entry

expected-repo:
YuukiAS/AI_Skills_Collection

derived branch:
reviewed/workflow-core--reviewed-first-bootstrap-normal-entry

expected-worktree:
/home/yuukias/AI_Skills_Collection-workflow-core--reviewed-first-bootstrap-normal-entry

expected-base-ref:
origin/main

expected-base-commit:
<Phase 0 exact origin/main OID>

objective:
Route Reviewed first bootstrap/resume through canonical Bridge 0.9.1 entries.
```

通过新 normal entry：

```text
ai-bridge reviewed-handoff task bootstrap ...
```

完成 exact first bootstrap。

本消息明确授权上述 exact repo/task-derived branch/worktree/base effect。CLI assertions本身不是授权；本 Kickoff 是授权来源。

这个调用必须走 candidate Machine Policy的 `prompt` / Auto-review path，并且当前用户授权必须足够，**不能要求我再做一次 manual worktree/bootstrap操作**。

成功标准：

- exact sibling branch/worktree创建；
- first REQUEST/CURRENT只在新 reviewed worktree；
- canonical AI main无 task bootstrap files；
- 没有 raw `git worktree add`。

这就是 primary FB-G1 positive normal-entry evidence。

如果 Auto-review/sandbox仍拒绝这个 exact candidate task-bootstrap：

```text
FB-G1=FAIL
NEXT_HANDOFF=PLANNER/CRITIC
```

禁止：

- 叫我运行 raw git；
- 改用 /tmp；
- raw创建 branch/worktree；
- 换 branch/path/base；
- permanent allow bootstrap；
- clone/remap。

只有 FB-G1 PASS 后才进入 Phase 4。

==================================================
5. PHASE 4 — AI_SKILLS WORKFLOW-CORE IMPLEMENTATION
==================================================

只在刚由 Bridge candidate创建的 exact worktree：

```text
/home/yuukias/AI_Skills_Collection-workflow-core--reviewed-first-bootstrap-normal-entry
```

进行 workflow-core consumer refinement。

Canonical source owner：

```text
skills/core/codex-system/codex-workflow-protocol/SKILL.md
```

允许直接相关 references/tests/evals/generated Marketplace/plugin payload/version/changelog/repository version parity/README/root CHANGELOG。

不要新增 root AGENTS 同义规则。

Production route必须明确：

```text
brand-new Reviewed task
-> ai-bridge reviewed-handoff task bootstrap

existing task locally or exact reviewed remote
-> ai-bridge reviewed-handoff materialize-worktree --mode resume

Bridge capability/version unavailable
-> fail early before substantive implementation
```

绝对不要：

- raw `git worktree add` fallback；
- alternate /tmp；
- clone；
- another branch；
- remote remap；
- 在 workflow-core 内实现 Git mechanics。

### AI_Skills version closure

启动 Phase 4 前重新读取：

- `docs/workflows/PLUGIN_VERSIONING_AND_CHANGELOGS.md`
- `VERSION`
- `scripts/codex_marketplace_config.json`
- `docs/plugin-changelogs/workflow-core.md`

package preparation观察值：

```text
repository VERSION = 5.1.0
workflow-core = 0.3
```

如果 workflow-core仍是0.3并且本次 user-visible production improvement达到 release-ready：

```text
workflow-core -> 0.4
```

repository使用 kickoff-time actual latest release 的下一 PATCH。若 latest仍5.1.0，则5.1.1；如其他合法 release已占用slot，则按 canonical policy使用新的 next PATCH。

若 workflow-core已不是0.3或 routing/source存在 material overlap，停止并报告 `AI_SKILLS_VERSION_OR_SOURCE_DRIFT`。

### AI reviewed branch publication

FB-G1创建的是本地 reviewed branch/worktree。

我授权该 exact branch上的 task-owned ordinary commits，以及普通 non-force publication到同名：

```text
origin/reviewed/workflow-core--reviewed-first-bootstrap-normal-entry
```

包括首次创建这个 same-name remote branch。

不授权其他新 remote branch、force、remote/upstream remap、PR/main integration、tag/release。

==================================================
6. PHASE 5 — FREEZE EXACT CANDIDATE PAIR
==================================================

冻结并记录：

Bridge：
- exact commit；
- version 0.9.1；
- actual loaded runtime/import source；
- installed Machine Policy identity。

AI_Skills：
- exact reviewed-branch commit；
- actual repository candidate version；
- workflow-core candidate version；
- exact loaded workflow-core source/generated plugin identity。

pair freeze后任何 production source改变都使相关 gate evidence失效并需重新冻结/rerun。

==================================================
7. PHASE 6 — FB-G2 TO FB-G5
==================================================

严格执行 Goal v0.2。

FB-G2：
wrong repo/path/base、derived-branch conflict、symlink、occupied/unrelated state、unexpected local/remote branch/scope drift必须 fail closed；bootstrap和raw Git保持 prompt。

FB-G3：
对 branch create、worktree create、REQUEST/CURRENT init做 deterministic failure injection。只能 rollback exact clean current-invocation objects；dirty/ambiguous/unowned state必须保留。

FB-G4：
必须包括：
- existing local metadata resume regression；
- remote-only resume：
  - canonical main没有该 task REQUEST/CURRENT；
  - exact remote reviewed branch有 valid REQUEST/CURRENT；
  - helper checkout前先读取/验证 remote tree metadata；
  - validate task/base/path/lineage；
  - rematerialize成功；
  - wrong/missing remote metadata fail closed。

FB-G5：
使用 frozen Bridge + AI pair；
通过 current official Codex/Marketplace candidate path加载 exact workflow-core candidate；
normal entry必须真实表现：
- missing -> task bootstrap；
- existing/remote-only -> materialize-worktree --mode resume；
- Bridge capability missing/old -> early fail；
- no raw Git fallback。

Unit tests/CI/helper-only output不能代替 FB-G1/FB-G4/FB-G5。

### /tmp fixtures

我授权：

```text
/tmp/ai-bridge-091-first-bootstrap-gates/
```

仅用于 deterministic negative、failure injection和额外 fixture。

**不能**用任何 /tmp positive替代 Phase 3 real sibling-path FB-G1。

仅可删除该 gate root中本任务创建的内容。

==================================================
8. CREDENTIAL / PROVIDER BOUNDARY
==================================================

FB-G5允许使用 current existing Codex/OpenAI/Marketplace account/provider/credential scope加载 exact workflow-core candidate。

不授权：

- paid API；
- new provider/account/token；
- 复制 auth.json 到新 runtime；
- secret display；
- resurrect Bridge candidate-plugin-replay；
- unrelated global plugin mutation。

若 official candidate loading需要新的 provider/credential scope，停止。

==================================================
9. EXPLICITLY FORBIDDEN
==================================================

本授权不允许：

- Phase 3以前创建 AI implementation branch/worktree；
- 任何 raw AI_Skills implementation cutover；
- raw Git product fallback；
- force / force-with-lease；
- destructive reset/clean/restore用户工作；
- arbitrary branch/worktree；
- remote remap；
- PR/main integration/tag/release；
- paid API；
- new credential/provider；
- generic Git/worktree facade；
- permanent bootstrap allow；
- auth DB/token/receipt store；
- cross-repo ledger；
- watcher/controller/new workflow/state machine；
- unrelated external effects。

==================================================
10. COMPLETION
==================================================

只有以下全部完成后才准备 pre-final Critic package：

- exact Bridge 0.9.1 candidate；
- real installed candidate Machine Policy；
- real sibling AI FB-G1 PASS；
- workflow-core implementation只在由 FB-G1创建的 exact worktree；
- exact AI candidate；
- frozen pair；
- FB-G2–FB-G5；
- README/CHANGELOG/version closure；
- no raw fallback；
- evidence只绑定 frozen pair；
- both repos处于可审计状态。

不要自行 integrate AI main、tag、release或publish。
