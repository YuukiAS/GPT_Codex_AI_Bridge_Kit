# Bridge Kit 0.9.1 Reviewed First-Bootstrap — Execution / Recovery Contract v0.2

Contract version: **0.2**  
Date: **2026-09-24**  
Task key: **reviewed-handoff--first-bootstrap-normal-entry**  
Approved Plan: **v0.2 @ eac789a35c4a83ce015a7744c722527580e79dc8**  
Canonical Goal: **docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_goal_v0.2_2026-09-24.md**  
Kickoff Draft: **docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_kickoff_v0.2_2026-09-24.md**  
Previous Contract: **v0.1**  
Revision finding: **ER-B1-RAW-CUTOVER-BYPASSES-FIRST-BOOTSTRAP**  
Status: **DRAFT FOR EXECUTION_READY CRITIC RE-REVIEW**

This contract changes execution ordering only. It removes the raw AI_Skills implementation cutover. It does not alter Plan v0.2 architecture.

## 1. Phase order

### Phase 0 — source / identity / drift preflight

Bridge:

- verify canonical worktree `/home/yuukias/GPT_Codex_AI_Bridge_Kit`;
- verify branch `main`, origin identity, current HEAD and dirty ownership;
- read latest AGENTS/Plan/Goal/current 0.9.0 source/version.

AI_Skills:

- verify canonical checkout `/home/yuukias/AI_Skills_Collection`;
- fetch `origin/main`;
- record one exact current OID as the future first-bootstrap base;
- read latest AGENTS/version policy/VERSION/workflow-core source/plugin version;
- inspect exact future branch/worktree for conflict **without creating them**.

Stop for material scope/version conflict.

### Phase 1 — Bridge implementation only

On Bridge canonical `main`:

- implement approved 0.9.1;
- focused tests;
- full relevant/full repo regression;
- Bridge version/README/CHANGELOG closure;
- commit and freeze exact Bridge candidate.

No AI implementation worktree exists yet.

### Phase 2 — Bridge candidate runtime + Machine Policy activation

For `/home/yuukias/.codex` only:

- verify actual runtime/import source;
- at most one local editable refresh if stale;
- standard managed-file backup;
- at most one Machine Policy install;
- validate:
  - task bootstrap -> prompt;
  - materialize-worktree -> allow;
  - raw git worktree add -> prompt.

Any failure stops before AI branch/worktree creation.

### Phase 3 — primary real FB-G1 and AI implementation placement

Use the activated Bridge candidate to invoke:

```text
ai-bridge reviewed-handoff task bootstrap
```

against canonical AI_Skills with:

- task `workflow-core--reviewed-first-bootstrap-normal-entry`;
- derived branch `reviewed/workflow-core--reviewed-first-bootstrap-normal-entry`;
- exact sibling worktree `/home/yuukias/AI_Skills_Collection-workflow-core--reviewed-first-bootstrap-normal-entry`;
- expected repo `YuukiAS/AI_Skills_Collection`;
- expected base ref `origin/main`;
- expected base commit = exact Phase-0 OID.

This must run through the real candidate Machine Policy prompt/Auto-review path and consume the Kickoff's exact current-user authorization without a second manual bootstrap action.

Success creates the AI implementation worktree. This action is the primary FB-G1 positive.

Failure handling:

```text
Auto-review/sandbox rejection
or
second manual user bootstrap required
=> FB-G1 FAIL
=> stop Planner/Critic
```

Forbidden recovery:

- raw `git worktree add`;
- changing to `/tmp`;
- another branch/path/base;
- permanent bootstrap allow;
- clone/remap.

### Phase 4 — AI_Skills workflow-core implementation

Only after Phase 3 PASS:

- implement small routing refinement in the exact FB-G1-created worktree;
- source-first regenerate;
- tests/evals/full relevant regression;
- version/changelog/repository release closure;
- commit and ordinary non-force publish exact reviewed branch.

### Phase 5 — freeze cross-repo candidate pair

Record exact Bridge + AI_Skills pair identities.

No release-critical gates may mix identities.

### Phase 6 — remaining FB-G2 through FB-G5

Run deterministic and normal-entry evidence.

`/tmp/ai-bridge-091-first-bootstrap-gates/` is allowed only for negatives/additional fixtures. It is never the primary FB-G1 positive.

### Phase 7 — pre-final package

Write exact pair evidence, gate results, docs/version closure and residual limitations, then stop for independent pre-final Critic.

## 2. Bridge bootstrap recovery

Unchanged from Plan:

- before branch creation -> no cleanup;
- current invocation branch only -> delete only if exact/unchanged/unowned;
- current invocation worktree/control-init partial -> bounded cleanup only when exact and clean;
- unknown/dirty/ambiguous -> preserve + evidence;
- no force, reset-hard, general clean or arbitrary removal.

## 3. Real sibling FB-G1 recovery

The AI implementation worktree is not created through any alternate mechanism.

If the candidate bootstrap fails before mutation:

- record command identity, effective Machine Policy result and observed rejection;
- leave AI repo topology unchanged;
- FB-G1 FAIL.

If candidate bootstrap creates partial state:

- use the Bridge bootstrap's approved invocation-local recovery semantics;
- do not manually “finish” it with raw Git.

If partial state becomes ambiguous:

- preserve;
- report exact branch/worktree/control paths;
- return Planner/Critic.

## 4. AI_Skills implementation recovery after FB-G1 PASS

Once FB-G1 has successfully created the exact reviewed worktree:

- all workflow-core edits occur there;
- do not recreate/move the worktree through raw Git;
- ordinary task commits are allowed;
- exact reviewed branch can be published ordinary non-force.

If AI main advances:

- fetch for awareness;
- do not silently recreate/rebase the branch;
- resolve only within already-authorized non-destructive repo policy and frozen semantics;
- material workflow-core/version overlap returns Planner.

## 5. Remote-only resume recovery

For local metadata loss:

- fetch exact reviewed branch;
- read REQUEST/CURRENT from remote tree before checkout;
- validate task/base/path/lineage;
- only then rematerialize.

If exactly one local metadata file exists, fail `LOCAL_TASK_METADATA_PARTIAL`.

Do not copy remote metadata to canonical main.

## 6. Candidate pair recovery

Classify gate failures as:

- Bridge implementation defect;
- bootstrap authorization/Auto-review environment failure;
- workflow-core consumer defect;
- Machine Policy/runtime identity defect;
- plugin candidate loading identity defect;
- deterministic fixture defect;
- approved architecture contradiction.

For implementation defects within frozen architecture:

- repair responsible repo;
- create new candidate member;
- re-freeze exact pair;
- rerun all affected evidence.

Never combine Bridge member N with AI member N+1.

FB-G1 is special: if the real sibling path is rejected by the current authorization/Auto-review boundary, that is a design/execution-boundary failure, not a reason to choose a different fixture.

## 7. Machine Policy recovery

Target only:

```text
/home/yuukias/.codex
```

On candidate regression:

- preserve evidence;
- restore exact pre-install managed-file backup;
- stop.

No second CODEX_HOME or repeated install attempts.

## 8. Version recovery

Bridge target fixed:

```text
0.9.1
```

AI_Skills:

- observe current canonical version policy at execution;
- workflow-core 0.3 -> 0.4 only if still current and release-ready;
- repository -> next actual PATCH;
- any material source/version overlap -> Planner stop.

Do not overwrite a release slot.

## 9. External-effect recovery

No paid API.

No new provider/account/credential.

Allowed Git publication:

- Bridge existing `origin/main`, ordinary non-force;
- exact AI reviewed branch, ordinary non-force, including its first same-name remote publication **after FB-G1 has created the local branch/worktree**.

No PR/merge/tag/release.

## 10. Stop conditions

Stop immediately if:

- Bridge candidate cannot establish prompt/allow/prompt Machine Policy;
- real sibling FB-G1 cannot execute from the one approved Kickoff;
- any raw AI bootstrap/worktree fallback appears necessary;
- remote-only resume must checkout before validation or copy metadata into main;
- workflow-core needs Git mechanics;
- rollback needs destructive/force cleanup;
- candidate pair/runtime/plugin identity cannot be proven;
- a new auth store/ledger/workflow/controller/watcher becomes necessary;
- version/source drift changes frozen scope.
