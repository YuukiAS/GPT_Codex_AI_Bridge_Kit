# Bridge Kit 0.9.1 Reviewed First-Bootstrap — Execution Package v0.1

Package version: **0.1**  
Date: **2026-09-24**  
Task key: **reviewed-handoff--first-bootstrap-normal-entry**  
Review stage: **DESIGN_PASS -> EXECUTION_READY CRITIC REVIEW**

This package binds the exact reviewed design and all execution artifacts required before implementation authorization.

## 1. Approved Plan

```text
Path:
docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_plan_v0.2_2026-09-24.md

Version:
0.2

Commit:
eac789a35c4a83ce015a7744c722527580e79dc8

Critic result:
PASS
```

The design is frozen.

## 2. Canonical Goal

```text
Path:
docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_goal_v0.1_2026-09-24.md

Version:
0.1

First commit:
f348f99f66bc026e698927bda14dcf85bf2efc3a
```

The Goal mechanically preserves FB-G1 through FB-G5, both repo scopes, cutover, Machine Policy, candidate-pair, release and stop conditions.

## 3. Kickoff Draft

```text
Path:
docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_kickoff_v0.1_2026-09-24.md

Version:
0.1

First commit:
88d75089e65475252a9650df102371bb84a7906a
```

The Kickoff is not authorization until independent execution-ready Critic PASS and the user sends the approved text.

## 4. Execution / Recovery Contract

```text
Path:
docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_execution_recovery_contract_v0.1_2026-09-24.md

Version:
0.1

First commit:
051ece793645b384b2a6d6585eebcd3a5b80b4d1
```

This contract freezes execution order, bounded cleanup, pair re-freeze semantics and stop conditions.

## 5. Exact per-repo cutover

### Bridge

```text
repo:
YuukiAS/GPT_Codex_AI_Bridge_Kit

task:
reviewed-handoff--first-bootstrap-normal-entry

branch:
main

worktree:
/home/yuukias/GPT_Codex_AI_Bridge_Kit
```

No Bridge implementation branch/worktree is created. This is the non-circular cutover for the capability that will later create Reviewed worktrees.

### AI_Skills

```text
repo:
YuukiAS/AI_Skills_Collection

task:
workflow-core--reviewed-first-bootstrap-normal-entry

branch:
reviewed/workflow-core--reviewed-first-bootstrap-normal-entry

worktree:
/home/yuukias/AI_Skills_Collection-workflow-core--reviewed-first-bootstrap-normal-entry
```

Its exact base is the one `origin/main` OID resolved at approved-Kickoff preflight and recorded before mutation, conditional on no material workflow-core/version-policy overlap.

The approved Kickoff asks for one cutover-only raw exact branch/worktree creation because the future bootstrap command is not yet implemented. That effect is excluded from FB-G1/FB-G5 evidence and is not a future fallback.

## 6. Exact implementation owners

### Bridge

Primary owners:

- `ai_bridge_kit/reviewed_handoff.py`;
- Reviewed CLI parser/routing;
- shared task initializer;
- local/remote task metadata discovery;
- Reviewed README/prompts as directly needed;
- Host/Machine Policy rule and validation for bootstrap prompt vs materializer allow;
- Reviewed/Host tests;
- version/README/CHANGELOG/evidence.

### AI_Skills

Canonical workflow-core source:

- `skills/core/codex-system/codex-workflow-protocol/SKILL.md`.

Directly related references/tests/evals/generated Marketplace payload/version/changelog/repository release owners may change.

Root AGENTS is not part of the consumer fix.

## 7. Machine Policy target

```text
CODEX_HOME:
/home/yuukias/.codex
```

Execution-ready Kickoff requests:

- candidate identity check;
- at most one local-only editable refresh if runtime is stale;
- standard managed-file backup;
- at most one Host/Machine Policy install;
- validation of:
  - `task bootstrap -> prompt`;
  - `materialize-worktree -> allow`;
  - raw `git worktree add -> prompt`;
- exact backup restore on candidate regression;
- no second CODEX_HOME/install loop.

## 8. Version closure

Bridge:

```text
0.9.0 -> 0.9.1 PATCH
```

AI_Skills observed at package preparation:

```text
repository VERSION = 5.1.0
workflow-core = 0.3
```

Execution must re-read canonical version policy and current release state.

If workflow-core remains 0.3 and the improvement is release-ready:

```text
workflow-core -> 0.4
repository -> next PATCH of actual current release
```

No numeric repository patch slot is reserved by this package.

## 9. FB-G1–FB-G5 evidence contract

### FB-G1

Real installed Machine Policy + real brand-new task + current-user Kickoff. `task bootstrap` remains prompt; Auto-review/normal approval consumes the current authorization without a second manual worktree/bootstrap step. No raw fallback and no canonical-main task files.

### FB-G2

Wrong repo/base/path, task-branch conflicts, symlink/occupied/ambiguous state fail closed. Bootstrap/raw Git remain prompt.

### FB-G3

Branch/worktree/control-init deterministic failure injection. Only exact clean current-invocation state may rollback; ambiguous/dirty/unowned state is preserved.

### FB-G4

Existing local resume plus remote-only resume. Exact remote REQUEST/CURRENT is fetched/read/validated before checkout. No canonical-main copy.

### FB-G5

Freeze exact pair:

```text
Bridge candidate commit/version/runtime/Machine Policy
+
AI_Skills commit/repository version/workflow-core version/loaded plugin identity
```

Official normal workflow-core candidate entry must route:

```text
missing -> task bootstrap
existing/remote-only -> materialize-worktree --mode resume
```

No raw Git fallback. No mixed-candidate evidence.

## 10. Gate fixtures

Approved Kickoff may use:

```text
/tmp/ai-bridge-091-first-bootstrap-gates/
```

only.

Fixed gate identities:

```text
gate--first-bootstrap
/tmp/ai-bridge-091-first-bootstrap-gates/first-bootstrap-worktree

gate--workflow-core-route
/tmp/ai-bridge-091-first-bootstrap-gates/workflow-core-worktree
```

Cleanup is restricted to task-created contents under that gate root.

## 11. Authorization requested by Kickoff

The Kickoff asks the user to authorize only:

- Bridge task-owned implementation on exact main/canonical worktree;
- AI exact cutover branch/worktree creation and exact branch implementation;
- ordinary non-force publication:
  - Bridge existing origin/main;
  - exact AI reviewed branch, including one initial remote branch creation;
- one bounded Machine Policy update on `/home/yuukias/.codex`;
- local gate fixtures;
- existing Codex/OpenAI/Marketplace credential/provider scope for official workflow-core candidate loading;
- no paid API.

It does not authorize:

- force/destructive Git;
- alternate branches/worktrees;
- remote remap;
- PR/main integration/tag/release;
- new provider/account/credential;
- secret display;
- paid API;
- auth DB/ledger;
- generic Git facade;
- new workflow/controller/watcher;
- unrelated external effects.

## 12. Execution-ready Critic review object

Review together as one package:

1. Plan v0.2;
2. Canonical Goal v0.1;
3. Kickoff Draft v0.1;
4. Execution / Recovery Contract v0.1;
5. this Package v0.1.

The Critic should decide only whether the execution package faithfully implements the frozen design and whether its exact authorization/cutover/recovery boundaries are executable and sufficiently narrow.

If PASS, the user may send the **verbatim** body under:

`## Copy to Codex after execution-ready Critic PASS`

from the Kickoff Draft.

Any substantive change after PASS requires package re-review.
