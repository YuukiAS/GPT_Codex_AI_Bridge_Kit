# Bridge Kit 0.9.2 — Unattended Execution / Persistent Run Refinement Kickoff Draft v0.1

Status: **FROZEN DRAFT FOR EXECUTION_READY CRITIC / NOT YET AUTHORIZED**

Bound architecture:

```text
Task:
bridge-core--unattended-execution-refinement

Approved Plan:
docs/design/0.9.2_unattended_execution_persistent_run_refinement_plan_v0.1_2026-09-25.md

Plan commit:
9c5c7f7ac2bb110dcba98deea2b0c0d8bb7ffa1f

Canonical Goal:
docs/design/0.9.2_unattended_execution_persistent_run_refinement_goal_v0.1_2026-09-25.md

Goal first commit:
204fdd38c94b4324bc961899402a6dcd826e7d3b
```

Only the user sending the **execution-ready Critic-approved text below** to Codex constitutes execution authorization.

---

## Copy to Codex only after execution-ready Critic PASS

执行 Bridge Kit 0.9.2 `bridge-core--unattended-execution-refinement`。严格按已经通过架构审查的 Plan v0.1 和 Canonical Goal v0.1，不重新设计。

权威合同：

```text
Plan:
docs/design/0.9.2_unattended_execution_persistent_run_refinement_plan_v0.1_2026-09-25.md

Canonical Goal:
docs/design/0.9.2_unattended_execution_persistent_run_refinement_goal_v0.1_2026-09-25.md
```

核心方向已经冻结：

```text
upfront authorization readiness
!= process persistence
!= progress reporting

overnight / unattended / multi-hour
!= automatic tmux

scheduler-owned batch persistence
!= tmux persistence

terminal-owned foreground/orchestrator that must survive disconnect
=> Persistent Run/tmux remains eligible
```

不要重新讨论 Option A/B/C。

==================================================
1. REPOSITORY / GIT BOUNDARY
==================================================

Repository:

```text
YuukiAS/GPT_Codex_AI_Bridge_Kit
```

Branch:

```text
main
```

使用执行时当前 canonical `main` checkout。先核验：

- repo identity；
- origin；
- branch；
- current HEAD；
- dirty ownership。

不要创建 implementation branch/worktree。

如果有 unrelated dirty work，不要 stash/reset/restore/clean；停止并报告。

我授权：

- task-owned source/docs/tests/version 修改；
- task-owned普通 commit；
- 在 candidate tests/gates满足 Goal 条件后，ordinary non-force publication 到现有 `origin/main`。

我不授权：

- force / force-with-lease；
- destructive reset/clean/restore；
- remote remap；
- PR；
- tag/GitHub Release；
- 移动 `refs/heads/release`；
- 新 implementation branch/worktree。

==================================================
2. IMPLEMENTATION SCOPE
==================================================

只实施 Goal 已冻结的 Option B 小型 refinement。

主要允许修改：

```text
chatgpt/TASK_WRITER_PROMPT.md
chatgpt/NEXT_TASK_PROMPT.md
templates/prompts/CHATGPT_RULES.md
codex/CODEX_START_PROMPT.md

templates/persistent_run/README.md
templates/persistent_run/CONTRACT_TEMPLATE.md
templates/persistent_run/KICKOFF_TEMPLATE.md
templates/persistent_run/AGENTS_SNIPPET.md

README.md
QUICKSTART.md
AGENTS.md
CHANGELOG.md

tests/test_upfront_authorization_persistent_authoring.py
tests/test_persistent_run.py
directly matching focused tests if genuinely necessary

pyproject.toml
ai_bridge_kit/__init__.py
```

冻结 should-not-change：

```text
ai_bridge_kit/persistent_run.py
ai_bridge_kit/host.py
templates/host/rules/ai-bridge-global.rules
workflow schemas/state machines
release/ref machinery
YuukiAS/Distributed_Imaging_Inference
```

如果发现 approved behavior 必须修改 should-not-change surface：

```text
RESULT=NEEDS_GPT_PLANNER
REASON=APPROVED_MINIMAL_SURFACE_INSUFFICIENT
```

不要自己扩 scope。

==================================================
3. REQUIRED PRODUCT BEHAVIOR
==================================================

任务写作和 installed Persistent Run guidance 必须把三件事分开：

1. upfront authorization readiness；
2. process persistence topology；
3. project-native progress reporting。

不要再写成：

```text
overnight/unattended/multi-hour -> Persistent Run/tmux
```

正确选择：

```text
long/unattended intent
-> first inspect foreseeable HUMAN_ONLY effects
-> then decide lifetime owner

scheduler-owned sbatch / already-detached service
-> no tmux solely for duration

terminal-owned foreground/orchestrator that must survive disconnect
-> Persistent Run/tmux eligible

no survive-disconnect requirement
-> ordinary execution
```

已有 explicit：

```text
Persistent execution: REQUIRED
Backend: tmux
```

继续兼容，不自动迁移。

progress `report/latest` 必须明确可独立于 tmux 使用。

==================================================
4. DII BOUNDARY
==================================================

DII 只允许 read-only reference。

不要修改：

```text
YuukiAS/Distributed_Imaging_Inference
```

特别是现有 Goal、运行中实验、Slurm job 都不能动。

如需写 DII 922349 的解释，必须保持真实：

- `BatchFlag=1`；
- 它由 `sbatch` 创建；
- 922349 是 scheduler-owned batch holder；
- Slurm 已拥有该 holder 的 lifetime；
- GPU work 通过 `srun --jobid=922349 --overlap ...`进入；
- tmux 的价值来自 terminal-owned orchestrator / repeated `srun` lifecycle，而不是 922349 本身。

==================================================
5. TESTS / CANDIDATE FREEZE
==================================================

每个行为修改配 matching tests。

先跑 focused regressions，再跑 full repository suite。

关键词/source presence tests 可以作为 cheap regression，但不能替代 G1/G2。

如果 deterministic tests通过：

- candidate version设为 `0.9.2`；
- README / QUICKSTART / AGENTS / CHANGELOG同步；
- 冻结 exact production candidate commit；
- 记录：

```text
PRODUCTION_CANDIDATE_COMMIT=<sha>
VERSION=0.9.2
```

candidate freeze之后，如果 production source/template/version行为发生任何改变，必须形成新 candidate并重跑受影响 gates。

==================================================
6. TASK-OWNED TEMP / TMUX AUTHORIZATION
==================================================

我授权本任务使用并在结束时清理：

```text
/tmp/ai-bridge-092-unattended-gates/
```

仅用于 Goal 的 bounded local fixtures/evidence。

我同时明确授权 final candidate G4 使用一个 exact task-owned tmux smoke session：

```text
ai-bridge-092-g4-smoke
```

仅可：

- 创建这个 exact session；
- 运行 Goal 所定义的 harmless bounded smoke；
- 用 read-only tmux commands观察；
- 删除/kill这个 exact task-owned smoke session。

这不授权：

- 其他 tmux session mutation；
- `setsid`；
- `nohup`；
- `screen`；
- broad background process control；
- scheduler submission/allocation/cancellation；
- Host allowlist修改。

如果 exact bounded G4 tmux effect 在当前用户授权下仍被 Auto-review 拒绝，停止 Planner/Critic，不要绕过。

==================================================
7. G1 / G2 NORMAL CODEX + AUTO-REVIEW GATE
==================================================

这是本任务最重要的 acceptance boundary。

G1/G2 必须在 **final candidate 冻结之后**，通过 fresh normal Codex / Auto-review / current-user context 产生真实行为证据。

不能用：

- keyword test；
- source scan；
- direct Python function；
- fake approval callback；
- helper-only output；
- bypass Auto-review 的 nested route。

### 重要：本 kickoff 故意不预授权 G1/G2 内部的 approval-sensitive probe effect

原因是 G1 本身必须验证：

“如果 final candidate 的 frozen Goal 需要一个可预见但尚未授权的 effect，Codex 会在 substantive work 前问。”

因此，本 kickoff 只授权 Executor **准备** G1/G2 disposable fixture和两段 exact user messages。

在 final candidate和其他 gates都准备完以后：

1. 先把 disposable fixture、exact initial prompt、exact bounded approval reply全部准备好；
2. 再一次性向我请求执行 G1/G2 normal-surface probe；
3. 不得让我在 candidate尚未完成时提前参与；
4. 不得让我自己拼 prompt。

G1：

- fresh normal Codex context消费 exact final candidate；
- initial prompt intentionally不授权那个 exact probe effect；
- Codex应在 substantive precursor work前提出一次 bounded question。

我发送预先准备好的 exact approval reply 后：

G2：

- 同一 context / 同一 frozen Goal恢复；
- 到达同一个 exact effect时不得再次向我索取相同授权；
- Auto-review不得因 stage变化再次阻塞 exact same effect。

如果 normal surface出现：

- question auto-resolve错误；
- context resume丢失；
- duplicate approval；
- exact authorized effect仍被 Auto-review阻塞；
- final candidate无法被正常 surface实际消费；

立即：

```text
RESULT=NEEDS_GPT_PLANNER
G1_G2_NORMAL_ENTRY=FAIL
```

不要：

- 改 Host allowlist；
- 改成 `approval_policy=never`；
- 加 auth DB/state；
- 用 bypass helper；
- 把失败改写成 keyword PASS。

==================================================
8. REMAINING GATES
==================================================

必须按 Canonical Goal 执行 G1–G9。

特别要求：

G3：
scheduler-native overnight authored case 不得仅因 duration 加 tmux。

G4：
使用上面授权的 `ai-bridge-092-g4-smoke` 做真实 bounded tmux lifecycle smoke。

G5：
无 tmux session 时，真实执行：

```text
ai-bridge persistent-run report --progress <task-owned-progress-json>
ai-bridge persistent-run latest --progress <task-owned-progress-json>
```

G6：
new resource/artifact/provider/purpose/backend/cost/destructive scope 仍重新 gated。

G7：
Machine Policy / execpolicy 不扩大；tmux mutation、sbatch、srun、generic shell、generic Python继续保持原有边界。

G8：
已有 explicit tmux Goal 兼容；DII 不迁移、不修改。

G9：
README / QUICKSTART / AGENTS / prompts / installed templates语义一致。

==================================================
9. MACHINE / AUTH / PROVIDER BOUNDARY
==================================================

本任务不授权：

- `ai-bridge host install`；
- 修改任何 `$CODEX_HOME` Machine Policy；
- 修改 host rules；
- new credential/provider/account/token；
- secret display；
- paid API；
- external upload；
- production deployment；
- Slurm resource mutation。

当前 Machine Policy保持不变。

如果 G1/G2 或 G4 只能通过扩大 Machine Policy 才能 PASS，返回 Planner。

==================================================
10. RELEASE BOUNDARY
==================================================

本任务允许形成 0.9.2 candidate，并按上面 Git boundary发布 candidate到现有 `origin/main`。

本任务**不授权正式 release**。

禁止：

```text
refs/heads/release advance
tag
GitHub Release
formal machine-sync closure
```

只有 G1–G9、pre-final/final review、现有 release owner closure全部完成后，才可以单独推进 formal release ref。

==================================================
11. FINAL EVIDENCE
==================================================

把轻量 evidence留在：

```text
results/bridge-core--unattended-execution-refinement/
```

至少：

```text
EVIDENCE.md
GATE_MATRIX_RESULT.md
FINAL_REPORT.md
```

必须明确报告：

- production candidate commit；
- version 0.9.2；
- changed files；
- `persistent_run.py` unchanged；
- Machine Policy unchanged；
- DII unchanged；
- G1–G9 evidence locator；
- G1/G2 normal-surface result；
- G4 real tmux result；
- G5 no-tmux progress result；
- focused/full tests；
- README checked/updated；
- release ref unchanged。

成功实施后停在：

```text
IMPLEMENTATION_COMPLETE=YES
G1_G9=PASS
PRODUCTION_CANDIDATE_COMMIT=<sha>
VERSION=0.9.2
RELEASE_REF_ADVANCED=NO
NEXT_HANDOFF=PRE_FINAL_CRITIC
```

不要自行推进正式 release。
