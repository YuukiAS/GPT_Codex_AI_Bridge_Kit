# Bridge Kit 0.9.1 Reviewed First-Bootstrap — Execution-Ready Critic Handoff v0.2

Date: **2026-09-24**  
Task key: **reviewed-handoff--first-bootstrap-normal-entry**  
Review stage: **EXECUTION_READY_REVIEW**  
Stable blocker under re-review: **ER-B1-RAW-CUTOVER-BYPASSES-FIRST-BOOTSTRAP**  
Package commit: **2d2d41e79f3abd52ecdc8416d4cf555fee28ada1**

以下内容可完整复制到独立 Critic thread。

~~~text
你是 AI Research Stack 的独立 Critic。

target_repo:
YuukiAS/GPT_Codex_AI_Bridge_Kit

target_domain:
Reviewed Handoff / Bridge Kit core

design_topic_or_task_key:
reviewed-handoff--first-bootstrap-normal-entry

review_stage:
EXECUTION_READY_REVIEW

source_branch/ref:
main

related consumer repo:
YuukiAS/AI_Skills_Collection

stable blocker:
ER-B1-RAW-CUTOVER-BYPASSES-FIRST-BOOTSTRAP

==================================================
REVIEW OBJECT
==================================================

Approved Plan:

docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_plan_v0.2_2026-09-24.md

Plan version:
0.2

Plan commit:
eac789a35c4a83ce015a7744c722527580e79dc8

Revised Canonical Goal:

docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_goal_v0.2_2026-09-24.md

Goal version:
0.2

Goal first commit:
399db248613948a9f4ed02555d456418ad9a37ee

Revised Kickoff Draft:

docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_kickoff_v0.2_2026-09-24.md

Kickoff version:
0.2

Kickoff first commit:
88126e5d8bb650ed110a70f9153cd7254590d544

Revised Execution / Recovery Contract:

docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_execution_recovery_contract_v0.2_2026-09-24.md

Contract version:
0.2

Contract first commit:
2d314edaa373b299ee54889ee8afca7bdc1e3064

Revised Execution Package:

docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_execution_package_v0.2_2026-09-24.md

Package version:
0.2

Package commit:
2d2d41e79f3abd52ecdc8416d4cf555fee28ada1

Previous package:

docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_execution_package_v0.1_2026-09-24.md

Previous package commit:
68118a7d3c091b54dcaa8492b7a52fa8dc25a3c9

Previous Critic result:
REVISE

This is READ-ONLY execution-ready re-review.

Do not:
- modify Git;
- implement production;
- create branch/worktree;
- install/modify Machine Policy;
- launch Executor;
- call paid API;
- start automation;
- revise Plan v0.2 architecture.

==================================================
0. GOVERNANCE / SOURCE
==================================================

Read latest AI_Skills_Collection main:

- docs/workflows/PLANNER_ROLE_CONTRACT.md
- docs/workflows/CRITIC_ROLE_CONTRACT.md
- docs/workflows/PLUGIN_CAPABILITY_GATE_POLICY.md
- docs/workflows/PLUGIN_VERSIONING_AND_CHANGELOGS.md
- AGENTS.md

Then read Bridge latest main:

- AGENTS.md
- approved Plan v0.2
- revised Goal v0.2
- revised Kickoff v0.2
- revised Execution/Recovery Contract v0.2
- revised Package v0.2
- previous Package v0.1 only to compare the removed raw cutover

Read current Bridge/AI source only as needed to verify execution feasibility.

Do not reopen already-PASS design blockers.

==================================================
1. PRIOR BLOCKER
==================================================

The previous package failed because it created the AI_Skills implementation
branch/worktree through raw git worktree add BEFORE task-bootstrap existed.

That was the same real mechanism that originally failed for the user.

The v0.2 execution package must close this by ORDERING ONLY.

Check that no raw AI implementation cutover remains in any authoritative v0.2
Goal/Kickoff/Contract/Package.

==================================================
2. REQUIRED PHASE ORDER
==================================================

The exact new order must be:

PHASE 0:
ordinary preflight only.

- Bridge identity/drift.
- AI_Skills fetch/read/version/base discovery.
- record exact AI origin/main OID.
- inspect exact sibling path/derived branch for conflict.
- NO AI branch/worktree creation.

PHASE 1:
Bridge only.

- implement approved Bridge 0.9.1 on canonical Bridge main;
- tests/regressions;
- version/README/CHANGELOG closure;
- freeze exact Bridge candidate.

PHASE 2:
activate Bridge candidate.

- verify actual runtime identity;
- bounded editable refresh only if stale;
- standard backup;
- at most one Machine Policy install on /home/yuukias/.codex;
- validate:
  task bootstrap -> prompt
  materialize-worktree -> allow
  raw git worktree add -> prompt

PHASE 3:
REAL FB-G1.

Use the NEW candidate command:

ai-bridge reviewed-handoff task bootstrap

against:

repo:
YuukiAS/AI_Skills_Collection

task:
workflow-core--reviewed-first-bootstrap-normal-entry

derived branch:
reviewed/workflow-core--reviewed-first-bootstrap-normal-entry

exact sibling worktree:
/home/yuukias/AI_Skills_Collection-workflow-core--reviewed-first-bootstrap-normal-entry

base:
exact Phase-0 origin/main OID

This must go through real candidate Machine Policy prompt/Auto-review and consume
the exact current-user Kickoff authorization without another manual bootstrap
action.

If rejected:
FB-G1=FAIL
stop Planner/Critic

Never:
- raw git fallback;
- /tmp substitution;
- alternate branch/path/base;
- permanent allow;
- clone/remap.

Only after FB-G1 PASS may Phase 4 begin.

PHASE 4:
implement workflow-core in the exact worktree created by Phase 3.

PHASE 5:
freeze exact Bridge + AI_Skills pair.

PHASE 6:
run remaining FB-G2–FB-G5.

Check the package matches this ordering exactly.

==================================================
3. REAL SIBLING FB-G1
==================================================

The primary FB-G1 positive must now be the REAL sibling AI_Skills implementation placement:

/home/yuukias/AI_Skills_Collection-workflow-core--reviewed-first-bootstrap-normal-entry

It cannot be replaced by:

- /tmp positive fixture;
- unit test;
- helper dry run;
- raw Git implementation setup.

The Kickoff itself explicitly authorizes the exact repo/task-derived branch/worktree/base effect.

Check that:

current-user Kickoff = authorization

while:

CLI assertions != authorization

and:

task bootstrap = prompt, not permanent allow.

If Auto-review still asks the user to manually create the worktree, FB-G1 fails.
That failure cannot be normalized away.

==================================================
4. RAW CUTOVER REMOVAL
==================================================

Confirm v0.2 removes:

- the one-time raw AI git worktree add cutover;
- authorization for raw cutover;
- language saying raw cutover is excluded from FB-G1/FB-G5;
- assumption that AI implementation worktree exists before Bridge candidate activation.

Raw git worktree add must remain prompt/gated and never become a normal
implementation recovery route.

==================================================
5. BRIDGE IMPLEMENTATION PLACEMENT
==================================================

Bridge still implements 0.9.1 on:

repo:
YuukiAS/GPT_Codex_AI_Bridge_Kit

branch:
main

worktree:
/home/yuukias/GPT_Codex_AI_Bridge_Kit

No Bridge task worktree is created.

This is allowed by the already-approved design/cutover strategy and avoids
circular dependence.

Review that Phase 1 contains no AI production mutation.

==================================================
6. AI_SKILLS PRODUCTION SCOPE AFTER FB-G1
==================================================

Only after Phase 3 PASS, workflow-core implementation may begin in the
FB-G1-created sibling worktree.

Canonical production owner:

skills/core/codex-system/codex-workflow-protocol/SKILL.md

The consumer route remains:

missing
-> Bridge task bootstrap

existing local/exact remote
-> Bridge materialize-worktree --mode resume

Bridge capability/version missing
-> fail early

No raw Git fallback.

No root AGENTS duplication.

Review that no AI production modification is authorized before real FB-G1.

==================================================
7. MACHINE POLICY AUTHORIZATION
==================================================

Target fixed:

/home/yuukias/.codex

Kickoff v0.2 authorizes only after Bridge candidate freeze:

- actual runtime identity check;
- max one local-only editable refresh if stale;
- standard backup;
- max one host install;
- validation;
- exact backup restore on candidate regression;
- no second CODEX_HOME/install loop.

Expected decisions:

task bootstrap -> prompt
materialize-worktree -> allow
raw git worktree add -> prompt

Check that this is sufficiently bounded and is the only global/live mutation.

==================================================
8. VERSION / PUBLICATION BOUNDARY
==================================================

Bridge:
0.9.1 PATCH.

AI_Skills package observation:
repo 5.1.0
workflow-core 0.3

Execution rereads canonical policy/current values.

If workflow-core still 0.3 and release-ready:
-> 0.4

AI repository:
-> next actual PATCH, no pre-reserved number.

AI reviewed branch is created locally by Phase-3 Bridge task bootstrap.

After implementation, Kickoff authorizes ordinary non-force publication of only:

origin/reviewed/workflow-core--reviewed-first-bootstrap-normal-entry

including first same-name remote branch publication.

No PR/main integration/tag/release.

==================================================
9. FB-G2–FB-G5
==================================================

FB-G2:
exact-scope fail-closed; prompt boundaries unchanged.

FB-G3:
current-invocation-only rollback.

FB-G4:
existing local resume + remote-only resume; exact remote metadata read/validated
before checkout; no main copy.

FB-G5:
exact frozen Bridge + AI_Skills pair through official loaded workflow-core
candidate entry:
missing -> bootstrap
existing/remote-only -> resume
no raw fallback.

Check that /tmp fixtures are explicitly limited to deterministic negatives /
additional fixtures and cannot satisfy FB-G1.

==================================================
10. AUTHORIZATION BOUNDARY
==================================================

The future user Kickoff may authorize only:

Bridge:
- current main task-owned implementation/commit/non-force existing origin/main publication.

Machine:
- one bounded candidate activation on /home/yuukias/.codex.

AI:
- read/fetch/base discovery before FB-G1;
- exact candidate task-bootstrap effect in Phase 3;
- only after success, task-owned implementation in that exact worktree;
- ordinary non-force exact reviewed branch publication.

Fixtures:
- task-owned /tmp gate root for deterministic cases only.

Existing provider/credential:
- current Codex/OpenAI/Marketplace scope only.

Forbidden:
- raw AI worktree cutover;
- alternate worktree/path/branch;
- force/destructive Git;
- remap;
- PR/main integration/tag/release;
- paid API;
- new credentials/provider;
- generic Git facade;
- permanent bootstrap allow;
- auth DB/ledger;
- unrelated external effects.

Review whether this is complete but not broader than necessary.

==================================================
11. OUTPUT
==================================================

CRITIC_RESULT=PASS | REVISE
REVIEW_STAGE=EXECUTION_READY

REVIEWED_PLAN_VERSION=0.2
REVIEWED_PLAN_PATH=docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_plan_v0.2_2026-09-24.md
REVIEWED_PLAN_COMMIT=eac789a35c4a83ce015a7744c722527580e79dc8

GOAL_PATH=docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_goal_v0.2_2026-09-24.md
GOAL_VERSION=0.2

KICKOFF_PATH=docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_kickoff_v0.2_2026-09-24.md
KICKOFF_VERSION=0.2

RECOVERY_CONTRACT_PATH=docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_execution_recovery_contract_v0.2_2026-09-24.md
RECOVERY_CONTRACT_VERSION=0.2

PACKAGE_PATH=docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_execution_package_v0.2_2026-09-24.md
PACKAGE_VERSION=0.2
PACKAGE_COMMIT=2d2d41e79f3abd52ecdc8416d4cf555fee28ada1

ER_B1_RAW_CUTOVER_BYPASSES_FIRST_BOOTSTRAP=CLOSED | OPEN
RAW_CUTOVER_REMOVED=YES | NO
PHASE_ORDER=PASS | REVISE
REAL_SIBLING_FB_G1=PASS | REVISE
AUTHORIZATION_BOUNDARY=PASS | REVISE
MACHINE_POLICY_BOUNDARY=PASS | REVISE
AI_SKILLS_POST_FB_G1_ONLY=PASS | REVISE
FB_G2_G5_PRESERVED=YES | NO
READY_FOR_CODEX=YES | NO

BLOCKERS=
NON_BLOCKING_NOTES=
NEXT_HANDOFF=CODEX | PLANNER

If REVISE:
block only on ER-B1 not actually being closed, a concrete authorization/execution
gap introduced by the reordered package, or a direct contradiction with approved
Plan v0.2.
Do not reopen Plan architecture.

If PASS:

1. explain briefly in Chinese that the package now implements/activates Bridge first,
   then uses the real new task-bootstrap command to create the actual AI_Skills
   sibling worktree as FB-G1;
2. state clearly that no raw cutover remains and /tmp cannot substitute for FB-G1;
3. state:
   READY_FOR_CODEX=YES
4. identify exactly:
   docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_kickoff_v0.2_2026-09-24.md
   as the only approved Kickoff;
5. do NOT rewrite it. The user should send the exact text under
   “## Copy to Codex after execution-ready Critic PASS”.

NEXT_HANDOFF=CODEX
~~~
