I explicitly authorize execution of the referenced frozen Goal using its Persistent Run contract.

Goal: `__GOAL_PATH__`

Start or resume the required persistent tmux session for this Goal, execute the already-authorized work, use only the resources authorized by that Goal, write its declared artifacts, and allow the run to continue after Codex / SSH disconnect.

Do not ask again merely because this same frozen Goal is long-running, detached or unattended.

This authorization does not permit scope expansion, different compute resources, resource cancellation, sudo, or fallback to another persistence backend unless the Goal explicitly permits it.
