# Bridge Kit 0.9.1 — First Bootstrap Authorization Transport Recovery Execution Package v0.1

Package version: **0.1**  
Date: **2026-09-24**  
Review stage: **RECOVERY_DESIGN_PASS -> EXECUTION_READY CRITIC REVIEW**  
Target repo: **YuukiAS/GPT_Codex_AI_Bridge_Kit**  
Related gate repo: **YuukiAS/AI_Skills_Collection**  
Parent task: **reviewed-handoff--first-bootstrap-normal-entry**

This package is intentionally minimal. It contains one Bridge-only implementation/activation stage and one real AI_Skills repo-local FB-G1-AT gate. It stops before workflow-core implementation.

## 1. Approved design

```text
Parent Plan:
docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_plan_v0.2_2026-09-24.md
commit:
eac789a35c4a83ce015a7744c722527580e79dc8

Recovery Plan:
docs/design/0.9.1_reviewed_handoff_first_bootstrap_authorization_transport_recovery_plan_v0.5_2026-09-24.md
commit:
bdeda15529fe784515185165b18cfbac4531951c

Critic result:
PASS
```

The recovery design is frozen.

## 2. Package artifacts

### Canonical Goal

```text
docs/design/0.9.1_reviewed_handoff_first_bootstrap_authorization_transport_recovery_goal_v0.1_2026-09-24.md
first commit:
adbb64782df12060903aa9ed1a810bf12aeb6a85
```

### Kickoff Draft

```text
docs/design/0.9.1_reviewed_handoff_first_bootstrap_authorization_transport_recovery_kickoff_v0.1_2026-09-24.md
first commit:
cfebda2a9ef5a11cc091ccb5a91aa62b9051f709
```

The Kickoff contains two reviewed repo-local prompts:

- Part A -> Bridge Codex;
- Part B -> AI_Skills Codex after Part A PASS.

### Execution / Recovery Contract

```text
docs/design/0.9.1_reviewed_handoff_first_bootstrap_authorization_transport_recovery_execution_contract_v0.1_2026-09-24.md
first commit:
f96a6196373fefb08b82afe3de70460c240184a9
```

## 3. Source observations at package preparation

Bridge latest main at package preparation contains only recovery design/docs after the implemented 0.9.1 candidate lineage; current package/version source is already:

```text
Bridge version:
0.9.1

implemented pre-recovery candidate:
44965d6707d27b48726a967fba46a9d836b7010c
```

AI_Skills package-preparation observations:

```text
repository VERSION:
5.1.0

workflow-core:
0.3
```

This package does not modify those AI_Skills production values.

The real gate resolves its exact base mechanically after the approved `git fetch --all --prune`; no free-form base selection is permitted.

## 4. Exact execution placement

### Part A — Bridge

```text
repo:
YuukiAS/GPT_Codex_AI_Bridge_Kit

branch:
main

worktree:
/home/yuukias/GPT_Codex_AI_Bridge_Kit
```

No new Bridge branch/worktree.

### Part B — AI_Skills gate

```text
repo:
YuukiAS/AI_Skills_Collection

cwd:
/home/yuukias/AI_Skills_Collection

task:
workflow-core--reviewed-first-bootstrap-normal-entry

derived branch:
reviewed/workflow-core--reviewed-first-bootstrap-normal-entry

derived sibling:
/home/yuukias/AI_Skills_Collection-workflow-core--reviewed-first-bootstrap-normal-entry
```

The exact base is:

```text
post-sync refs/remotes/origin/main OID
```

resolved after the canonical-profile check and `git fetch --all --prune`.

This is deterministic, not a caller-selected commit.

## 5. Exact Bridge implementation scope

Allowed:

- `ai_bridge_kit/reviewed_handoff.py`;
- direct Reviewed CLI parser changes needed to enforce cwd repo-local scope;
- `templates/host/rules/ai-bridge-global.rules`;
- `ai_bridge_kit/host.py` only if direct policy validation needs adjustment;
- `tests/test_reviewed_handoff.py`;
- `tests/test_host_policy.py`;
- direct CLI/version tests if required;
- `README.md`;
- `CHANGELOG.md`;
- `results/reviewed-handoff--first-bootstrap-normal-entry/**`.

Required behavior:

- repo-local self-scope;
- deterministic sibling;
- post-sync exact `origin/main` base;
- canonical origin-only fetch profile;
- RL-B1 executable fence;
- RL-B1 output symlink fence;
- zero bootstrap network;
- `task bootstrap -> allow`;
- existing `materialize-worktree -> allow`;
- raw `git worktree add -> prompt`.

No workflow-core modification in this package.

## 6. Machine Policy boundary

Target:

```text
/home/yuukias/.codex
```

Part A approved scope requests only:

- actual runtime identity check;
- at most one local editable refresh if stale;
- standard backup;
- at most one host install;
- validation;
- exact restore on Part-A activation failure.

No second CODEX_HOME/install loop.

## 7. Real FB-G1-AT admission

Part B must fail closed unless AI_Skills has the exact canonical safe class:

```text
configured remotes = ["origin"]

remote.origin.skipFetchAll = unset/false

remote.origin.skipDefaultUpdate = unset/false

remote.origin.fetch = exactly:
+refs/heads/*:refs/remotes/origin/*
```

No config mutation/normalization is allowed.

Then:

```text
git fetch --all --prune
```

must succeed through the existing low-friction rule.

Post-sync:

- capture `origin/main` OID;
- require `origin/reviewed/<task_key>` absent.

## 8. Real FB-G1-AT proof

The gate must prove on the actual sibling path:

- one Part-B current-user Kickoff;
- no second approval/manual bootstrap;
- installed exact Bridge recovery candidate;
- `task bootstrap -> allow`;
- raw `git worktree add -> prompt`;
- real sibling created at:
  `/home/yuukias/AI_Skills_Collection-workflow-core--reviewed-first-bootstrap-normal-entry`;
- REQUEST/CURRENT valid;
- canonical AI_Skills main not polluted;
- zero network inside bootstrap;
- no raw Git fallback;
- workflow-core untouched.

A `/tmp` positive is insufficient.

## 9. Failure semantics

### Part A failure

- do not run Part B;
- restore Machine Policy backup if activation had started;
- return Planner.

### Part B noncanonical repo

- no config normalization;
- no raw Git;
- no worktree creation;
- return `FB_G1_AT=NOT_ELIGIBLE`;
- workflow-core remains paused.

### Part B product failure

If allow still prompts, sandbox still blocks sibling creation, or runtime identity is wrong:

- preserve evidence;
- no raw Git/`/tmp` fallback;
- workflow-core remains paused;
- return Planner;
- Bridge owner restores the pre-install Machine Policy backup before the next recovery attempt.

### Part B success

- preserve the created Reviewed sibling;
- do not edit workflow-core;
- return Planner.

## 10. Authorization requested by the reviewed Kickoff

Part A asks the user to authorize only:

- Bridge main task-owned recovery implementation;
- ordinary bounded non-force publication of Bridge main through the existing Bridge publisher;
- one bounded candidate activation on `/home/yuukias/.codex`.

Part B asks the user to authorize only:

- the exact repo-local AI_Skills first-bootstrap task;
- deterministic sibling derived by the helper;
- current post-sync `origin/main` as base;
- local-only branch/worktree/control-state creation.

Not authorized:

- raw AI_Skills `git worktree add`;
- workflow-core source edits;
- AI reviewed-branch publication;
- cross-repo target selection;
- `/tmp` fallback;
- Git config normalization;
- force/destructive Git;
- PR/main integration/tag/release;
- paid API;
- new provider/account/credential;
- unrelated external effects.

## 11. Execution-ready Critic review object

Review together:

1. Recovery Plan v0.5;
2. Recovery Goal v0.1;
3. Recovery Kickoff Draft v0.1;
4. Recovery Execution Contract v0.1;
5. this Recovery Execution Package v0.1.

The Critic should verify only execution readiness and authorization fidelity for the already-PASS recovery design.

If PASS:

- Part A is the only prompt to send to Bridge Codex;
- after Part A reports `READY_FOR_AI_SKILLS_FB_G1_AT=YES`, Part B is the only prompt to send to AI_Skills Codex;
- workflow-core implementation still waits for the real gate result.

Any substantive semantic change after execution-ready PASS requires re-review.
