# Phase 0 Goal Prompt Note

The canonical executable Goal prompt for the accepted 0.8.0 Phase 0 probe is:

```text
prompts/tasks/0.8.0_candidate_plugin_replay_phase0_probe.md
```

Its scope is deliberately limited to runtime-path selection and evidence capture. It must not be interpreted as authorization to implement the 0.8.0 production command.
