# Bridge Kit 0.9.0 Low-Friction Operations Convergence — Recovery Kickoff Draft v0.2

Status: **DRAFT FOR RECOVERY EXECUTION-READY CRITIC REVIEW / NOT YET USER AUTHORIZATION**

Bound objects:

- Critic-approved amended Plan:
  `docs/design/0.9.0_low_friction_operations_convergence_plan_v0.4_2026-09-24.md`
- Plan commit:
  `bf20c4ff4f2a72f2ef2b5a57cd236519d91ef4cd`
- Amended Recovery Goal:
  `docs/design/0.9.0_low_friction_operations_convergence_recovery_goal_v0.2_2026-09-24.md`
- Recovery Goal first commit:
  `71f39b51b53ad3bff4e89567293eae38dd88aa82`
- Same task:
  `bridge-core--low-friction-operations-convergence`
- Current recovery source candidate before this amendment:
  `37a773cd83971d64f194a7d82c55a24889bd8164`

Only the user sending the Critic-approved text below constitutes execution authorization.

---

## Copy to Codex after recovery execution-ready Critic PASS

你现在继续同一个 Bridge Kit 0.9.0 recovery task，只做已经由 Plan v0.4 批准的 GitHub HTTPS-only publisher scope correction。

```text
Repository:
YuukiAS/GPT_Codex_AI_Bridge_Kit

Task:
bridge-core--low-friction-operations-convergence

Branch:
main

Canonical worktree:
/home/yuukias/GPT_Codex_AI_Bridge_Kit

Machine Policy identity:
CODEX_HOME=/home/yuukias/.codex
```

权威合同：

```text
Approved amended Plan:
docs/design/0.9.0_low_friction_operations_convergence_plan_v0.4_2026-09-24.md

Amended Recovery Goal:
docs/design/0.9.0_low_friction_operations_convergence_recovery_goal_v0.2_2026-09-24.md
```

先读取 latest `main`、根 `AGENTS.md`、上述 Plan/Goal、当前
`results/bridge-core--low-friction-operations-convergence/EVIDENCE.md` 与
`FINAL_REPORT.md`。

不要创建 successor，不要重新设计 Persistent Run、Reviewed materializer、plugin-replay、Notifications、taxonomy、wrapper count 或 one-release decision。

### 我现在授权的 production 修复范围

只允许修改现有 single `ai-bridge host publish-current-branch` 及直接 tests/docs，使 trusted low-friction publisher 的**正向 transport 仅为 GitHub HTTPS**：

```text
https://github.com/<owner>/<repo>.git
```

必须使用 effective fetch/push URL（包含 Git URL rewriting 之后的结果）做 publisher-specific classification。

下列情况必须在第一次 remote/network Git operation 之前 fail closed：

```text
git@github.com:<owner>/<repo>.git
ssh://git@github.com/<owner>/<repo>.git
other SSH/scp-style/custom transport
literal HTTPS rewritten effectively to SSH/custom
effective fetch HTTPS but effective push SSH/custom
```

失败后只回 ordinary raw `git push` approval；不得自动改 remote、不得造第二个 wrapper。

不要全局收窄 shared `_canonical_repo_identity()`，避免误伤 Reviewed Handoff / local disposable repo identity。

### 必须保留的 security negatives

以下原有 fences/canaries 全部保留：

- `GIT_SSH`
- `GIT_SSH_COMMAND`
- `GIT_SSH_VARIANT`
- `core.sshCommand`
- local/worktree/command `credential.helper`
- `GIT_ASKPASS`
- `SSH_ASKPASS`
- `core.askPass`
- `GIT_CONFIG_COUNT`
- `GIT_CONFIG_KEY_*`
- `GIT_CONFIG_VALUE_*`
- `GIT_CONFIG_GLOBAL`
- `GIT_CONFIG_SYSTEM`
- `GIT_CONFIG_NOSYSTEM`
- multiple destination / pushurl mismatch
- pre-push hook
- reviewed_runner guard
- mirror/followTags/submodule/push-option/signing expansion
- final branch/HEAD/destination/config drift

Rejected case必须证明：

```text
no injected executable runs
no remote/network publication through trusted helper
```

### 不再要求的 SSH 正向 gate

我明确不要求、也不授权为了本任务配置真实 GitHub SSH 正向 publication。

不要：

- create/upload SSH key；
- start/manage ssh-agent；
- modify ~/.ssh/config；
- modify known_hosts；
- mutate SSH_AUTH_SOCK；
- switch remote to SSH。

真实 GitHub SSH positive publication **不是 0.9.0 release gate**。

### 受影响的最终 gates

只重跑受此次 scope correction 影响的：

**G3**
- existing authorized same-name branch；
- GitHub HTTPS；
- existing normal system/global/gh credential helper；
- bounded publisher；
- no repeated approval；
- exact ordinary non-force publication。

**G4**
- GitHub HTTPS positive；
- all retained exact-effect/security negatives；
- effective SSH/scp/custom remote fail-closed before network；
- literal HTTPS rewritten to SSH/custom negative；
- HTTPS fetch / SSH push mismatch negative；
- 不再要求 SSH positive publication。

**G14**
- source/runtime/docs/evidence 全部绑定同一个 new final candidate；
- final documentation必须明确：
  `trusted low-friction = GitHub HTTPS`
  `SSH/custom = ordinary approval`。

另外运行必要的 focused/full should-not-change regressions，确认 B1/B2/B3、Reviewed Handoff、Persistent Run、Notifications、plugin-replay、G13/G15 没被这次窄改动破坏。

### Git / publication授权

我授权：

- 当前 `main` 上 task-owned ordinary commits；
- 本任务 ordinary non-force publication 到 `origin/main`；
- 使用现有 bounded publisher 做 GitHub HTTPS normal-entry验证；
- GitHub fetch/read/check；
- disposable local Git repo / bare remote / config fixture 用于 effective URL rewrite / fail-closed tests。

不授权：

- force / force-with-lease；
- reset/clean/restore用户工作；
- remote/tag delete；
- new remote branch workaround；
- upstream/remote remap；
- PR create/merge；
- GitHub release/tag；
- unrelated repository write。

### Machine Policy / installed executable授权

Plan v0.4 本身没有授予新的 install 权限；本消息现在提供一个**条件式、最多一次**的同身份 refresh/install authorization，目标固定为：

```text
/home/yuukias/.codex
```

在新的 HTTPS-only final candidate 冻结之前，禁止执行 refresh/install。

冻结 final candidate 后：

1. 先检查 `ai-bridge` trusted executable、imported `ai_bridge_kit` source 与当前 canonical checkout/final candidate identity；
2. 如果现有 editable install 已经直接加载该 final candidate，且 Machine Policy managed files没有 drift：
   - **不要做冗余 install**；
   - 直接运行 `ai-bridge host validate` 与 affected normal-entry gates；
3. 如果 trusted executable/package identity 是 stale：
   - 我授权最多一次本地-only `pip install -e /home/yuukias/GPT_Codex_AI_Bridge_Kit`，仅更新当前既有 Python environment 到这个 frozen final candidate；
   - 不允许新 virtualenv、外部 package source、额外 dependency/provider scope；
4. 如果 Machine Policy managed files / trusted executable binding 需要 refresh：
   - 我授权最多一次 `ai-bridge host install --codex-home /home/yuukias/.codex`；
   - managed-file写入前必须使用 Bridge 标准 backup；
5. 然后运行 `ai-bridge host validate` 与 G3/G4/G14 normal-entry evidence；
6. 如果这次 bounded refresh/install 引入 regression：
   - 保存 evidence；
   - 恢复 exact pre-refresh managed-file backup；
   - 不重试第二个 CODEX_HOME；
   - 不进行第二轮 live install；
   - 回 Planner/Critic。

这不是对任何其他机器/CODEX_HOME的授权。

### Credential/provider边界

继续只使用当前已经存在的 GitHub HTTPS credential manager / gh credential helper 和现有 account/provider/credential scope。

不授权：

- new provider/account；
- new credential/token；
- secret display；
- paid API；
- unrelated external service。

### Evidence / final report规则

当前 recovery evidence 中真实发生过的 SSH positive failure 保留为历史 provenance，不删除、不改写成“从未发生”。

实现并冻结一个 new final candidate 后：

1. 重跑 G3/G4/G14 与 required should-not-change；
2. 记录 HTTPS-only approved scope；
3. 更新：
   ```text
   results/bridge-core--low-friction-operations-convergence/EVIDENCE.md
   results/bridge-core--low-friction-operations-convergence/FINAL_REPORT.md
   ```
4. 明确说明：
   - prior SSH positive failure是旧 scope 下的真实 evidence；
   - Plan v0.4 后 SSH positive不再属于产品 claim；
   - SSH/custom trusted-helper fail-closed仍然通过 security negative gate；
5. 返回 pre-final Critic。

如果实现发现必须重新设计 architecture、增加 wrapper、改变 B1/B2/B3/Persistent Run/Reviewed materializer/credential scope，立即停止回 Planner/Critic。
