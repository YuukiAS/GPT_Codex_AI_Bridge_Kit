# Bridge Kit 0.9.2 — Unattended Execution / Persistent Run Refinement Execution Package v0.1

Date: **2026-09-25**  
Task key: **bridge-core--unattended-execution-refinement**  
Review stage: **EXECUTION_READY_REVIEW**  
Status: **FROZEN PACKAGE / NOT AUTHORIZED FOR EXECUTION**

## 1. Package identity

```text
Approved architecture Plan:
docs/design/0.9.2_unattended_execution_persistent_run_refinement_plan_v0.1_2026-09-25.md

Plan version:
v0.1

Plan commit:
9c5c7f7ac2bb110dcba98deea2b0c0d8bb7ffa1f

Architecture PASS transfer record:
docs/design/0.9.2_unattended_execution_persistent_run_refinement_architecture_pass_transfer_v0.1_2026-09-25.md

Architecture PASS transfer record first commit:
ae899586e8f4d90bcdca6cffb9d23a8bfa3f0cce

Canonical Goal:
docs/design/0.9.2_unattended_execution_persistent_run_refinement_goal_v0.1_2026-09-25.md

Goal version:
v0.1

Goal first commit:
204fdd38c94b4324bc961899402a6dcd826e7d3b

Kickoff Draft:
docs/design/0.9.2_unattended_execution_persistent_run_refinement_kickoff_v0.1_2026-09-25.md

Kickoff version:
v0.1

Kickoff first commit:
69ef4b426fd0e35d379749fb58f0ccc26a0852b8
```

The original Critic response is not currently stored on Bridge `main`; the user transferred the architecture PASS and its conditions to the Planner. The transfer record preserves that provenance without pretending to be the original Critic artifact.

## 2. Frozen execution interpretation

The package does not change Option B.

It freezes:

```text
upfront authorization readiness
!= process persistence
!= progress reporting
```

It explicitly rejects:

```text
long/unattended == tmux
```

and freezes the topology rule:

```text
scheduler-owned batch / already detached
-> no tmux solely for duration

terminal-owned foreground/orchestrator that must survive disconnect
-> Persistent Run/tmux eligible
```

Progress remains project-native and independent from tmux.

## 3. Critic conditions incorporated

The package incorporates all user-transferred architecture PASS conditions:

- no new state/auth store;
- no daemon/watcher/controller;
- no Host allowlist expansion;
- no scheduler wrapper;
- primary change surface = authoring prompts + installed Persistent Run templates + README/QUICKSTART/AGENTS/CHANGELOG + focused tests;
- `persistent_run.py` is should-not-change;
- G1/G2 require final-candidate normal Codex/Auto-review behavior evidence;
- source/keyword tests cannot close G1/G2;
- Auto-review/context-resume failure returns Planner instead of widening permissions;
- DII 922349 is scheduler-owned batch-holder reference evidence;
- tmux value in that example is terminal-owned orchestrator/`srun` lifecycle, not allocation persistence;
- DII is not migrated;
- Machine Policy remains unchanged;
- release ref advances only after all Gate/final closure.

## 4. Execution phases

The Canonical Goal freezes the execution order:

```text
Phase 0
source/drift preflight

Phase 1
minimal authoring/template/docs/tests implementation

Phase 2
focused + full deterministic regression

Phase 3
0.9.2 production candidate freeze

Phase 4
G3/G4/G5/G6/G7/G8/G9 final-candidate gates

Phase 5
G1/G2 fresh normal Codex + Auto-review + same-context resume gate

Phase 6
evidence package -> pre-final Critic
```

No formal release occurs in this package.

## 5. G1/G2 real-surface rule

This is a mandatory execution-ready property, not an optional QA note.

Before asking the user to participate:

- candidate must already be frozen;
- all possible deterministic work must already be complete;
- fixture and exact two user messages must already be prepared.

The initial G1 probe intentionally lacks approval for one exact harmless local effect.

Expected:

```text
fresh normal Codex
-> consumes final candidate
-> sees required foreseeable HUMAN_ONLY effect
-> asks before substantive precursor work
-> user sends exact prepared approval
-> same context resumes same Goal
-> later same effect executes
-> no duplicate user approval merely because stage changed
```

If the real surface cannot do this, result is Planner re-entry, not permission expansion.

## 6. DII interpretation

The package does not modify DII.

Current source evidence for job `922349` shows:

```text
BatchFlag=1
SubmitLine=sbatch ...
formal work prefix=srun --jobid=922349 --overlap
```

Therefore all implementation/docs/examples must avoid saying tmux is keeping 922349 alive.

Correct description:

```text
Slurm owns batch-holder lifetime.
tmux is useful only for terminal-owned orchestration around repeated srun/foreground lifecycle when that orchestration must survive disconnect.
```

## 7. Should-not-change gate

The execution package treats these as hard should-not-change surfaces:

```text
ai_bridge_kit/persistent_run.py
ai_bridge_kit/host.py
templates/host/rules/ai-bridge-global.rules
workflow schema/state machinery
release/ref machinery
DII repository
```

Need to change one -> return Planner/Critic.

## 8. Candidate / evidence identity

A production candidate is frozen only after:

- source/templates/tests/docs/version changes complete;
- focused regressions pass;
- full repository suite passes;
- version = 0.9.2;
- README/QUICKSTART/AGENTS/CHANGELOG align.

Later evidence-only commits do not change production identity.

Any later production/template/version change creates a new candidate and invalidates impacted Gate evidence.

## 9. Release boundary

This package may eventually authorize ordinary candidate publication to existing `origin/main` through the Critic-approved Kickoff.

It never authorizes:

```text
refs/heads/release movement
tag
GitHub Release
machine-sync release closure
```

Formal distribution remains a later closure step after capability gates and required review.

## 10. Execution-ready Critic decision requested

Critic should review the package as one bound object:

1. approved Plan v0.1;
2. Canonical Goal v0.1;
3. Kickoff Draft v0.1;
4. this Package v0.1.

The key execution-ready questions are:

- does the Goal faithfully implement the already-PASS Option B without reopening architecture?
- is the should-change surface narrow enough?
- are G1/G2 truly normal-surface behavior gates rather than keyword gates?
- does G1/G2 user interaction occur only after candidate/preparation is complete?
- does the package fail closed on Auto-review/context-resume failure without Host expansion?
- does G4 remain a bounded real tmux check without turning tmux into a universal backend?
- is G5 genuinely no-tmux progress evidence?
- is the DII 922349 interpretation accurate and read-only?
- are Machine Policy, DII and formal release correctly outside implementation mutation scope?
- is the future Kickoff complete but not broader than necessary?

A PASS at this stage should set `READY_FOR_CODEX=YES` and approve the Kickoff **verbatim**. Until then, execution is not authorized.
