# Bridge Kit 0.9.2 — Unattended Execution Refinement Execution-Ready Critic Handoff v0.1

Date: **2026-09-25**  
Task key: **bridge-core--unattended-execution-refinement**  
Review stage: **EXECUTION_READY_REVIEW**  
Package commit: **f596ff7aafd2c0a2d407c4bf4a02da5599edf8bb**

以下整段可直接复制给独立 Critic thread。

---

你是 AI Research Stack 的独立 Critic。

这是 **READ-ONLY execution-ready review**。

不要实现代码。  
不要修改 Git。  
不要启动 Codex Executor。  
不要启动 Persistent Run。  
不要修改 DII。  
不要修改 Machine Policy。  
不要调用付费 API。  
不要创建 automation。  
不要推进正式 release。

## Active Review Context

```text
target_repo:
YuukiAS/GPT_Codex_AI_Bridge_Kit

target_domain:
Bridge Kit / unattended authorization readiness + Persistent Run

design_topic_or_task_key:
bridge-core--unattended-execution-refinement

source_branch/ref:
main

review_stage:
EXECUTION_READY_REVIEW
```

## Review object

Architecture-approved Plan:

```text
path:
docs/design/0.9.2_unattended_execution_persistent_run_refinement_plan_v0.1_2026-09-25.md

version:
v0.1

commit:
9c5c7f7ac2bb110dcba98deea2b0c0d8bb7ffa1f
```

User-transferred architecture PASS record:

```text
path:
docs/design/0.9.2_unattended_execution_persistent_run_refinement_architecture_pass_transfer_v0.1_2026-09-25.md

first commit:
ae899586e8f4d90bcdca6cffb9d23a8bfa3f0cce
```

Canonical Goal:

```text
path:
docs/design/0.9.2_unattended_execution_persistent_run_refinement_goal_v0.1_2026-09-25.md

version:
v0.1

first commit:
204fdd38c94b4324bc961899402a6dcd826e7d3b
```

Kickoff Draft:

```text
path:
docs/design/0.9.2_unattended_execution_persistent_run_refinement_kickoff_v0.1_2026-09-25.md

version:
v0.1

first commit:
69ef4b426fd0e35d379749fb58f0ccc26a0852b8
```

Execution Package:

```text
path:
docs/design/0.9.2_unattended_execution_persistent_run_refinement_execution_package_v0.1_2026-09-25.md

version:
v0.1

package commit:
f596ff7aafd2c0a2d407c4bf4a02da5599edf8bb
```

The original Critic architecture reply is not stored on Bridge main. The user explicitly transferred its PASS and conditions to Planner; the transfer record preserves that fact without treating Planner as Critic.

## 0. Required reading

First read latest `YuukiAS/AI_Skills_Collection` main:

- `docs/workflows/PLANNER_ROLE_CONTRACT.md`
- `docs/workflows/CRITIC_ROLE_CONTRACT.md`
- `docs/workflows/PLUGIN_CAPABILITY_GATE_POLICY.md`

Then read Bridge latest main:

- `AGENTS.md`
- approved Plan v0.1
- architecture PASS transfer record
- Canonical Goal v0.1
- Kickoff Draft v0.1
- Execution Package v0.1
- current relevant source/templates/tests:
  - `chatgpt/TASK_WRITER_PROMPT.md`
  - `chatgpt/NEXT_TASK_PROMPT.md`
  - `templates/prompts/CHATGPT_RULES.md`
  - `codex/CODEX_START_PROMPT.md`
  - `templates/persistent_run/README.md`
  - `templates/persistent_run/CONTRACT_TEMPLATE.md`
  - `templates/persistent_run/KICKOFF_TEMPLATE.md`
  - `templates/persistent_run/AGENTS_SNIPPET.md`
  - `ai_bridge_kit/persistent_run.py`
  - `templates/host/GLOBAL_AGENTS_SNIPPET.md`
  - `templates/host/rules/ai-bridge-global.rules`
  - `tests/test_upfront_authorization_persistent_authoring.py`
  - `tests/test_persistent_run.py`

Read DII only as read-only evidence if needed:

- `YuukiAS/Distributed_Imaging_Inference`
- current Goal and `resource_receipt.json` for job 922349.

## 1. Do not reopen the already-PASS architecture

The approved architecture is Option B:

```text
upfront authorization readiness
!= process persistence
!= progress reporting
```

No new:

- state;
- auth store/database;
- daemon;
- watcher;
- controller;
- Host allowlist;
- scheduler wrapper.

Persistent Run stays, but tmux is selected by lifetime topology, not by duration alone.

Only block this execution package for a concrete contradiction, missing execution boundary, unsafe authorization, untestable Gate, or direct incompatibility with the approved architecture.

## 2. Check execution surface

The Goal intentionally limits implementation primarily to:

- authoring prompts;
- installed Persistent Run templates;
- README/QUICKSTART/AGENTS/CHANGELOG;
- focused tests;
- version owners.

Hard should-not-change:

```text
ai_bridge_kit/persistent_run.py
ai_bridge_kit/host.py
templates/host/rules/ai-bridge-global.rules
workflow schema/state machinery
release/ref machinery
Distributed_Imaging_Inference
```

If implementation would need one of those, Goal says return Planner/Critic.

Review whether this is sufficiently narrow and still capable of implementing the PASSed semantics.

## 3. G1/G2 are the most important execution-ready check

The user explicitly required:

- G1/G2 must use the **final candidate**;
- evidence must come from the **normal Codex / Auto-review / current-user context path**;
- keyword/source-presence tests are insufficient;
- Auto-review/context-resume failure must stop and return Planner;
- no Host permission expansion is allowed.

Review the exact Phase 5 contract.

It requires:

1. candidate freeze first;
2. all deterministic work first;
3. disposable local fixture fully prepared before user action;
4. fresh normal Codex context consumes exact final candidate;
5. initial prompt intentionally lacks one exact approval-sensitive local effect;
6. Codex asks before substantive precursor work;
7. user sends a pre-prepared exact bounded approval;
8. same context resumes same frozen Goal;
9. later exact same effect does not trigger duplicate user approval;
10. Auto-review does not re-block exact same authorized effect.

If this is not actually testable through a normal surface, require the **smallest** correction to the execution package. Do not solve it by inventing a new helper/bypass/state store.

## 4. Check user-interaction burden

The package deliberately postpones the unavoidable G1/G2 user interaction until:

- final candidate is frozen;
- deterministic gates are done;
- fixture exists;
- exact initial prompt exists;
- exact approval reply exists.

Review that the Executor cannot make the user debug an unfinished candidate.

If execution-ready PASS is possible, the user should only need the one bounded G1/G2 interaction when the task reaches that final gate.

## 5. DII 922349 interpretation

The package freezes:

```text
922349 = scheduler-owned batch holder
BatchFlag=1
SubmitLine=sbatch ...
formal GPU work = srun --jobid=922349 --overlap ...
```

Therefore:

- Slurm owns job 922349 lifetime;
- tmux does not persist that allocation;
- tmux value is terminal-owned orchestrator/repeated srun lifecycle where the orchestrator itself must survive disconnect.

Check that Goal/Kickoff do not accidentally regress to “interactive allocation itself requires tmux”.

DII remains read-only and is not migrated.

## 6. G4 / G5 boundary

### G4

Kickoff preauthorizes one exact task-owned harmless tmux smoke session:

```text
ai-bridge-092-g4-smoke
```

Only create/observe/clean that exact session.

If Auto-review rejects the exact bounded authorized smoke, Goal returns Planner/Critic. No fallback backend or allowlist expansion.

### G5

Must prove real:

```text
persistent-run report
persistent-run latest
```

against project-native JSON with **no corresponding tmux session**.

Review that this is enough to prove semantic decoupling while keeping `persistent_run.py` unchanged unless the current code contradicts the claim.

## 7. Authorization boundary

Future Kickoff authorizes only:

- task-owned Bridge main edits;
- ordinary commits;
- ordinary non-force existing `origin/main` publication after candidate closure;
- task-owned `/tmp/ai-bridge-092-unattended-gates/`;
- exact `ai-bridge-092-g4-smoke` tmux effect.

It explicitly does **not** preauthorize the G1/G2 inner approval-sensitive probe effect, because G1 needs a real missing-authorization condition.

It forbids:

- Host install/change;
- scheduler resource mutation;
- DII mutation;
- paid API;
- new provider/credential;
- force/destructive Git;
- new implementation branch/worktree;
- PR/tag/GitHub Release;
- moving `refs/heads/release`.

Review whether this is complete and no broader than necessary.

## 8. Candidate/release boundary

Implementation may prepare `0.9.2` candidate and ordinary-publish it to current `origin/main` under the future approved Kickoff.

It may not advance formal `refs/heads/release`.

Formal release happens only after:

- G1–G9 PASS;
- required pre-final/final closure;
- existing release owner process.

Check that no wording in Goal/Kickoff prematurely calls main candidate a formal release.

## 9. Capability Gate Matrix execution-readiness

Review G1–G9 as frozen by Goal:

- G1 foreseeable approval before precursor work — final-candidate normal surface;
- G2 exact same frozen effect not asked twice — same context + resume;
- G3 scheduler-native overnight no auto-tmux;
- G4 terminal-owned tmux retained with real bounded smoke;
- G5 progress without tmux;
- G6 new authority dimension reopens gate;
- G7 no broad permissions;
- G8 compatibility/no DII migration;
- G9 user-facing docs truth.

Mechanical tests alone cannot close G1/G2/G4/G5 where Goal requires behavior evidence.

## 10. PASS / REVISE discipline

If REVISE, every blocker must include:

```text
BLOCKER_N=
REQUIREMENT=
DIRECT_EVIDENCE=
CAUSAL_RISK=
MINIMAL_CLOSE_CONDITION=
```

Do not block for “could be cleaner” or speculative extra safety.

If PASS, this is an **execution-ready PASS**. Per Critic contract:

- review Plan + Goal + Kickoff as one package;
- set `READY_FOR_CODEX=YES`;
- output the approved Kickoff **verbatim** from:
  `docs/design/0.9.2_unattended_execution_persistent_run_refinement_kickoff_v0.1_2026-09-25.md`;
- do not rewrite a semantically new kickoff after PASS.

## 11. Expected final fields

```text
CRITIC_RESULT=PASS | REVISE
REVIEW_STAGE=EXECUTION_READY

REVIEWED_PLAN_VERSION=v0.1
REVIEWED_PLAN_COMMIT=9c5c7f7ac2bb110dcba98deea2b0c0d8bb7ffa1f

GOAL_PATH=docs/design/0.9.2_unattended_execution_persistent_run_refinement_goal_v0.1_2026-09-25.md
GOAL_VERSION=v0.1

KICKOFF_PATH=docs/design/0.9.2_unattended_execution_persistent_run_refinement_kickoff_v0.1_2026-09-25.md
KICKOFF_VERSION=v0.1

PACKAGE_PATH=docs/design/0.9.2_unattended_execution_persistent_run_refinement_execution_package_v0.1_2026-09-25.md
PACKAGE_VERSION=v0.1
PACKAGE_COMMIT=f596ff7aafd2c0a2d407c4bf4a02da5599edf8bb

OPTION_B_PRESERVED=YES | NO
SHOULD_NOT_CHANGE_BOUNDARY=PASS | REVISE
G1_G2_NORMAL_SURFACE_CONTRACT=PASS | REVISE
G4_TMUX_BOUNDARY=PASS | REVISE
G5_PROGRESS_WITHOUT_TMUX=PASS | REVISE
DII_922349_INTERPRETATION=PASS | REVISE
AUTHORIZATION_BOUNDARY=PASS | REVISE
MACHINE_POLICY_UNCHANGED=YES | NO
FORMAL_RELEASE_EXCLUDED=YES | NO
READY_FOR_CODEX=YES | NO

BLOCKERS=
NON_BLOCKING_NOTES=
NEXT_HANDOFF=CODEX | PLANNER
```
