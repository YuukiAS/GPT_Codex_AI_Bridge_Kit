# Bridge Kit 0.9.1 Reviewed First-Bootstrap — Execution Package v0.2

Package version: **0.2**  
Date: **2026-09-24**  
Task key: **reviewed-handoff--first-bootstrap-normal-entry**  
Review stage: **EXECUTION_READY_REVISE -> CRITIC RE-REVIEW**  
Stable blocker addressed: **ER-B1-RAW-CUTOVER-BYPASSES-FIRST-BOOTSTRAP**

This package supersedes Execution Package v0.1 only in execution ordering/cutover. The Critic-approved Plan v0.2 architecture is unchanged.

## 1. Frozen design authority

```text
Plan:
docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_plan_v0.2_2026-09-24.md

Plan version:
0.2

Plan commit:
eac789a35c4a83ce015a7744c722527580e79dc8

Design Critic result:
PASS
```

## 2. Canonical Goal v0.2

```text
Path:
docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_goal_v0.2_2026-09-24.md

Version:
0.2

First commit:
399db248613948a9f4ed02555d456418ad9a37ee
```

Goal v0.2 removes the raw AI worktree cutover and makes the real sibling AI_Skills bootstrap the primary FB-G1 positive.

## 3. Kickoff Draft v0.2

```text
Path:
docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_kickoff_v0.2_2026-09-24.md

Version:
0.2

First commit:
88126e5d8bb650ed110a70f9153cd7254590d544
```

The Kickoff explicitly authorizes the exact Phase-3 candidate task-bootstrap effect and contains **no raw AI_Skills worktree cutover authorization**.

Only the user sending the Critic-approved Kickoff creates current-user execution authorization.

## 4. Execution / Recovery Contract v0.2

```text
Path:
docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_execution_recovery_contract_v0.2_2026-09-24.md

Version:
0.2

First commit:
2d314edaa373b299ee54889ee8afca7bdc1e3064
```

The contract freezes the corrected execution order and fail-closed recovery semantics.

## 5. Exact corrected phase ordering

### Phase 0 — preflight only

- Bridge canonical main identity/drift checks.
- AI_Skills canonical checkout fetch/read/version/base discovery.
- Record exact AI `origin/main` OID.
- Check exact future sibling path/derived branch for conflicts.
- **Do not create AI branch/worktree.**

### Phase 1 — Bridge only

- implement approved Bridge 0.9.1 on canonical Bridge `main`;
- deterministic/full tests;
- version/README/CHANGELOG closure;
- freeze exact Bridge candidate.

### Phase 2 — activate Bridge candidate

- verify actual runtime identity;
- bounded local editable refresh only if stale;
- standard backup;
- at most one Machine Policy install on `/home/yuukias/.codex`;
- validate:
  - task bootstrap -> prompt;
  - materialize-worktree -> allow;
  - raw git worktree add -> prompt.

### Phase 3 — real FB-G1 / create AI implementation placement

Use the **new Bridge candidate**:

```text
ai-bridge reviewed-handoff task bootstrap
```

against:

```text
repo:
YuukiAS/AI_Skills_Collection

task:
workflow-core--reviewed-first-bootstrap-normal-entry

derived branch:
reviewed/workflow-core--reviewed-first-bootstrap-normal-entry

exact sibling worktree:
/home/yuukias/AI_Skills_Collection-workflow-core--reviewed-first-bootstrap-normal-entry

base ref:
origin/main

base commit:
exact Phase-0 recorded origin/main OID
```

This runs through the candidate Machine Policy `prompt`/Auto-review path and must consume the exact current-user Kickoff authorization without another user-visible manual bootstrap step.

This action is the **primary FB-G1 positive normal-entry evidence**.

If rejected:

```text
FB-G1=FAIL
stop -> Planner/Critic
```

No raw Git, `/tmp`, alternate branch/path, clone, remap or permanent allow.

### Phase 4 — workflow-core implementation

Only after Phase 3 PASS:

- implement in the exact sibling worktree created by the product command;
- source-first generation/tests/evals;
- version/changelog/repository release parity;
- ordinary commits;
- ordinary non-force publication of exact reviewed branch.

### Phase 5 — freeze exact candidate pair

Freeze Bridge + AI_Skills members.

### Phase 6 — FB-G2 through FB-G5

Run remaining release-critical evidence.

`/tmp/ai-bridge-091-first-bootstrap-gates/` remains available only for deterministic negatives/additional fixtures, never as a substitute for sibling-path FB-G1.

## 6. Exact repo placement and authorization requested

### Bridge

```text
repo:
YuukiAS/GPT_Codex_AI_Bridge_Kit

task:
reviewed-handoff--first-bootstrap-normal-entry

branch:
main

worktree:
/home/yuukias/GPT_Codex_AI_Bridge_Kit
```

Requested authorization:

- task-owned implementation;
- ordinary commits;
- ordinary non-force existing `origin/main` publication;
- bounded local tests/evidence.

### AI_Skills

Before Phase 3 only read/fetch/preflight canonical:

```text
/home/yuukias/AI_Skills_Collection
```

No branch/worktree mutation is authorized through raw Git.

Phase 3 authorization is only the exact Bridge candidate bootstrap effect:

```text
task:
workflow-core--reviewed-first-bootstrap-normal-entry

derived branch:
reviewed/workflow-core--reviewed-first-bootstrap-normal-entry

worktree:
/home/yuukias/AI_Skills_Collection-workflow-core--reviewed-first-bootstrap-normal-entry

base:
exact kickoff-time origin/main OID
```

After FB-G1 success:

- task-owned edits/commits in that worktree;
- ordinary non-force publication of that exact branch to same-name origin branch, including first remote publication.

No other branch/worktree is authorized.

## 7. Bridge implementation scope

Primary:

- `ai_bridge_kit/reviewed_handoff.py`;
- CLI route for `task bootstrap`;
- shared task initializer;
- local/remote scope discovery;
- Reviewed docs/prompts as directly required;
- Machine Policy rule + validation for bootstrap prompt/materializer allow/raw Git prompt;
- direct tests;
- version/README/CHANGELOG/evidence.

Unrelated product areas remain out of scope.

## 8. AI_Skills workflow-core scope

Canonical source:

```text
skills/core/codex-system/codex-workflow-protocol/SKILL.md
```

Direct adjacent references/tests/evals/generated plugin/version/changelog/repository release owners may change.

No root AGENTS duplication.

Required consumer routing:

```text
missing
-> task bootstrap

existing local/exact remote
-> materialize-worktree --mode resume

Bridge capability/version missing
-> fail before substantive work

never raw Git fallback
```

## 9. Machine Policy boundary

Target only:

```text
/home/yuukias/.codex
```

Kickoff v0.2 requests:

- at most one local editable refresh if runtime stale;
- one standard managed-file backup;
- at most one host install;
- exact validation;
- restore exact backup on candidate regression;
- no second CODEX_HOME/install loop.

## 10. Version closure

Bridge:

```text
0.9.0 -> 0.9.1
```

AI_Skills package observation:

```text
repository = 5.1.0
workflow-core = 0.3
```

Execution re-reads canonical policy/current values.

If still 0.3 and release-ready:

```text
workflow-core -> 0.4
repository -> next actual PATCH
```

No repository patch slot is pre-reserved.

## 11. FB-G1 through FB-G5

### FB-G1

Primary positive is **Phase-3 real sibling AI_Skills task bootstrap**.

Not acceptable substitutes:

- raw Git cutover;
- `/tmp` positive fixture;
- unit test;
- helper-only dry run.

### FB-G2

Exact-scope negatives and prompt boundaries.

### FB-G3

Partial failure rollback.

### FB-G4

Existing local resume + remote-only rematerialization with metadata read/validation before checkout.

### FB-G5

Frozen exact Bridge + AI_Skills pair through official loaded workflow-core candidate entry.

## 12. What v0.2 explicitly removes from v0.1

Removed completely:

- one-time raw AI_Skills `git worktree add` implementation cutover;
- authorization for raw cutover;
- language exempting that raw cutover from FB-G1/FB-G5;
- any assumption that AI workflow-core implementation worktree exists before Bridge candidate activation.

No raw cutover remains anywhere in the authoritative v0.2 Goal/Kickoff/Contract/Package.

## 13. Recovery boundary

If Phase 3 is rejected by Auto-review/sandbox:

- preserve evidence;
- FB-G1 FAIL;
- return Planner/Critic.

Do not request raw Git from the user and do not change the path.

If Bridge bootstrap leaves partial state, only its approved current-invocation rollback applies.

If AI implementation later exposes a source/version conflict, stop without recreating the worktree.

If a candidate member changes, re-freeze the pair and rerun affected evidence.

## 14. Explicit prohibitions

No:

- raw AI worktree implementation cutover;
- generic worktree allow;
- permanent task-bootstrap allow;
- alternate sibling/`/tmp` implementation path;
- generic Git facade;
- force/destructive cleanup;
- auth DB/token store;
- cross-repo ledger;
- new watcher/controller/workflow/state machine;
- paid API;
- new provider/account/credential;
- PR/main integration/tag/release;
- unrelated external effects.

## 15. Execution-ready Critic object

Review together:

1. approved Plan v0.2;
2. Canonical Goal v0.2;
3. Kickoff Draft v0.2;
4. Execution/Recovery Contract v0.2;
5. this Execution Package v0.2.

The re-review should focus on stable blocker:

```text
ER-B1-RAW-CUTOVER-BYPASSES-FIRST-BOOTSTRAP
```

and verify that:

- raw cutover is gone;
- Bridge is implemented/activated first;
- real sibling AI first bootstrap is now FB-G1;
- exact current-user authorization covers that candidate command;
- `/tmp` cannot substitute for FB-G1;
- no architecture was changed beyond execution ordering.

If PASS, the user may send the exact body under:

`## Copy to Codex after execution-ready Critic PASS`

from Kickoff v0.2.

Any semantic change after PASS requires re-review.
