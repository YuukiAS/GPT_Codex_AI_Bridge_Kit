# Bridge Kit 0.8.0 Candidate Plugin Replay — Phase 0 Execution Contract

Date: 2026-09-08

Status: **AUTHORIZED FOR BOUNDED PHASE 0 RESEARCH PROBE ONLY**

Inputs:

- `docs/design/0.8.0_candidate_plugin_replay_plan_v0.2_2026-09-07.md`
- `docs/design/0.8.0_candidate_plugin_replay_critic_decision_2026-09-08.md`
- `prompts/tasks/0.8.0_candidate_plugin_replay_phase0_probe.md`

The next executable step is intentionally small: run the real Codex runtime probe and select the smallest supported native path. This file does not authorize production implementation.

## Allowed outcome

Exactly one:

```text
SELECTED_MODE=A_RETURN_TO_CRITIC
SELECTED_MODE=B0
SELECTED_MODE=B1
SELECTED_MODE=NOT_READY
```

## Frozen order

```text
A -> if not proven -> B0 -> only on genuine B0 failure -> B1
```

No B2/B3.

## One-time raw-command authorization

Raw `codex plugin ...` and `codex exec ...` commands required by the frozen A/B0/B1 research probe are authorized only for this bounded Phase 0 experiment. This does not create or imply a permanent Host Policy allow. Normal confirmation must be used when required.

## Production-code prohibition

During Phase 0 do not implement or release:

- `candidate-plugin-replay` wrapper/CLI;
- persistent Host Policy rules for that command;
- production lock/journal subsystem;
- release/version/changelog changes;
- changes to existing `plugin-replay` semantics.

The only committed result expected from Phase 0 is the evidence report and, only if strictly necessary, a small harmless task-owned probe fixture.

## Acceptance

Phase 0 is complete only when the actual installed Codex runtime has selected one of A/B0/B1 or demonstrated NOT_READY, the executed mode has been cleaned up, pre-existing same-name plugin state remains unchanged, and the evidence report is sufficient for Critic review without relying on plugin-list-only proxy PASS.
