# Bridge Kit 0.9.0 Low-Friction Operations Convergence — Recovery Execution Package v0.1

Package version: **0.1**  
Date: **2026-09-24**  
Task key: **bridge-core--low-friction-operations-convergence**  
Target repo: **YuukiAS/GPT_Codex_AI_Bridge_Kit**  
Source branch/ref: **main**  
Review stage: **PRE_FINAL_IMPLEMENTATION_REVISE -> RECOVERY EXECUTION_READY REVIEW**

This package is a bounded recovery package for the existing task. It is not a successor, new Plan, or architecture revision.

## Approved design authority

- Plan:
  `docs/design/0.9.0_low_friction_operations_convergence_plan_v0.3_2026-09-23.md`
- Plan version: `0.3`
- Approved Plan commit:
  `11f770fdb8a8d928aefbfda5f42527270a19f8b4`
- Design status: `PASS`

No Plan v0.4 is created.

## Current implementation baseline

- Candidate:
  `88fa67c44e6e491ed548f033ce0d3c33f63525c4`
- Status:
  `PRE_FINAL_REVISE / NOT FINAL`

The existing implementation is retained wherever unaffected.

Existing result files:

- `results/bridge-core--low-friction-operations-convergence/EVIDENCE.md`
- `results/bridge-core--low-friction-operations-convergence/FINAL_REPORT.md`

are provenance from the first implementation attempt and are not authoritative final completion evidence until the recovery final candidate is frozen and the required gates are rerun.

## Stable findings to close

```text
B1-PUBLISH-PREFLIGHT-TRANSPORT-FENCE
B2-PERSISTENT-RUN-REPORTER-INCOMPLETE
B3-WORKTREE-FROZEN-SCOPE-FIDELITY
B4-FINAL-GATE-EVIDENCE-INCOMPLETE
```

No already-closed design finding is reopened.

## Recovery Goal

- Path:
  `docs/design/0.9.0_low_friction_operations_convergence_recovery_goal_v0.1_2026-09-24.md`
- Version: `0.1`
- First commit:
  `411abef0040ebc33e5b831abd4e74bc7d50ef4db`

The Recovery Goal is the mechanical contract for B1-B4.

## Recovery Kickoff Draft

- Path:
  `docs/design/0.9.0_low_friction_operations_convergence_recovery_kickoff_v0.1_2026-09-24.md`
- Version: `0.1`
- First commit:
  `fe0037850c4625eb91e5a09d04530ab61aec884e`

Only the user sending the Critic-approved Recovery Kickoff creates current-user authorization.

## Frozen execution placement

```text
repo:
YuukiAS/GPT_Codex_AI_Bridge_Kit

task:
bridge-core--low-friction-operations-convergence

branch:
main

worktree:
/home/yuukias/GPT_Codex_AI_Bridge_Kit

replacement Machine Policy identity:
/home/yuukias/.codex
```

No new implementation branch/worktree is created.

## Recovery authorization requested

The Recovery Kickoff asks only for:

1. B1-B3 bounded repairs in the same task/main/worktree;
2. ordinary task-owned non-force publication to `origin/main`;
3. disposable local/config/worktree/progress fixtures needed by B1-B4;
4. bounded official plugin normal-entry evidence under existing Codex/OpenAI provider/credential scope;
5. exactly one replacement/final-candidate Machine Policy install/validate on `/home/yuukias/.codex`, after the new final candidate is frozen;
6. standard backup and restore-on-regression for that replacement;
7. existing Notifications only if already configured and within the existing provider/recipient boundary, capped at four operational test messages.

It does not request:

- paid API;
- new provider/account/data/credential/recipient scope;
- force/destructive Git;
- new branch/PR/release/tag;
- another CODEX_HOME;
- another wrapper;
- generic Git facade;
- auth DB/token store;
- daemon/watcher hierarchy;
- new workflow;
- candidate-plugin-replay;
- unrelated external side effects.

## B1 recovery binding

The package specifically requires transport/config rejection before the first remote/network Git command, or an already-sanitized explicit environment for every such preflight.

Required executable-canary regression families:

- `GIT_SSH`;
- `GIT_SSH_COMMAND`;
- `GIT_ASKPASS`;
- `SSH_ASKPASS`;
- local/worktree/command `credential.helper`;
- `core.sshCommand`.

Required config-source regression families:

- `GIT_CONFIG_COUNT` / KEY / VALUE;
- `GIT_CONFIG_GLOBAL`;
- `GIT_CONFIG_SYSTEM`;
- `GIT_CONFIG_NOSYSTEM`.

The canary must not execute before rejection.

## B2 recovery binding

Persistent Run must add the approved event-driven reporting behavior, not only JSON normalization:

- concise normalized report;
- machine-local latest;
- bounded recent history;
- reconnect query;
- real event comparison;
- dedupe/cadence;
- progress-source/report-interval/delivery-mode/stall-threshold contract fields;
- optional one-shot Notifications projection;
- no daemon/watcher/controller;
- no automatic intervention.

Required normal-entry evidence includes a multi-event process and a separate UNKNOWN/stall case.

## B3 recovery binding

Reviewed materialization must:

- freeze stable worktree identity rather than resolving both sides to a new symlink target at invocation;
- reject changed symlink/path redirection;
- prove frozen base_commit lineage against frozen `origin/<base_branch>` before bootstrap;
- preserve sibling + `/tmp` cases;
- implement invocation-owned partial rollback/evidence without touching pre-existing/remote/user work.

## B4 recovery binding

After B1-B3:

1. freeze one new final candidate;
2. consume the one newly authorized replacement Machine Policy install on `/home/yuukias/.codex`;
3. rerun affected G1-G15 normal-entry evidence on that same candidate;
4. only then update EVIDENCE/FINAL_REPORT.

Minimum direct gates are the Critic-required G1/G2, G3/G4, G5/G6/G7, G9, G10/G11, G13, and G14, with G8/G12/G15 should-not-change confirmation.

## Recovery review object

The next independent Critic must review together:

1. approved Plan v0.3;
2. existing Canonical Goal v0.1;
3. Recovery Goal v0.1;
4. Recovery Kickoff Draft v0.1;
5. this Recovery Package v0.1.

The review is not an architecture redesign. It should answer only:

- Does the Recovery Goal faithfully close B1-B4 inside Plan v0.3?
- Does the Recovery Kickoff authorize exactly the bounded effects required to execute that recovery, including exactly one replacement Machine Policy install on the same CODEX_HOME?
- Does it avoid asking the user to re-decide architecture or reauthorize unrelated effects?

If PASS, the Critic must return the exact text under
`## Copy to Codex after recovery execution-ready Critic PASS`
from the Recovery Kickoff verbatim.
