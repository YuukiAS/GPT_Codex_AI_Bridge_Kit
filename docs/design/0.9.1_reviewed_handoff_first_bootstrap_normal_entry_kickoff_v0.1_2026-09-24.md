# Bridge Kit 0.9.1 Reviewed First-Bootstrap — Kickoff Draft v0.1

Status: **DRAFT FOR EXECUTION_READY CRITIC REVIEW / NOT AUTHORIZED**

Bound design:

- Plan: `docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_plan_v0.2_2026-09-24.md`
- Plan commit: `eac789a35c4a83ce015a7744c722527580e79dc8`
- Canonical Goal: `docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_goal_v0.1_2026-09-24.md`
- Task: `reviewed-handoff--first-bootstrap-normal-entry`

Only the user sending the Critic-approved text below constitutes execution authorization.

---

## Copy to Codex after execution-ready Critic PASS

执行 Bridge Kit 0.9.1 Reviewed first-bootstrap normal-entry 任务。不要重新设计已 PASS 的 Plan v0.2。

权威合同：

```text
Plan:
docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_plan_v0.2_2026-09-24.md

Canonical Goal:
docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_goal_v0.1_2026-09-24.md
```

### Bridge implementation identity

```text
Repository:
YuukiAS/GPT_Codex_AI_Bridge_Kit

Task:
reviewed-handoff--first-bootstrap-normal-entry

Branch:
main

Canonical worktree:
/home/yuukias/GPT_Codex_AI_Bridge_Kit
```

我授权在这个 exact Bridge repo/main/canonical worktree 内完成 Goal 允许的 0.9.1 production implementation、focused/full tests、docs/version/evidence、ordinary task-owned commits，以及 ordinary non-force publication 到现有 `origin/main`。

不要创建 Bridge implementation branch/worktree；这是为避免用尚未实现的 first-bootstrap capability bootstrap 自己。

### AI_Skills workflow-core implementation identity

```text
Repository:
YuukiAS/AI_Skills_Collection

Task:
workflow-core--reviewed-first-bootstrap-normal-entry

Branch:
reviewed/workflow-core--reviewed-first-bootstrap-normal-entry

Exact worktree:
/home/yuukias/AI_Skills_Collection-workflow-core--reviewed-first-bootstrap-normal-entry
```

这次 AI_Skills implementation worktree 是**一次性 cutover exception**，不是 post-0.9.1 产品 fallback。

我授权在执行开始时：

1. 对 canonical `/home/yuukias/AI_Skills_Collection` 执行一次 bounded `git fetch origin main`；
2. 记录当时唯一 exact `origin/main` OID；
3. 如果 workflow-core source/version/release policy没有与本 package 冲突的 material drift，则仅以该 exact OID 作为本次 AI_Skills implementation base；
4. 创建且只创建：
   - branch `reviewed/workflow-core--reviewed-first-bootstrap-normal-entry`
   - worktree `/home/yuukias/AI_Skills_Collection-workflow-core--reviewed-first-bootstrap-normal-entry`
5. 允许为这一个 exact cutover 使用当前 raw Git branch/worktree mutation path。

不允许换 branch/path、备用 `/tmp`、clone、remote remap、第二 worktree 或另一 base。

如果 Auto-review/Host 仍拒绝**这个已明确授权的 exact cutover effect**，停止并报告 `CUTOVER_APPROVAL_BLOCKED`；不要改路径、不要要求用户执行另一个 raw command、不要把 cutover失败冒充 product FB-G1 failure。

这一个 cutover raw Git creation：
- 不算 FB-G1/FB-G5 PASS evidence；
- 不写成未来正常 workflow；
- 0.9.1 capability 可用后不得继续作为 workflow-core fallback。

### Bridge implementation scope

只修改 Goal 明确允许的 Bridge source/tests/templates/docs/version/evidence。

必须实现：

- `ai-bridge reviewed-handoff task bootstrap`;
- branch只能由 `reviewed/<task_key>` 派生；
- new bootstrap Machine Policy = `prompt`；
- existing `materialize-worktree` = artifact-bound existing `allow`；
- shared task initializer；
- first REQUEST/CURRENT only in new reviewed worktree；
- remote-only resume reads exact remote REQUEST/CURRENT before checkout；
- conservative current-invocation rollback；
- raw `git worktree add` remains prompt。

不要增加第二 workflow/state machine、auth DB/token/receipt store、cross-repo ledger、generic Git/worktree facade、permanent bootstrap allow 或 force cleanup。

### AI_Skills workflow-core scope

只做 approved SMALL consumer routing refinement。

Canonical source owner：

```text
skills/core/codex-system/codex-workflow-protocol/SKILL.md
```

允许直接相关 references/tests/evals/generated plugin payload/version/changelog/repository release parity。

不要在 root AGENTS 增加同义规则。

Production routing必须是：

```text
task missing
-> ai-bridge reviewed-handoff task bootstrap

task exists locally or exact reviewed remote
-> ai-bridge reviewed-handoff materialize-worktree --mode resume

required Bridge capability/version missing
-> fail early before substantive implementation
```

绝对不要：

- raw `git worktree add` fallback；
- alternate /tmp；
- clone；
- another branch；
- remote remap；
- 在 workflow-core 内复制 Git mechanics。

### AI_Skills version closure

启动前实际读取 latest：

- `docs/workflows/PLUGIN_VERSIONING_AND_CHANGELOGS.md`
- `VERSION`
- `scripts/codex_marketplace_config.json`
- `docs/plugin-changelogs/workflow-core.md`

当前 package preparation 时观察值是：

```text
repository VERSION = 5.1.0
workflow-core = 0.3
```

如果 workflow-core 仍为 0.3 且本次 user-visible routing improvement完成 release closure：

```text
workflow-core -> 0.4
```

exact repository release number按 kickoff-time latest released VERSION 做 PATCH increment。若 latest仍是 5.1.0，则为 5.1.1；如果其他独立 release 已合法占用该 slot，则按当前 policy 使用 latest release 的下一 PATCH，不要覆盖旧 release。

如果 workflow-core 已不是 0.3，或 source/routing发生 material overlap，停止并报告 `AI_SKILLS_VERSION_OR_SOURCE_DRIFT`，不要自行猜版本或合并语义。

### AI reviewed branch publication

我授权这个 exact AI task branch的 ordinary task-owned commits，以及首次创建同名远端 branch所必需的**一次 exact ordinary non-force publication**：

```text
reviewed/workflow-core--reviewed-first-bootstrap-normal-entry
->
origin/reviewed/workflow-core--reviewed-first-bootstrap-normal-entry
```

不授权：
- force / force-with-lease；
- 其他新 remote branch；
- upstream/remotes remap；
- branch/tag delete；
- PR create/merge；
- main integration；
- release/tag publish。

后续该 exact branch上的 ordinary non-force task publication可以继续，但不得扩大为任意 reviewed/* branch authority。

### Machine Policy authorization

Target fixed to:

```text
CODEX_HOME=/home/yuukias/.codex
```

在 Bridge 0.9.1 implementation + deterministic/full tests通过并冻结 Bridge candidate之前，不要 install。

冻结后：

1. 检查 actual `ai-bridge` executable / imported `ai_bridge_kit` source identity；
2. 如果当前 editable install已经加载 exact candidate，不做冗余 package reinstall；
3. 如果 runtime identity stale，我授权最多一次 local-only editable refresh：
   `pip install -e /home/yuukias/GPT_Codex_AI_Bridge_Kit`
   仅使用当前 existing Python environment，不创建新环境、不从外部 package source升级；
4. 对 managed Machine Policy 写入前创建标准 Bridge backup；
5. 我授权最多一次：
   `ai-bridge host install --codex-home /home/yuukias/.codex`
6. 运行 `ai-bridge host validate`；
7. 必须验证：
   - `task bootstrap` => prompt；
   - `materialize-worktree` => allow；
   - raw `git worktree add` => prompt；
8. 若 candidate导致回归，保存 evidence，恢复 exact pre-install managed-file backup并停止；
9. 不安装第二个 CODEX_HOME，不做 install loop。

### Gate fixtures / local effects authorization

我授权使用且仅使用：

```text
/tmp/ai-bridge-091-first-bootstrap-gates/
```

作为 FB-G1–FB-G5 的 task-owned disposable local gate root，可在其中创建/删除：

- fixture Git repo；
- local bare origin；
- exact gate branches/worktrees；
- task control state；
- candidate plugin local config/cache；
- non-secret evidence。

固定 gate task/worktree：

```text
FB-G1 task:
gate--first-bootstrap

FB-G1 worktree:
/tmp/ai-bridge-091-first-bootstrap-gates/first-bootstrap-worktree

FB-G5 task:
gate--workflow-core-route

FB-G5 worktree:
/tmp/ai-bridge-091-first-bootstrap-gates/workflow-core-worktree
```

这些 fixture cleanup 只允许删除这个 gate root 内本任务创建的内容，不得影响其他 repo/cache/user work。

### FB-G1–FB-G5

严格按 Goal 执行全部 gates。

特别：

FB-G1：
- real installed Machine Policy；
- brand-new task；
- one current-user-approved Kickoff；
- bootstrap是 prompt，不是 permanent allow；
- approval/Auto-review必须消费当前授权，不得要求第二个 manual worktree/bootstrap步骤；
- 若仍重复问或要求 raw Git，FAIL并回 Planner/Critic。

FB-G4：
- local metadata resume；
- remote-only exact reviewed branch REQUEST/CURRENT在 checkout前读取校验；
- canonical main无 task metadata；
- wrong/missing remote metadata fail closed。

FB-G5：
- freeze exact Bridge candidate + exact AI_Skills/workflow-core candidate pair；
- verified loaded runtime/plugin identity；
- real workflow-core normal entry：
  missing -> bootstrap；
  existing/remote-only -> resume；
  no raw fallback。

不要用 unit tests/CI/helper-only output代替 FB-G1/FB-G4/FB-G5。

### Candidate plugin / credential boundary

FB-G5 使用 AI_Skills 当前官方 Codex/Marketplace candidate path加载 exact workflow-core candidate。

允许使用当前现有 Codex/OpenAI account/provider/credential scope。

不授权：

- paid API；
- 新 provider/account/token/credential；
- 复制 auth.json到新 runtime；
- secret display；
- Bridge candidate-plugin-replay resurrect；
- 修改 unrelated global plugin state。

如果 normal candidate loading需要新的 credential/provider scope，停止并报告。

### Publication / destructive boundary

允许：

- Bridge main task-owned ordinary commits；
- Bridge existing `origin/main` ordinary non-force publication；
- AI exact reviewed branch cutover creation和 ordinary non-force publication；
- normal fetch/read/check；
- task-owned local fixture effects；
- approved single Machine Policy update above。

禁止：

- force/force-with-lease；
- destructive reset/clean/restore用户工作；
- remote/tag deletion；
- arbitrary new branch/worktree；
- remote/upstream remap；
- PR/merge；
- GitHub release/tag；
- paid API；
- unrelated external messages/services；
- new provider/data/credential scope。

### Completion

只有以下都满足后才进入 pre-final Critic：

- exact Bridge 0.9.1 candidate冻结；
- exact AI_Skills/workflow-core candidate冻结；
- frozen pair identities已记录；
- FB-G1–FB-G5真实 gates完成；
- README/CHANGELOG/version closure完成；
- Machine Policy identity验证完成；
- no raw product fallback；
- evidence只绑定该 pair；
- working trees/branches按合同处于可审状态。

任何 production source在 pair freeze 后变化，rerun affected gates。

不要自行 main-integrate AI_Skills，不要 tag/release/publish。
