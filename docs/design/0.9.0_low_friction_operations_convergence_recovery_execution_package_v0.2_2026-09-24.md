# Bridge Kit 0.9.0 Low-Friction Operations Convergence — Recovery Execution Package v0.2

Package version: **0.2**  
Date: **2026-09-24**  
Task key: **bridge-core--low-friction-operations-convergence**  
Target repo: **YuukiAS/GPT_Codex_AI_Bridge_Kit**  
Source branch/ref: **main**  
Review stage: **PLAN_V0_4_PASS -> RECOVERY_EXECUTION_READY REVIEW**

This package amends the existing recovery package for the same task. It is not a successor and does not introduce a new architecture.

## Approved design authority

- Plan:
  `docs/design/0.9.0_low_friction_operations_convergence_plan_v0.4_2026-09-24.md`
- Plan version:
  `0.4`
- Plan commit:
  `bf20c4ff4f2a72f2ef2b5a57cd236519d91ef4cd`
- Critic result:
  `PASS`

Previous Plan v0.3 remains historical design provenance; v0.4 supersedes only the trusted publisher transport claim.

## Amended Recovery Goal

- Path:
  `docs/design/0.9.0_low_friction_operations_convergence_recovery_goal_v0.2_2026-09-24.md`
- Version:
  `0.2`
- First commit:
  `71f39b51b53ad3bff4e89567293eae38dd88aa82`

Recovery Goal v0.1 remains historical provenance and is superseded for execution only where it required a real positive SSH publication.

## Amended Recovery Kickoff

- Path:
  `docs/design/0.9.0_low_friction_operations_convergence_recovery_kickoff_v0.2_2026-09-24.md`
- Version:
  `0.2`
- First commit:
  `d49a7fa2d4e23f58cf2ab4196587c55d55a9f123`

Only the user sending the Critic-approved Kickoff v0.2 creates current-user recovery authorization.

## Current implementation/evidence baseline

```text
current main before v0.4 amendment:
c3ecb175880456a3b53eced1ff9b3c6190b240ee

recovery source candidate:
37a773cd83971d64f194a7d82c55a24889bd8164

canonical worktree:
/home/yuukias/GPT_Codex_AI_Bridge_Kit

branch:
main

Machine Policy identity:
/home/yuukias/.codex
```

B1/B2/B3 implementation already exists and is not reopened.

The only remaining product-scope correction is the HTTPS-only trusted publisher claim plus affected final evidence.

## Semantic delta from Recovery Package v0.1

Only these changes are approved:

1. trusted low-friction publisher positive path is GitHub HTTPS only;
2. real GitHub SSH agent/config positive publication is removed from G4/recovery completion;
3. effective SSH/scp/custom fetch or push URL must fail closed before remote/network Git operation;
4. URL rewriting to SSH/custom is explicitly tested;
5. all SSH/config executable-injection negatives remain;
6. shared `_canonical_repo_identity()` is not globally narrowed;
7. rerun affected G3/G4/G14 plus required should-not-change only;
8. do not configure SSH.

No other recovery capability or gate is redesigned.

## Affected implementation owner

Primary production owner:

- `ai_bridge_kit/host.py`

Direct test/doc/evidence owners only:

- `tests/test_host_policy.py`
- directly related Host/CLI tests if required
- README/QUICKSTART/AGENTS only where trusted transport scope is described
- task EVIDENCE/FINAL_REPORT after real gate rerun

Do not modify Persistent Run, Reviewed materializer, Notifications, plugin-replay or workflow architecture unless a direct regression is discovered.

## G3 / G4 / G14 execution contract

### G3

Real existing-branch GitHub HTTPS bounded publication remains required.

Must prove:

- same-name remote branch exists;
- current-user task/branch is already authorized;
- ordinary non-force publication succeeds through the single bounded helper;
- no repeated approval;
- exact branch/ref effect.

### G4

Positive:

- GitHub HTTPS bounded publication;
- existing system/global/gh credential-helper path.

Negative:

- all existing exact-effect/security fences;
- `GIT_SSH` / `GIT_SSH_COMMAND` / `GIT_SSH_VARIANT`;
- `core.sshCommand`;
- credential-helper injection;
- askpass injection;
- `GIT_CONFIG_COUNT/KEY/VALUE`;
- `GIT_CONFIG_GLOBAL`;
- `GIT_CONFIG_SYSTEM`;
- `GIT_CONFIG_NOSYSTEM`;
- effective SSH/scp-style origin;
- `ssh://` origin;
- literal HTTPS rewritten effectively to SSH/custom;
- HTTPS fetch / SSH push mismatch.

Rejected cases must prove no injected executable execution and no remote/network publication through the trusted helper.

A real positive SSH publication is not required.

### G14

One final candidate must own:

- source;
- tests;
- installed runtime/trusted executable identity;
- G3/G4 evidence;
- final docs;
- EVIDENCE/FINAL_REPORT.

Documentation claim must be exactly:

```text
trusted low-friction publication = GitHub HTTPS
SSH/custom transport = ordinary approval path
```

## Should-not-change

Focused/full regression must confirm no direct regression to:

- B1 transport-execution fence;
- B2 Persistent Run reporter;
- B3 Reviewed materializer;
- G13 Reviewed runner publisher authority;
- G15 architecture boundary;
- production plugin-replay;
- Notifications authority;
- display taxonomy and compatibility identifiers.

Do not rerun or reopen unrelated architecture work merely for ceremony.

## SSH machine configuration explicitly excluded

Do not:

- create/upload SSH key;
- start/manage ssh-agent;
- change `~/.ssh/config`;
- change `known_hosts`;
- mutate `SSH_AUTH_SOCK`;
- switch remote to SSH;
- add SSH wrapper.

SSH/custom remains a supported **ordinary approval fallback**, not a trusted low-friction positive path.

## Machine Policy refresh/install authorization requested by Kickoff

Plan v0.4 provides no install authority by itself.

Kickoff v0.2 requests a **conditional maximum-one refresh/install authorization** for the existing identity only:

```text
/home/yuukias/.codex
```

After final candidate freeze:

- inspect trusted executable/imported source identity;
- if the existing editable install already loads the final candidate and managed files have no drift, skip redundant install and only validate;
- if package identity is stale, permit at most one local-only editable refresh from the same canonical checkout into the existing environment;
- if Machine Policy managed files/binding need refresh, permit at most one host install for the same CODEX_HOME;
- standard backup before managed-file writes;
- restore exact backup and stop on regression;
- no second CODEX_HOME/install loop.

This is part of the Kickoff authorization request and is not inferred from the consumed previous install authorization.

## Authorization boundary

Kickoff v0.2 requests only:

- same repo/task/main/canonical worktree;
- publisher-specific HTTPS-only correction;
- ordinary task-owned commits/non-force `origin/main` publication;
- affected G3/G4/G14 and direct should-not-change evidence;
- disposable local Git/config fixtures;
- same existing GitHub HTTPS credential/account/provider scope;
- conditional max-one local runtime/Machine Policy refresh on `/home/yuukias/.codex`.

It does **not** request:

- paid API;
- new credential/provider/account;
- secret display;
- destructive/force Git;
- new remote branch;
- PR/release/tag;
- remote/upstream remap;
- another CODEX_HOME;
- another wrapper;
- generic Git facade;
- auth DB/token store;
- daemon/watcher hierarchy;
- new workflow;
- candidate-plugin-replay;
- unrelated external effect.

## Evidence handling

Historical SSH positive failures remain in evidence as real history.

After v0.4 implementation and new final-candidate gate rerun:

- record that Plan v0.4 removed SSH positive from the product claim;
- retain SSH/custom fail-closed negative evidence;
- update EVIDENCE/FINAL_REPORT only with same-final-candidate truth;
- return to pre-final Critic before final release completion claim.

## Recovery Critic review object

The next Critic reviews together:

1. Plan v0.4;
2. Recovery Goal v0.2;
3. Recovery Kickoff Draft v0.2;
4. this Recovery Package v0.2.

Review only:

- fidelity to approved HTTPS-only amendment;
- preservation of SSH/config negatives;
- shared-helper compatibility;
- affected G3/G4/G14 gate scope;
- conditional same-CODEX_HOME refresh/install authorization boundary.

Do not reopen unrelated already-approved architecture.

If PASS, the Critic should return the exact text under
`## Copy to Codex after recovery execution-ready Critic PASS`
from Recovery Kickoff v0.2 verbatim.
