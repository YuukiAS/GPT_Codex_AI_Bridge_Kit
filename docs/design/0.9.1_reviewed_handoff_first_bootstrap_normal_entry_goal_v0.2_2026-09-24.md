# Bridge Kit 0.9.1 Reviewed First-Bootstrap — Canonical Goal v0.2

Goal version: **0.2**  
Date: **2026-09-24**  
Task key: **reviewed-handoff--first-bootstrap-normal-entry**  
Target repo: **YuukiAS/GPT_Codex_AI_Bridge_Kit**  
Related consumer repo: **YuukiAS/AI_Skills_Collection**  
Approved Plan: **docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_plan_v0.2_2026-09-24.md**  
Approved Plan commit: **eac789a35c4a83ce015a7744c722527580e79dc8**  
Previous Goal: **docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_goal_v0.1_2026-09-24.md**  
Execution-ready revision reason: **ER-B1-RAW-CUTOVER-BYPASSES-FIRST-BOOTSTRAP**  
Status: **DRAFT FOR EXECUTION_READY CRITIC RE-REVIEW / NOT EXECUTION AUTHORIZATION**

This Goal preserves the Critic-approved Plan v0.2 architecture. The only execution-package change from Goal v0.1 is ordering: Bridge 0.9.1 is implemented and activated first, then the **new candidate task-bootstrap command itself** creates the real sibling AI_Skills implementation branch/worktree. There is no raw Git cutover.

## 1. Positive completion

The task is complete only when one frozen Bridge + AI_Skills candidate pair proves all of the following:

1. Bridge 0.9.1 is implemented first on the already-authorized canonical Bridge `main` checkout;
2. the frozen Bridge candidate is loaded by the real `ai-bridge` runtime and its Machine Policy is installed/validated on the one authorized CODEX_HOME;
3. the **primary FB-G1 positive** is the real sibling AI_Skills implementation placement:
   - repo `YuukiAS/AI_Skills_Collection`;
   - task `workflow-core--reviewed-first-bootstrap-normal-entry`;
   - branch derived as `reviewed/workflow-core--reviewed-first-bootstrap-normal-entry`;
   - worktree `/home/yuukias/AI_Skills_Collection-workflow-core--reviewed-first-bootstrap-normal-entry`;
   - created through `ai-bridge reviewed-handoff task bootstrap`, not raw Git;
4. the current-user-approved Kickoff is sufficient for that `task bootstrap` prompt/Auto-review path; there is no second manual worktree/bootstrap action;
5. if the real sibling task-bootstrap is rejected, FB-G1 = FAIL and execution returns Planner/Critic; no raw Git fallback, `/tmp` substitution, permanent bootstrap allow, alternate branch or clone is used;
6. only after FB-G1 succeeds does workflow-core implementation proceed in that exact newly-created reviewed worktree;
7. first REQUEST/CURRENT exist only in that reviewed worktree; canonical AI_Skills main is not polluted with bootstrap task files;
8. existing artifact-bound `materialize-worktree` remains permanent-allow and supports local/remote-only resume;
9. remote-only resume reads/validates exact remote REQUEST/CURRENT before checkout and never copies them into canonical main;
10. rollback remains current-invocation-only, clean, bounded and non-destructive;
11. workflow-core production routing actually uses:
    - task missing -> Bridge `task bootstrap`;
    - task existing locally or on exact reviewed remote -> Bridge `materialize-worktree --mode resume`;
    - missing/old Bridge capability -> early fail before substantive work;
    - never raw `git worktree add` fallback;
12. Bridge closes as 0.9.1 PATCH with README/CHANGELOG/version parity;
13. AI_Skills closes the workflow-core user-visible routing change under its current version policy;
14. FB-G1 through FB-G5 PASS on the exact frozen cross-repo candidate pair.

Unit tests, CI, helper-only execution or `/tmp` fixtures cannot substitute for the real sibling-path FB-G1 positive.

## 2. Frozen architecture

Unchanged from Plan v0.2:

- new first-bootstrap entry: `ai-bridge reviewed-handoff task bootstrap`;
- `task bootstrap` = Machine Policy prompt, never permanent allow;
- `materialize-worktree` = artifact-bound existing-task permanent allow;
- raw `git worktree add` = prompt/gated, never normal fallback;
- branch = `reviewed/<task_key>`, no caller `--branch`;
- first REQUEST/CURRENT only in new reviewed worktree;
- one shared internal task initializer;
- remote-only resume validates exact remote metadata before checkout;
- no canonical-main metadata copy;
- no auth DB/token/receipt store;
- no cross-repo registry/ledger;
- no new watcher/controller/workflow/state machine;
- no generic Git/worktree facade;
- no force/destructive cleanup.

If implementation requires changing these semantics, stop and return Planner/Critic.

## 3. Exact execution placement and non-circular ordering

### Phase 0 — ordinary preflight only

Bridge canonical implementation source:

```text
repo:
YuukiAS/GPT_Codex_AI_Bridge_Kit

branch:
main

worktree:
/home/yuukias/GPT_Codex_AI_Bridge_Kit
```

AI_Skills canonical source used only for read/fetch/version/base discovery before FB-G1:

```text
repo:
YuukiAS/AI_Skills_Collection

canonical checkout:
/home/yuukias/AI_Skills_Collection

base ref:
origin/main

implementation task:
workflow-core--reviewed-first-bootstrap-normal-entry

derived branch:
reviewed/workflow-core--reviewed-first-bootstrap-normal-entry

exact future worktree:
/home/yuukias/AI_Skills_Collection-workflow-core--reviewed-first-bootstrap-normal-entry
```

At Kickoff preflight:

- fetch AI_Skills `origin/main`;
- record the exact OID once;
- verify the exact sibling worktree path and derived branch do not already conflict;
- read current AI_Skills version policy, VERSION, workflow-core source/version;
- do not create the AI branch/worktree yet.

### Phase 1 — Bridge implementation only

Implement approved Bridge 0.9.1 on canonical Bridge `main`.

Complete:

- production code;
- focused/full deterministic regressions;
- Bridge version = 0.9.1;
- README/CHANGELOG candidate closure;
- candidate commit freeze.

No AI_Skills implementation branch/worktree exists yet.

### Phase 2 — activate the Bridge candidate

On the one authorized identity:

```text
CODEX_HOME=/home/yuukias/.codex
```

- verify loaded runtime identity;
- local editable refresh only if stale;
- standard managed-file backup;
- at most one Machine Policy install;
- validate:
  - `task bootstrap` -> prompt;
  - `materialize-worktree` -> allow;
  - raw `git worktree add` -> prompt.

Do not continue to AI_Skills if candidate activation/validation fails.

### Phase 3 — real sibling-path FB-G1 and AI_Skills placement

Invoke the **new Bridge candidate** normal entry against canonical AI_Skills:

```text
ai-bridge reviewed-handoff task bootstrap   --target /home/yuukias/AI_Skills_Collection   --task-key workflow-core--reviewed-first-bootstrap-normal-entry   --expected-repo YuukiAS/AI_Skills_Collection   --expected-worktree /home/yuukias/AI_Skills_Collection-workflow-core--reviewed-first-bootstrap-normal-entry   --expected-base-ref origin/main   --expected-base-commit <exact Phase-0 origin/main OID>   --objective "Route Reviewed first bootstrap/resume through canonical Bridge 0.9.1 entries."
```

Exact spelling of newly implemented options must match the approved Plan, but the effect is fixed.

This command:

- runs through the real candidate Machine Policy `prompt` path;
- consumes the current-user Kickoff authorization;
- must not require another user-visible manual bootstrap action;
- creates the derived reviewed branch and exact sibling worktree;
- creates first REQUEST/CURRENT only there.

This is the primary FB-G1 positive.

If Auto-review/sandbox rejects it:

```text
FB-G1=FAIL
NEXT_HANDOFF=PLANNER/CRITIC
```

Do not:

- ask the user to run raw Git;
- switch to `/tmp`;
- create a raw branch/worktree;
- permanently allow bootstrap;
- change branch/path/base.

### Phase 4 — workflow-core implementation

Only after Phase 3 succeeds, implement the approved small workflow-core consumer routing change in:

```text
/home/yuukias/AI_Skills_Collection-workflow-core--reviewed-first-bootstrap-normal-entry
```

Then perform source-first generation, tests/evals, version/changelog closure and ordinary non-force publication of the exact reviewed branch.

### Phase 5 — freeze cross-repo candidate pair

Freeze exact Bridge and AI_Skills members.

### Phase 6 — remaining gates

Run FB-G2–FB-G5 and required should-not-change regressions.

`/tmp/ai-bridge-091-first-bootstrap-gates/` may be used only for deterministic negatives/additional fixtures. It cannot replace the real sibling-path FB-G1 positive.

## 4. Bridge implementation scope

Allowed:

- `ai_bridge_kit/reviewed_handoff.py`;
- Reviewed CLI parser/routing for `task bootstrap`;
- shared internal task initializer;
- local/remote task-scope discovery;
- `templates/reviewed_handoff/README.md`;
- direct Reviewed prompt changes only if necessary to reference the canonical entry;
- `templates/host/rules/ai-bridge-global.rules`;
- `ai_bridge_kit/host.py` only for Machine Policy validation/install expectations if required;
- direct Reviewed/Host/CLI/version tests;
- `pyproject.toml`, `ai_bridge_kit/__init__.py`;
- `README.md`, `CHANGELOG.md`;
- task-owned evidence.

Do not change unrelated publisher, Persistent Run, Notifications, plugin-replay, Lite or Controlled Mode.

## 5. AI_Skills workflow-core scope

Canonical production owner:

```text
skills/core/codex-system/codex-workflow-protocol/SKILL.md
```

Allowed when directly required:

- workflow references;
- `agents/openai.yaml` only if the routing entry actually requires trigger metadata;
- workflow-core tests/evals;
- source-first generated Marketplace/plugin payload;
- `scripts/codex_marketplace_config.json`;
- `docs/plugin-changelogs/workflow-core.md`;
- root version parity owners;
- root README/CHANGELOG as required by canonical release policy;
- task evidence.

Do not add another root AGENTS rule.

Routing must be executable and explicit:

```text
Reviewed task requested
-> discover task + Bridge capability
   -> truly missing:
      ai-bridge reviewed-handoff task bootstrap
   -> existing locally or on exact reviewed remote:
      ai-bridge reviewed-handoff materialize-worktree --mode resume

Bridge capability/version unavailable:
-> fail early before substantive work
```

workflow-core must not implement Git mechanics.

## 6. Version/release closure

### Bridge

Target:

```text
0.9.0 -> 0.9.1
```

### AI_Skills

Package-preparation observation remains:

```text
repository VERSION = 5.1.0
workflow-core = 0.3
```

Execution must re-read current canonical version policy and current values before mutation.

If workflow-core is still 0.3 and this production improvement reaches release-ready closure:

```text
workflow-core -> 0.4
```

Repository release is the next PATCH from actual current release. No numeric patch slot is reserved now.

Any workflow-core material overlap/version drift -> stop Planner.

## 7. Machine Policy boundary

The approved Kickoff may authorize final-candidate activation only on:

```text
/home/yuukias/.codex
```

After Bridge candidate freeze:

- inspect actual runtime identity;
- at most one local-only editable refresh if stale;
- standard backup;
- at most one `host install`;
- validate expected prompt/allow/prompt decisions;
- restore exact managed-file backup on candidate regression;
- no second CODEX_HOME/install loop.

Plan/Goal text is not itself current-user install authorization.

## 8. FB-G1–FB-G5 execution evidence

### FB-G1 — real sibling first bootstrap

The primary positive is the actual AI_Skills implementation placement in Phase 3.

Required:

- exact sibling path under `/home/yuukias`;
- candidate `task bootstrap`;
- real installed candidate Machine Policy;
- one explicit user Kickoff;
- no second manual bootstrap action;
- no raw Git;
- REQUEST/CURRENT only in new reviewed worktree.

A `/tmp` PASS is insufficient.

### FB-G2 — exact scope fail closed

Use deterministic fixtures for wrong repo/base/path, derived-branch conflict, symlink, occupied/unrelated state, unexpected local/remote branch and scope drift. Bootstrap/raw Git stay prompt.

### FB-G3 — partial failure recovery

Inject failures after branch creation, worktree creation and control initialization. Only exact clean current-invocation state may rollback. Preserve dirty/ambiguous/unowned state.

### FB-G4 — local + remote-only resume

Required normal entry:

- local-complete resume regression;
- remove local task branch/worktree/task files from the canonical checkout while exact remote reviewed branch retains valid REQUEST/CURRENT;
- helper fetches/reads/validates those remote files before checkout;
- rematerialization succeeds;
- wrong/missing remote metadata fails closed;
- no canonical-main metadata copy.

### FB-G5 — frozen cross-repo pair

Freeze:

Bridge:
- exact commit;
- 0.9.1;
- actual loaded runtime;
- Machine Policy identity.

AI_Skills:
- exact reviewed-branch commit;
- actual repository candidate version;
- actual workflow-core candidate version;
- exact loaded source/generated workflow-core plugin identity.

Official normal workflow-core candidate entry must demonstrate:

```text
missing -> task bootstrap
existing/remote-only -> materialize-worktree --mode resume
Bridge capability missing/old -> early fail
no raw Git fallback
```

No mixed-candidate evidence.

## 9. Publication and external-effect boundary

Future approved Kickoff may authorize:

Bridge:
- task-owned commits on main;
- ordinary non-force publication to existing `origin/main`.

AI_Skills:
- no raw branch/worktree creation;
- branch/worktree are created only by the new candidate `task bootstrap`;
- task-owned commits on the exact derived reviewed branch;
- ordinary non-force publication of that exact branch, including its first same-name remote publication after implementation.

Local:
- deterministic fixture effects only under `/tmp/ai-bridge-091-first-bootstrap-gates/`.

Provider/credentials:
- current existing Codex/OpenAI/Marketplace scope only;
- no paid API;
- no new account/provider/token/auth copy.

Forbidden:

- force/destructive Git;
- alternate branch/worktree/clone/remap;
- PR/main integration/tag/release;
- new credential/provider;
- secret display;
- unrelated external effects.

## 10. Stop rules

Stop and return Planner/Critic if:

- Bridge candidate cannot reach validated prompt/allow/prompt Machine Policy;
- real sibling Phase-3 `task bootstrap` is rejected or requires a second manual user bootstrap action;
- implementation needs raw Git fallback;
- remote-only resume must checkout before validation or copy metadata into main;
- shared initializer cannot remain single-source;
- rollback requires force/destructive cleanup;
- workflow-core needs Git mechanics;
- version/source overlap changes scope;
- exact loaded candidate pair cannot be proven.

Do not create a successor merely because a test fails.
