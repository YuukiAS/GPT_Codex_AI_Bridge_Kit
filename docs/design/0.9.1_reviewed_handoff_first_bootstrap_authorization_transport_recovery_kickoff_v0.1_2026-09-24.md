# Bridge Kit 0.9.1 — First Bootstrap Authorization Transport Recovery Kickoff Draft v0.1

Status: **DRAFT FOR EXECUTION_READY CRITIC REVIEW / NOT AUTHORIZED**

Bound recovery design:

- Recovery Plan:
  `docs/design/0.9.1_reviewed_handoff_first_bootstrap_authorization_transport_recovery_plan_v0.5_2026-09-24.md`
- Recovery Plan commit:
  `bdeda15529fe784515185165b18cfbac4531951c`
- Recovery Goal:
  `docs/design/0.9.1_reviewed_handoff_first_bootstrap_authorization_transport_recovery_goal_v0.1_2026-09-24.md`

This recovery deliberately uses two repo-local Codex contexts.

- **Part A**: Bridge Codex hardens/installs the recovery candidate.
- **Part B**: after Part A reports ready, AI_Skills Codex runs the real repo-local FB-G1-AT gate.

Part B is the single current-user Kickoff for the actual AI_Skills first-bootstrap effect. There is no raw Git fallback.

Only send these after execution-ready Critic PASS.

---

## Part A — Copy to Bridge Codex first

执行 Bridge 0.9.1 first-bootstrap authorization-transport recovery。

权威文件：

```text
Recovery Plan:
docs/design/0.9.1_reviewed_handoff_first_bootstrap_authorization_transport_recovery_plan_v0.5_2026-09-24.md

Recovery Goal:
docs/design/0.9.1_reviewed_handoff_first_bootstrap_authorization_transport_recovery_goal_v0.1_2026-09-24.md
```

只执行 Bridge recovery hardening；不要修改 AI_Skills workflow-core，不要创建 AI_Skills worktree。

### Exact Bridge identity

```text
Repository:
YuukiAS/GPT_Codex_AI_Bridge_Kit

Branch:
main

Worktree:
/home/yuukias/GPT_Codex_AI_Bridge_Kit

Parent task:
reviewed-handoff--first-bootstrap-normal-entry
```

我授权在这个 exact Bridge main/canonical worktree 内：

- 读取 latest main/AGENTS/recovery Plan/Goal；
- 实现 recovery Plan v0.5 要求的 Bridge-only hardening；
- 修改 Goal 允许的 source/tests/README/CHANGELOG/evidence；
- 运行 focused/full tests；
- task-owned ordinary commit；
- 使用 bounded：
  `ai-bridge host publish-current-branch --expected-repo YuukiAS/GPT_Codex_AI_Bridge_Kit --expected-branch main`
  做 ordinary non-force publication；
- 冻结一个 exact 0.9.1 recovery candidate。

### Required hardening

必须保持 repo-local architecture，不重新设计：

- cwd Git repo only；
- branch = `reviewed/<task_key>`；
- deterministic sibling worktree；
- base = post-sync local `origin/main` exact OID；
- bootstrap zero network；
- RL-B1 reachable executable fence；
- RL-B1 output symlink fence；
- RL-B2 canonical remote profile：
  - configured remotes exactly `origin`;
  - `remote.origin.skipFetchAll` unset/false；
  - `remote.origin.skipDefaultUpdate` unset/false；
  - exactly one:
    `+refs/heads/*:refs/remotes/origin/*`；
- noncanonical repos fail closed；
- no config normalization；
- raw `git worktree add` remains prompt。

### Machine Policy activation authorization

只有 recovery candidate source/tests/docs/evidence closure通过后：

Target：

```text
CODEX_HOME=/home/yuukias/.codex
```

我授权：

1. 检查实际 `ai-bridge` executable/import source identity；
2. 如果 existing editable install 不是 exact candidate，最多一次 local-only：
   `pip install -e /home/yuukias/GPT_Codex_AI_Bridge_Kit`
   到当前 existing Python environment；
3. managed-file mutation前创建标准 Bridge backup；
4. 最多一次：
   `ai-bridge host install --codex-home /home/yuukias/.codex`；
5. 运行 `ai-bridge host validate`；
6. 必须验证：
   - `reviewed-handoff task bootstrap -> allow`;
   - `materialize-worktree -> allow`;
   - raw `git worktree add -> prompt`;
   - `git fetch --all --prune -> allow`；
7. 保存 exact pre-install backup locator 和 exact candidate/runtime/policy identity。

如果安装/validate失败，恢复 exact backup并停止。

### Part A stop condition

Part A 不运行真实 AI_Skills gate。

完成后只报告：

```text
BRIDGE_RECOVERY_IMPLEMENTATION=PASS | FAIL
BRIDGE_CANDIDATE_COMMIT=
BRIDGE_VERSION=
LOADED_AI_BRIDGE_IDENTITY=
MACHINE_POLICY_IDENTITY=
PRE_INSTALL_BACKUP_LOCATOR=
TASK_BOOTSTRAP_POLICY=allow | other
MATERIALIZE_POLICY=allow | other
RAW_WORKTREE_POLICY=prompt | other
FETCH_ALL_PRUNE_POLICY=allow | other
READY_FOR_AI_SKILLS_FB_G1_AT=YES | NO
```

只有 `READY_FOR_AI_SKILLS_FB_G1_AT=YES` 时，用户才发送 Part B。

禁止：

- 修改 workflow-core；
- 创建 AI_Skills branch/worktree；
- raw Git fallback；
- 新 CODEX_HOME；
- force/destructive Git；
- PR/tag/release；
- paid API；
- new provider/account/credential；
- unrelated source changes。

---

## Part B — Copy to AI_Skills Codex only after Part A PASS

这是 Bridge 0.9.1 recovery 的真实 FB-G1-AT gate。

只做 gate，不实现 workflow-core。

### Exact AI_Skills context

```text
Repository:
YuukiAS/AI_Skills_Collection

Canonical cwd/worktree:
/home/yuukias/AI_Skills_Collection

Task:
workflow-core--reviewed-first-bootstrap-normal-entry

Derived branch:
reviewed/workflow-core--reviewed-first-bootstrap-normal-entry

Derived sibling:
/home/yuukias/AI_Skills_Collection-workflow-core--reviewed-first-bootstrap-normal-entry
```

本消息授权这个 exact repo-local first-bootstrap effect。

不要跨 repo。
不要 raw `git worktree add`。
不要改用 `/tmp`。
不要修改 workflow-core。

### Gate preflight

先验证：

```text
configured remotes = exactly ["origin"]

remote.origin.skipFetchAll = unset/false

remote.origin.skipDefaultUpdate = unset/false

remote.origin.fetch =
+refs/heads/*:refs/remotes/origin/*
```

还要验证已安装 Bridge candidate 的 RL-B1 executable/write fences对当前 AI_Skills repo可通过。

如果 remote profile 非 canonical 或 RL-B1 fence不通过：

- 不修改 Git config；
- 不创建 worktree；
- 不使用 raw Git；
- 直接报告 FB-G1-AT FAIL/NOT-ELIGIBLE；
- workflow-core保持未修改。

### Authoritative sync

canonical profile通过后，运行 existing low-friction：

```text
git fetch --all --prune
```

然后记录 exact：

```text
POST_SYNC_ORIGIN_MAIN_OID=<oid>
```

并检查：

```text
refs/remotes/origin/reviewed/workflow-core--reviewed-first-bootstrap-normal-entry
```

如果已经存在，不运行 first bootstrap；报告已有 Reviewed task，需要回 Planner按 existing-task route处理。

### Real first bootstrap

只有 reviewed remote-tracking branch absent 时，从当前 AI_Skills cwd 调用 installed recovery candidate 的 repo-local：

```text
ai-bridge reviewed-handoff task bootstrap   --task-key workflow-core--reviewed-first-bootstrap-normal-entry   --expected-repo YuukiAS/AI_Skills_Collection   --expected-base-commit <POST_SYNC_ORIGIN_MAIN_OID>   --objective "Route Reviewed first bootstrap/resume through canonical Bridge 0.9.1 entries."
```

使用 recovery candidate 实际 CLI 的最终 approved syntax；不得增加 caller branch/path/base-ref selector。

成功必须证明：

- `task bootstrap` 没有触发第二次 approval/Auto-review；
- actual sibling：
  `/home/yuukias/AI_Skills_Collection-workflow-core--reviewed-first-bootstrap-normal-entry`
  被创建；
- branch为：
  `reviewed/workflow-core--reviewed-first-bootstrap-normal-entry`；
- REQUEST/CURRENT正确；
- canonical AI_Skills main没有 bootstrap task files；
- bootstrap内部 zero network；
- no raw Git fallback；
- no alternate path；
- no workflow-core production edit。

### Part B stop condition

无论 PASS/FAIL，都不要继续 workflow-core。

只报告：

```text
FB_G1_AT=PASS | FAIL | NOT_ELIGIBLE
AI_SKILLS_CWD=
CONFIGURED_REMOTES=
ORIGIN_SKIP_FETCH_ALL=
ORIGIN_SKIP_DEFAULT_UPDATE=
ORIGIN_FETCH_REFSPEC=
POST_SYNC_ORIGIN_MAIN_OID=
REMOTE_REVIEWED_BRANCH_PRESENT=YES | NO
TASK_BOOTSTRAP_POLICY=allow | other
SECOND_APPROVAL_OCCURRED=YES | NO
REAL_SIBLING_CREATED=YES | NO
REQUEST_CURRENT_VALID=YES | NO
CANONICAL_MAIN_POLLUTED=YES | NO
BOOTSTRAP_NETWORK_CALLS=0 | nonzero
RAW_GIT_FALLBACK_USED=YES | NO
WORKFLOW_CORE_MODIFIED=NO
NEXT_HANDOFF=PLANNER
```

如果 gate FAIL：
- 不尝试 raw Git、`/tmp`、config normalization或 permanent permission widening；
- 保留 evidence；
- 告诉用户把结果交回 Planner；
- workflow-core保持暂停。

如果 gate PASS：
- 保留新 Reviewed sibling；
- 不继续实现 workflow-core；
- 返回 Planner，由 Planner恢复之前批准的 consumer-routing执行。
