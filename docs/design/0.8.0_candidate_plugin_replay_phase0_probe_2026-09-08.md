# Bridge Kit 0.8.0 Candidate Plugin Replay Phase 0 Runtime Probe

Date: 2026-09-08

Conclusion:

```text
SELECTED_MODE=B0
```

`B1` was not run because the frozen A -> B0 -> B1 rule stops immediately after
B0 succeeds.

## Environment

- Bridge Kit commit at probe fixture selection: `14883729b1d13345d817b3adb0797243bc03cb06`
- Branch: `main`
- Worktree before fixture/report edits: clean after fast-forward pull from
  `3b061167794d593b113ca8f4a8a43c4c8000fc01` to
  `bc98305d3edcb2aa1f4b592eb9aa6db91594fd63`
- `CODEX_HOME`: `/Users/yuukias/.codex`
- Non-secret `CODEX_HOME` locator: resolved path above; no alternate identity
  selected or copied.
- Codex executable: `/Applications/ChatGPT.app/Contents/Resources/codex`
- Codex CLI version: `codex-cli 0.153.4`
- Codex source commit locator: not reliably observable from the installed CLI.

## Candidate Fixture

The probe used a harmless committed same-name fixture:

- plugin name: `visualize`
- pre-existing same-name installed identity:
  `visualize@openai-bundled`
- candidate commit:
  `14883729b1d13345d817b3adb0797243bc03cb06`
- committed manifest:
  `.agents/plugins/marketplace.json`
- committed manifest entry:
  `name=visualize`,
  `source.source=local`,
  `source.path=./docs/design/fixtures/candidate-plugin-replay/plugins/visualize`
- staged marketplace:
  `/private/tmp/ai-bridge-candidate-replay-phase0-20260908T003927/marketplace`
- generated candidate marketplace:
  `ai-bridge-candidate-phase0-20260908T003927`
- staged plugin path:
  `/private/tmp/ai-bridge-candidate-replay-phase0-20260908T003927/marketplace/plugins/visualize`
- staged plugin tree digest:
  `1e74d588415748362af2251d8570ea42df9087f3f1e35bed223406ca8cbce8b1`
- unique harmless marker:
  `AI_BRIDGE_PHASE0_CANDIDATE_VISUALIZE_MARKER_20260908`

The fixture was staged from the candidate Git tree with `git show` /
`git ls-tree`, not from dirty working-tree content.

## Smoke A

Question: can the uninstalled candidate be consumed from repo/cwd marketplace
discovery alone?

Command shape:

```text
codex exec --ignore-user-config --json \
  -C /Users/yuukias/Code/GPT_Codex_AI_Bridge_Kit \
  --skip-git-repo-check --ephemeral \
  -o .../outputs/a-last-message.txt \
  <natural candidate-proof prompt>
```

Result:

```text
NO_CANDIDATE_SKILL_LOADED
```

Conclusion: A failed to establish real runtime loading. The probe therefore
continued to B0.

## Smoke B0

B0 used process-local marketplace configuration for plugin add and child
selection. It did not run persistent `codex plugin marketplace add`.

Install command shape:

```text
codex plugin \
  -c marketplaces.ai-bridge-candidate-phase0-20260908T003927.source_type="local" \
  -c marketplaces.ai-bridge-candidate-phase0-20260908T003927.source="/private/tmp/ai-bridge-candidate-replay-phase0-20260908T003927/marketplace" \
  add --json visualize@ai-bridge-candidate-phase0-20260908T003927
```

Install result:

```json
{
  "pluginId": "visualize@ai-bridge-candidate-phase0-20260908T003927",
  "name": "visualize",
  "marketplaceName": "ai-bridge-candidate-phase0-20260908T003927",
  "version": "0.8.0-phase0.1",
  "installedPath": "/Users/yuukias/.codex/plugins/cache/ai-bridge-candidate-phase0-20260908T003927/visualize/0.8.0-phase0.1",
  "authPolicy": "ON_USE"
}
```

Installed plugin tree digest:
`1e74d588415748362af2251d8570ea42df9087f3f1e35bed223406ca8cbce8b1`.

Digest match: installed tree digest matched the staged tree digest exactly.

Effective-plugin preflight command shape:

```text
codex plugin \
  -c marketplaces.ai-bridge-candidate-phase0-20260908T003927.source_type="local" \
  -c marketplaces.ai-bridge-candidate-phase0-20260908T003927.source="/private/tmp/ai-bridge-candidate-replay-phase0-20260908T003927/marketplace" \
  -c plugins.visualize@ai-bridge-candidate-phase0-20260908T003927.enabled=true \
  list --json
```

Preflight same-name identities:

```text
visualize@openai-bundled: installed=true, enabled=true
visualize@ai-bridge-candidate-phase0-20260908T003927: installed=true, enabled=true
```

Implementation note from the live CLI: the process-local plugin enable override
must use `plugins.visualize@...enabled=true`. Passing
`plugins."visualize@...".enabled=true` through CLI `-c` made the current loader
treat the quote characters as part of the plugin name.

Child command shape:

```text
codex exec --ignore-user-config --json \
  -c marketplaces.ai-bridge-candidate-phase0-20260908T003927.source_type="local" \
  -c marketplaces.ai-bridge-candidate-phase0-20260908T003927.source="/private/tmp/ai-bridge-candidate-replay-phase0-20260908T003927/marketplace" \
  -c plugins.visualize@ai-bridge-candidate-phase0-20260908T003927.enabled=true \
  -c approval_policy="never" \
  -c sandbox_workspace_write.network_access=false \
  --disable memories \
  -s workspace-write \
  -C /private/tmp/ai-bridge-candidate-replay-phase0-20260908T003927/workspace \
  --add-dir /private/tmp/ai-bridge-candidate-replay-phase0-20260908T003927/outputs \
  --skip-git-repo-check \
  --ephemeral \
  -o .../outputs/b0-marker-last-message.txt \
  <natural candidate-proof prompt>
```

Child-period `config.toml` hashes:

```text
before child: b3d473350c874fe72382c19e409a3a928ad54c0c5a1113b9058deeff2d093464
after child, before cleanup: b3d473350c874fe72382c19e409a3a928ad54c0c5a1113b9058deeff2d093464
```

Hash result: unchanged.

Runtime consumption proof:

```text
AI_BRIDGE_PHASE0_CANDIDATE_VISUALIZE_MARKER_20260908
```

The child event stream showed the skill file read from the installed candidate
cache path:

```text
/Users/yuukias/.codex/plugins/cache/ai-bridge-candidate-phase0-20260908T003927/visualize/0.8.0-phase0.1/skills/visualize/SKILL.md
```

This path is the installed candidate plugin path, not the committed fixture
source path and not the pre-existing `visualize@openai-bundled` plugin.

Conclusion: B0 passed on the current real Codex runtime.

## Cleanup And Preservation

B0 cleanup removed only:

- `visualize@ai-bridge-candidate-phase0-20260908T003927`
- `/private/tmp/ai-bridge-candidate-replay-phase0-20260908T003927/marketplace`

Post-cleanup verification:

```text
candidate_absent=true
staging_absent=true
production_present=true
production_digest_before=41d5c82fb7897657df481a26120b3893b3b9bb06464c3517e760413a333831a9
production_digest_after=41d5c82fb7897657df481a26120b3893b3b9bb06464c3517e760413a333831a9
production_digest_unchanged=true
```

No persistent candidate marketplace was added, so no marketplace remove was
required.

## Remaining Production Gate

No runtime unknown blocks production implementation of B0. The later production
work still must implement the planned lock, `active.json`
journal-before-first-persistent-mutation ordering, stale cleanup, Host Policy capability, old
`plugin-replay` regression coverage, and the selected-mode Smoke C recovery
gate before release.
