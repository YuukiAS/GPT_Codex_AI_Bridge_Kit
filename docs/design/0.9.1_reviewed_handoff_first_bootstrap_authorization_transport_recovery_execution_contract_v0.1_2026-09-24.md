# Bridge Kit 0.9.1 — First Bootstrap Authorization Transport Recovery Execution Contract v0.1

Contract version: **0.1**  
Date: **2026-09-24**  
Parent task: **reviewed-handoff--first-bootstrap-normal-entry**  
Approved recovery Plan: **v0.5 @ bdeda15529fe784515185165b18cfbac4531951c**  
Recovery Goal: **docs/design/0.9.1_reviewed_handoff_first_bootstrap_authorization_transport_recovery_goal_v0.1_2026-09-24.md**  
Kickoff Draft: **docs/design/0.9.1_reviewed_handoff_first_bootstrap_authorization_transport_recovery_kickoff_v0.1_2026-09-24.md**  
Status: **DRAFT FOR EXECUTION_READY CRITIC REVIEW**

This contract freezes execution order, per-context ownership, restoration, and stop conditions. It does not authorize execution.

## 1. Phase order

### Phase A1 — Bridge preflight

In:

```text
/home/yuukias/GPT_Codex_AI_Bridge_Kit
```

verify:

- repo identity;
- branch `main`;
- origin;
- latest AGENTS/recovery Plan/Goal;
- 0.9.1 version parity;
- current production source lineage from implemented candidate 44965d;
- no overlapping production drift after the recovery design package.

Only docs-only drift inside the approved recovery package is expected.

### Phase A2 — Bridge-only hardening

Implement recovery Plan v0.5 only.

No AI_Skills write.

Required implementation classes:

- repo-local self-scope;
- deterministic sibling;
- exact post-sync `origin/main` base assertion;
- RL-B1 executable fence;
- RL-B1 output path fence;
- RL-B2 canonical remote profile;
- network-free bootstrap;
- Host rule `task bootstrap: prompt -> allow`;
- focused/full regressions;
- README/CHANGELOG/evidence correction.

### Phase A3 — candidate freeze

Create one exact Bridge candidate commit.

Record:

- commit;
- version 0.9.1;
- focused/full test results;
- README/CHANGELOG state;
- evidence locator.

No Machine Policy mutation before this freeze.

### Phase A4 — Machine Policy activation

Target only:

```text
/home/yuukias/.codex
```

- verify actual runtime/import identity;
- local editable refresh only if stale and authorized by the approved Kickoff;
- standard backup;
- at most one host install;
- validate exact policy decisions.

On failure:
- restore exact backup;
- stop;
- do not proceed to FB-G1-AT.

### Phase A5 — Bridge handoff

Bridge execution stops after reporting:

```text
READY_FOR_AI_SKILLS_FB_G1_AT=YES
```

with exact candidate/runtime/policy/backup identities.

Bridge Codex does not create the AI_Skills sibling.

---

## 2. Phase B — real AI_Skills FB-G1-AT

This phase is initiated only by the user sending Part B of the approved Kickoff to a Codex context operating the AI_Skills canonical repo.

### B1 — repo-local context

Required cwd:

```text
/home/yuukias/AI_Skills_Collection
```

Any other repo context fails the gate.

### B2 — local safe-class checks

Before network sync:

- configured remotes exactly `origin`;
- `skipFetchAll` unset/false;
- `skipDefaultUpdate` unset/false;
- exactly one canonical full-heads origin refspec;
- RL-B1 executable/write fences pass;
- deterministic sibling is absent/unoccupied;
- local reviewed branch absent.

No config normalization.

### B3 — authoritative sync

Run only the existing allowed:

```text
git fetch --all --prune
```

Then capture:

- `origin/main` OID;
- reviewed remote-tracking branch presence.

If reviewed remote branch exists:
- do not first-bootstrap;
- return Planner for existing-task routing.

### B4 — trusted bootstrap

Invoke installed Bridge candidate from the same AI_Skills cwd.

No caller-selected target/path/base-ref.

The helper itself re-checks:

- current repo identity;
- exact canonical remote profile;
- exact local `origin/main` OID;
- reviewed remote-tracking branch absence;
- all RL-B1 fences.

Then it creates only the deterministic sibling.

### B5 — gate verification

Verify:

- no second approval;
- branch/path exact;
- REQUEST/CURRENT valid;
- canonical main unpolluted;
- zero network inside bootstrap;
- raw Git fallback absent;
- workflow-core unchanged.

Stop immediately after evidence.

---

## 3. Bridge candidate recovery

If implementation fails before candidate freeze:
- repair only within recovery Plan v0.5;
- no install.

If a repair changes accepted architecture:
- stop Planner/Critic.

If tests expose a previously missed generic security path:
- stop and return Planner/Critic rather than broadening the scanner/framework.

---

## 4. Machine Policy restoration

The pre-install managed-file backup is mandatory evidence.

If activation fails:
- restore immediately in Bridge context.

If Part B later reports FB-G1-AT failure due:
- approval still occurring;
- sibling sandbox rejection;
- installed policy/runtime mismatch;

then the recovery attempt is not validated.

The user should return the Part B result to the Bridge recovery thread; Bridge owner restores the exact pre-install Machine Policy backup before a subsequent recovery attempt.

Failure restoration is not part of the low-friction product gate and may require this explicit recovery handoff.

Do not broaden permission as a failure workaround.

---

## 5. AI_Skills topology recovery

Before successful bootstrap:
- no AI topology mutation should exist.

If bootstrap partially mutates:
- use only existing invocation-local rollback;
- preserve dirty/ambiguous state;
- no raw Git completion.

If bootstrap succeeds:
- preserve the new sibling for later workflow-core work;
- do not push/create remote reviewed branch in this recovery gate;
- do not edit workflow-core.

---

## 6. Evidence ownership

Bridge results own:

- implementation diff/tests;
- candidate identity;
- Machine Policy backup/install/validate;
- final Part B gate transcript/result supplied back by the user;
- recovery conclusion.

AI_Skills worktree may contain only the normal bootstrap-created Reviewed task state before workflow-core continuation.

No separate ledger/database is introduced.

---

## 7. Stop conditions

Stop and return Planner/Critic if:

- repo-local self-scope cannot be enforced without redesign;
- RL-B1 fence needs a generic Git security framework;
- canonical remote-profile proof cannot be implemented narrowly;
- bootstrap requires network;
- standing allow still causes approval;
- real sibling remains blocked by sandbox;
- AI_Skills config is noncanonical for the trusted safe class;
- raw Git fallback appears necessary;
- workflow-core must be modified before FB-G1-AT PASS;
- Machine Policy restoration cannot be performed from the saved backup.

## 8. Completion

Recovery execution is complete only after real Part B FB-G1-AT PASS.

Then:

```text
RECOVERY_COMPLETE=YES
WORKFLOW_CORE_MODIFIED=NO
NEXT_HANDOFF=PLANNER
```

The original workflow-core continuation remains a separate next phase.
