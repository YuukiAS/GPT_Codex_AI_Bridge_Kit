# Bridge 0.9.2 Unattended Execution Refinement — Architecture PASS Transfer Record v0.1

Date: 2026-09-25  
Task key: `bridge-core--unattended-execution-refinement`  
Status: USER-TRANSFERRED CRITIC SUMMARY / NOT A PLANNER SELF-APPROVAL

## Reviewed object

```text
Proposal:
docs/design/0.9.2_unattended_execution_persistent_run_refinement_plan_v0.1_2026-09-25.md

Proposal version:
v0.1

Proposal commit:
9c5c7f7ac2bb110dcba98deea2b0c0d8bb7ffa1f

Review stage:
PRE_IMPLEMENTATION_ARCHITECTURE
```

## Provenance

The original independent Critic response is not present on Bridge `main` at the time this execution package is prepared.

The user transferred the Critic result back to the Planner thread and explicitly stated that Proposal v0.1 received pre-implementation architecture `PASS`.

This file records only that transferred summary and its execution-package conditions. It is **not** a Planner-authored substitute for the Critic review and does not expand the PASS.

## User-transferred PASS conditions

The execution package must preserve all of the following:

1. Keep **Option B**.
2. Separate:
   - upfront authorization readiness;
   - process persistence;
   - progress reporting.
3. Do not add:
   - a new state;
   - an authorization store/database;
   - daemon/watcher;
   - Host allowlist expansion;
   - scheduler wrapper.
4. Primary implementation surface:
   - authoring prompts;
   - installed Persistent Run templates;
   - `README.md`;
   - `QUICKSTART.md`;
   - `AGENTS.md`;
   - `CHANGELOG.md`;
   - focused tests.
5. `ai_bridge_kit/persistent_run.py` is **should-not-change by default**. If the approved behavior cannot be achieved without changing it, stop and return Planner/Critic rather than silently broadening scope.
6. G1/G2 must produce behavior evidence from the **final candidate through the normal Codex / Auto-review path**. Keyword/source-presence tests alone are insufficient.
7. If the actual target surface exposes an Auto-review or context-resume blocker, stop and return Planner. Do not widen permissions or invent a bypass.
8. DII job `922349` must be described accurately as a **scheduler-owned batch holder**. Its lifetime is owned by Slurm after `sbatch`.
9. tmux's real value in the DII example is the **terminal-owned orchestrator / `srun` lifecycle**, not job `922349` itself.
10. Existing DII Goal and active run are not migrated or modified.
11. Machine Policy remains unchanged.
12. The formal `release` ref advances only after all capability gates and closure are complete.

## Planner consequence

The Planner may now form a frozen execution package that implements only the already-PASS architecture above.

No implementation, release, machine sync, DII mutation, paid API call, Persistent Run launch, or automation is authorized by this record.
