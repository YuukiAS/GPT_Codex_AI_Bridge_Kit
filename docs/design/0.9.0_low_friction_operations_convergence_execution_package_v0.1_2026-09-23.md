# Bridge Kit 0.9.0 Low-Friction Operations Convergence — Execution Package v0.1

Package version: **0.1**  
Date: **2026-09-23**  
Task key: **bridge-core--low-friction-operations-convergence**  
Target repo: **YuukiAS/GPT_Codex_AI_Bridge_Kit**  
Source branch/ref: **main**  
Review stage: **DESIGN_PASS -> EXECUTION_READY CRITIC REVIEW**

This index binds the exact execution package that must be reviewed together before any implementation begins.

## Approved design

- Plan:
  `docs/design/0.9.0_low_friction_operations_convergence_plan_v0.3_2026-09-23.md`
- Plan version: `0.3`
- Approved Plan commit:
  `11f770fdb8a8d928aefbfda5f42527270a19f8b4`
- Design Critic result transferred by user: `PASS`

The design PASS is not execution authorization.

## Canonical Goal

- Path:
  `docs/design/0.9.0_low_friction_operations_convergence_goal_v0.1_2026-09-23.md`
- Goal version: `0.1`
- First commit:
  `036a01d616e15c241a890e2c97577d0211d12719`

The Goal preserves approved G1-G15, source scope, Machine Policy live-install boundary, same-final-candidate release contract, restoration rules, and stop conditions.

## Kickoff Draft

- Path:
  `docs/design/0.9.0_low_friction_operations_convergence_kickoff_v0.1_2026-09-23.md`
- Kickoff version: `0.1`
- First commit:
  `ccb1ecdb31db4a297de047bd4fe5699e01b38c57`

The Kickoff is a draft until independent execution-ready Critic PASS.

Only the user sending the Critic-approved Kickoff creates current-user execution authorization.

## Execution placement frozen for Critic review

The package proposes:

```text
repo:
YuukiAS/GPT_Codex_AI_Bridge_Kit

task:
bridge-core--low-friction-operations-convergence

branch:
main

worktree:
the current canonical checkout in which the approved Kickoff is sent;
resolve and record its absolute git rev-parse --show-toplevel path at startup;
no new/sibling worktree or branch is authorized for this implementation task.
```

This avoids a bootstrap dependency on the not-yet-implemented Reviewed worktree materializer. The materializer itself is validated in disposable test repositories/worktrees.

## Execution authorization surface in the Kickoff

The Kickoff explicitly covers:

- approved Bridge production source areas only;
- tests and temporary candidate policy/config probes;
- temporary local Git/bare-remote/worktree/tmux fixtures;
- ordinary task-owned commits on `main`;
- bounded ordinary non-force publication to `origin/main`;
- one late final-candidate real Machine Policy install/upgrade on the current resolved CODEX_HOME only, with standard backup and restore-on-regression boundary;
- current existing HTTPS credential manager / SSH agent/config paths;
- up to four operational Notifications test messages to already-configured existing recipients only;
- no paid API.

It explicitly does **not** authorize:

- force/destructive Git;
- new branch/remote/upstream topology as workaround;
- PR/release/tag creation;
- another CODEX_HOME;
- new provider/data/account/credential/recipient scope;
- paid API;
- auth DB/token store;
- wrapper registry/generic Git facade;
- daemon/controller/watcher hierarchy;
- new workflow class;
- unrelated external side effects.

## Execution-ready Critic review object

The next Critic review must treat these three artifacts as one package:

1. approved Plan v0.3;
2. Canonical Goal v0.1;
3. Kickoff Draft v0.1.

Critic should verify that Goal/Kickoff do not broaden or weaken the approved design, especially:

- G1-G15 fidelity;
- B2 transport execution fence including `GIT_CONFIG_GLOBAL`, `GIT_CONFIG_SYSTEM`, `GIT_CONFIG_NOSYSTEM`;
- no second wrapper;
- Reviewed watcher authority;
- Machine Policy install timing/restoration;
- exact user authorization boundary;
- display taxonomy vs compatibility identifiers;
- no paid/new-provider scope;
- same-final-candidate release closure.

If PASS, Critic should return the Kickoff **verbatim** as the approved execution prompt. Any semantic change to the Kickoff after PASS requires re-review.
