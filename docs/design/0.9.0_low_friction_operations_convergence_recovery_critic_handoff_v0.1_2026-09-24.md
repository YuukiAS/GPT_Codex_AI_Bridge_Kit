# Bridge Kit 0.9.0 Low-Friction Operations Convergence — Recovery Execution-Ready Critic Handoff v0.1

Date: **2026-09-24**  
Task key: **bridge-core--low-friction-operations-convergence**  
Review stage: **RECOVERY_EXECUTION_READY**  
Recovery package commit: **e1d37ad6cd959101a15e1219d33f04f5cbe5e4b4**

以下内容可完整复制到独立 Critic thread。

~~~text
你是 AI Research Stack 的独立 Critic。

target_repo:
YuukiAS/GPT_Codex_AI_Bridge_Kit

target_domain:
Bridge Kit core

design_topic_or_task_key:
bridge-core--low-friction-operations-convergence

review_stage:
RECOVERY_EXECUTION_READY

source_branch/ref:
main

approved design:
docs/design/0.9.0_low_friction_operations_convergence_plan_v0.3_2026-09-23.md

approved Plan version:
0.3

approved Plan commit:
11f770fdb8a8d928aefbfda5f42527270a19f8b4

existing canonical Goal:
docs/design/0.9.0_low_friction_operations_convergence_goal_v0.1_2026-09-23.md

current implementation baseline:
88fa67c44e6e491ed548f033ce0d3c33f63525c4

Recovery Goal:
docs/design/0.9.0_low_friction_operations_convergence_recovery_goal_v0.1_2026-09-24.md

Recovery Goal version:
0.1

Recovery Goal first commit:
411abef0040ebc33e5b831abd4e74bc7d50ef4db

Recovery Kickoff Draft:
docs/design/0.9.0_low_friction_operations_convergence_recovery_kickoff_v0.1_2026-09-24.md

Recovery Kickoff version:
0.1

Recovery Kickoff first commit:
fe0037850c4625eb91e5a09d04530ab61aec884e

Recovery Package:
docs/design/0.9.0_low_friction_operations_convergence_recovery_execution_package_v0.1_2026-09-24.md

Recovery Package version:
0.1

Recovery Package commit:
e1d37ad6cd959101a15e1219d33f04f5cbe5e4b4

Previous Critic result:
PRE_FINAL_IMPLEMENTATION_REVISE

Stable findings:
- B1-PUBLISH-PREFLIGHT-TRANSPORT-FENCE
- B2-PERSISTENT-RUN-REPORTER-INCOMPLETE
- B3-WORKTREE-FROZEN-SCOPE-FIDELITY
- B4-FINAL-GATE-EVIDENCE-INCOMPLETE

This is READ-ONLY recovery execution-ready review.

Do not:
- modify Git;
- implement production;
- install/upgrade Machine Policy;
- create branch/worktree;
- launch Executor;
- call paid API;
- start automation;
- redesign Plan v0.3;
- create successor task.

==================================================
0. GOVERNANCE / SOURCE
==================================================

Read latest AI_Skills_Collection main:
- docs/workflows/PLANNER_ROLE_CONTRACT.md
- docs/workflows/CRITIC_ROLE_CONTRACT.md
- docs/workflows/PLUGIN_CAPABILITY_GATE_POLICY.md

Then read Bridge latest main:
- AGENTS.md
- approved Plan v0.3
- existing Canonical Goal v0.1
- Recovery Goal v0.1
- Recovery Kickoff Draft v0.1
- Recovery Package v0.1
- current source necessary to understand the four findings:
  - ai_bridge_kit/host.py
  - ai_bridge_kit/persistent_run.py
  - ai_bridge_kit/reviewed_handoff.py
  - ai_bridge_kit/reviewed_runner.py
  - templates/persistent_run/*
  - relevant tests
  - results/bridge-core--low-friction-operations-convergence/EVIDENCE.md
  - results/bridge-core--low-friction-operations-convergence/FINAL_REPORT.md

Do not treat the old EVIDENCE/FINAL_REPORT complete wording as final authority.
The implementation baseline 88fa67c... was explicitly REVISE.

No new broad web research is necessary unless you find a concrete current-source ambiguity.
This review is package fidelity/authorization review, not a new architecture round.

==================================================
1. PLAN FIDELITY
==================================================

The Recovery Package must preserve Plan v0.3 exactly:

- native safe reads;
- one bounded publish-current-branch;
- one bounded reviewed-handoff materializer;
- plugin-replay KEEP_NARROW;
- candidate-plugin-replay absent;
- official plugin normal-entry;
- Persistent Run producer owns progress truth;
- Bridge normalization/projection only;
- Notifications transport-only;
- one integrated 0.9.0 release;
- no new wrapper/Git facade/auth DB/token store/daemon/watcher hierarchy/workflow class.

If Recovery Goal/Kickoff adds a new architecture mechanism, REVISE.
Otherwise do not reopen design findings already PASS.

==================================================
2. B1 RECOVERY CONTRACT
==================================================

Verify the Recovery Goal mechanically closes:

B1-PUBLISH-PREFLIGHT-TRANSPORT-FENCE

Required semantics:

- transport/config/credential/askpass rejection must complete before the first remote/network Git command,
  OR every such remote preflight must already run under the proven sanitized environment;
- no unsanitized ls-remote/fetch/push before rejection;
- executable-canary regressions for:
  - GIT_SSH
  - GIT_SSH_COMMAND
  - GIT_ASKPASS
  - SSH_ASKPASS
  - local/worktree/command credential.helper
  - core.sshCommand
- canary must not execute;
- no remote mutation in rejected cases;
- config-source regression includes:
  - GIT_CONFIG_COUNT / KEY / VALUE
  - GIT_CONFIG_GLOBAL
  - GIT_CONFIG_SYSTEM
  - GIT_CONFIG_NOSYSTEM
- normal HTTPS system/global credential manager remains usable;
- normal SSH agent/user config remains usable;
- no second wrapper;
- custom transport returns to ordinary raw push approval.

Check that the Recovery Kickoff authorizes the needed bounded tests without broadening credentials/provider scope.

==================================================
3. B2 RECOVERY CONTRACT
==================================================

Verify the Recovery Goal closes:

B2-PERSISTENT-RUN-REPORTER-INCOMPLETE

The package must require more than static progress normalization:

- event-driven reporter in existing Persistent Run;
- concise normalized report;
- machine-local latest;
- bounded recent history;
- reconnect query;
- event comparison;
- dedupe/rate-limit;
- contract fields:
  - Progress source locator
  - Report interval
  - Delivery mode
  - Stall threshold
- start/resume/stage-change/periodic/material-ETA/stall/block/failure/completion semantics;
- optional existing one-shot Notifications projection;
- notifier failure does not kill the run;
- no daemon/controller/watcher;
- no automatic intervention.

Required gate:
- real bounded multi-event process;
- separate UNKNOWN/stall case;
- not only static JSON fixture.

The Recovery Kickoff may use existing Notifications only if already configured,
same provider/recipient, max four operational test messages.

No new provider/credential and no paid API.

==================================================
4. B3 RECOVERY CONTRACT
==================================================

Verify the Recovery Goal closes:

B3-WORKTREE-FROZEN-SCOPE-FIDELITY

Required:

- frozen worktree identity must not move because both stored locator and caller assertion are resolved through a changed symlink target at invocation;
- changed symlink target/path redirection fail closed before mutation;
- preserve exact sibling and /tmp cases;
- bootstrap validates base_commit lineage against frozen origin/<base_branch>, not only local commit existence;
- invocation tracks branch/worktree objects it created;
- bounded rollback removes only invocation-created clean exact objects;
- never remote delete/reset/clean/pre-existing object removal;
- ownership/cleanliness ambiguity leaves state + recovery evidence.

No auth DB/schema family/new workflow.

==================================================
5. B4 / FINAL-EVIDENCE CONTRACT
==================================================

Verify the package requires:

1. B1-B3 repairs first;
2. one NEW final candidate after repairs;
3. no early EVIDENCE/FINAL_REPORT completion rewrite;
4. exactly one replacement/final-candidate Machine Policy install on:
   /home/yuukias/.codex
5. standard backup + restore-on-regression;
6. same-candidate real gate evidence.

At minimum the Recovery Goal must directly require:

- G1/G2 real normal Codex entry under installed replacement Machine Policy;
- G3/G4 real bounded publisher, positive HTTPS and standard SSH paths, all affected transport negatives;
- G5/G6/G7 trusted materializer normal entry, sibling + /tmp, no duplicate Auto-review interruption;
- G9 official Codex/AI_Skills install/reinstall/fresh-session route;
- G10/G11 multi-event reporter + UNKNOWN/stall;
- G13 real reviewed_runner guard environment;
- G14 same final candidate identity everywhere;
- G8/G12/G15 should-not-change confirmation.

Unit tests/CI cannot substitute for these normal-entry gates.

==================================================
6. RECOVERY AUTHORIZATION BOUNDARY
==================================================

The old Machine Policy install authorization was already consumed.

Review that the Recovery Kickoff asks only for:

- B1-B3 repairs in the same repo/task/main/worktree;
- ordinary task-owned non-force publication to origin/main;
- disposable local test fixtures;
- bounded official plugin normal-entry validation using existing Codex/OpenAI provider/account/credential scope;
- exactly one replacement/final-candidate Machine Policy install/validate on /home/yuukias/.codex;
- standard backup + restore-on-regression;
- existing Notifications only if already configured and within same provider/recipient boundary.

It must NOT authorize:

- paid API;
- new provider/account/data/credential/recipient scope;
- force/destructive Git;
- new remote branch;
- PR/release/tag;
- remap upstream/remote;
- second CODEX_HOME;
- second wrapper;
- generic Git facade;
- auth DB/token store;
- daemon/watcher hierarchy;
- new workflow;
- candidate-plugin-replay;
- unrelated external effects.

The user sending the Critic-approved Recovery Kickoff is the new execution authorization.
This Critic PASS itself must not start execution.

==================================================
7. OUTPUT
==================================================

Return:

CRITIC_RESULT=PASS | REVISE
REVIEW_STAGE=RECOVERY_EXECUTION_READY
REVIEWED_PLAN_VERSION=0.3
RECOVERY_GOAL_PATH=docs/design/0.9.0_low_friction_operations_convergence_recovery_goal_v0.1_2026-09-24.md
RECOVERY_GOAL_VERSION=0.1
RECOVERY_KICKOFF_PATH=docs/design/0.9.0_low_friction_operations_convergence_recovery_kickoff_v0.1_2026-09-24.md
RECOVERY_KICKOFF_VERSION=0.1
RECOVERY_PACKAGE_PATH=docs/design/0.9.0_low_friction_operations_convergence_recovery_execution_package_v0.1_2026-09-24.md
RECOVERY_PACKAGE_COMMIT=e1d37ad6cd959101a15e1219d33f04f5cbe5e4b4
B1_PACKAGE_FIDELITY=PASS | REVISE
B2_PACKAGE_FIDELITY=PASS | REVISE
B3_PACKAGE_FIDELITY=PASS | REVISE
B4_PACKAGE_FIDELITY=PASS | REVISE
AUTHORIZATION_BOUNDARY=PASS | REVISE
READY_FOR_CODEX=YES | NO
BLOCKERS=
NON_BLOCKING_NOTES=
NEXT_HANDOFF=CODEX | PLANNER

If REVISE:
block only on a concrete mismatch between the already-approved architecture/findings and this recovery Goal/Kickoff, or a real missing authorization/recovery path.
Do not reopen Plan v0.3.

If PASS:

1. explain briefly in Chinese that the recovery package only authorizes B1-B4 closure and one replacement Machine Policy install, not a redesign;

2. state:
READY_FOR_CODEX=YES

3. reproduce verbatim the exact text under:

## Copy to Codex after recovery execution-ready Critic PASS

from:

docs/design/0.9.0_low_friction_operations_convergence_recovery_kickoff_v0.1_2026-09-24.md

without rewording or expanding authorization.

That exact text is the only recovery Kickoff the user should send to Codex.
~~~
