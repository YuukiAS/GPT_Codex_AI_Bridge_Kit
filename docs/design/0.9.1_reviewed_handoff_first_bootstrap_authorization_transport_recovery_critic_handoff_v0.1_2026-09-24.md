# Bridge Kit 0.9.1 — First Bootstrap Authorization Transport Recovery Critic Handoff v0.1

Date: **2026-09-24**  
Parent task: **reviewed-handoff--first-bootstrap-normal-entry**  
Review stage: **RECOVERY_DESIGN_REVIEW**  
Recovery Plan: **docs/design/0.9.1_reviewed_handoff_first_bootstrap_authorization_transport_recovery_plan_v0.1_2026-09-24.md**  
Plan commit: **53e535347ba07fb2b234174f6a4cab64509c713e**

以下内容可完整复制到独立 Critic thread。

~~~text
你是 AI Research Stack 的独立 Critic。

target_repo:
YuukiAS/GPT_Codex_AI_Bridge_Kit

target_domain:
Reviewed Handoff / Bridge Kit core

design_topic_or_task_key:
reviewed-handoff--first-bootstrap-normal-entry

review_stage:
RECOVERY_DESIGN_REVIEW

source_branch/ref:
main

related consumer repo:
YuukiAS/AI_Skills_Collection

parent approved Plan:
docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_plan_v0.2_2026-09-24.md

parent Plan version:
0.2

parent Plan commit:
eac789a35c4a83ce015a7744c722527580e79dc8

implemented Bridge candidate:
44965d6707d27b48726a967fba46a9d836b7010c

recovery Plan:
docs/design/0.9.1_reviewed_handoff_first_bootstrap_authorization_transport_recovery_plan_v0.1_2026-09-24.md

recovery Plan version:
0.1

recovery Plan commit:
53e535347ba07fb2b234174f6a4cab64509c713e

This is READ-ONLY recovery design review.

Do not:
- modify production;
- install/modify Machine Policy;
- create branch/worktree;
- implement workflow-core;
- launch Executor;
- call paid API;
- start automation;
- redesign the already-PASS first-bootstrap lifecycle / remote-only resume / shared initializer / candidate-pair architecture.

==================================================
0. GOVERNANCE / SOURCE
==================================================

Read latest AI_Skills_Collection main:

- docs/workflows/PLANNER_ROLE_CONTRACT.md
- docs/workflows/CRITIC_ROLE_CONTRACT.md
- docs/workflows/PLUGIN_CAPABILITY_GATE_POLICY.md

Then read Bridge latest main:

- AGENTS.md
- parent Plan v0.2
- Goal v0.2
- Kickoff v0.2
- Execution/Recovery Contract v0.2
- Execution Package v0.2
- recovery Plan v0.1
- ai_bridge_kit/host.py
- ai_bridge_kit/reviewed_handoff.py
- templates/host/rules/ai-bridge-global.rules
- tests/test_host_policy.py
- tests/test_reviewed_handoff.py
- results/reviewed-handoff--first-bootstrap-normal-entry/EVIDENCE.md
- commit 44965d6707d27b48726a967fba46a9d836b7010c

Note:
tracked EVIDENCE.md currently ends before the post-install FB-G1 rejection.
The current user directly transferred the real runtime fact:
task bootstrap -> execpolicy prompt -> approval/Auto-review REJECT,
with no AI_Skills topology mutation and no workflow-core implementation started.

Independently check current upstream only for:
- Codex execpolicy prompt/allow semantics;
- approvals_reviewer / auto_review behavior;
- workspace-write / additional permissions;
- allow-rule sandbox bypass behavior;
- current project/user rules mechanism;
- whether any released task-scoped authorization primitive exists.

Do not perform unrelated research.

==================================================
1. FAILURE ATTRIBUTION
==================================================

Planner concluded:

A. execpolicy:
WORKING_AS_CONFIGURED.
The installed rule correctly returned prompt.

B. approval/Auto-review:
PRIMARY OBSERVED REJECTION LAYER.
Prompt is routed to the configured approval reviewer.
Auto-review is a risk-based reviewer, not a mechanical receipt for a prior Kickoff.

C. sandbox:
UNRESOLVED SECOND LAYER.
The command did not execute far enough to prove sibling write.
workspace-write normally permits cwd/writable roots, while the target sibling is outside the Bridge cwd.

D. helper:
exact-effect checks exist, but the current caller-selectable target/worktree/base shape is too broad for unconditional permanent allow.

E. Kickoff:
authorization was explicit; the failure is transport/enforcement.

Independently verify this attribution.

Do not misdiagnose the failure as “prefix rule missing”.

==================================================
2. CURRENT-UPSTREAM ASSUMPTION TO REVIEW
==================================================

Planner found no released mechanism that directly binds:

current chat message authorization
-> exact task-scoped execpolicy allow
-> exact task-scoped filesystem write

without another approval/provisioning step.

Specifically review:

- whether prompt rules can mechanically consume an earlier user message;
- whether a rule can choose its reviewer or bind to a message;
- whether current additional_permissions avoids approval routing;
- whether current project-local rules provide a zero-interaction same-session exact grant;
- whether execpolicy allow currently bypasses sandbox for direct allowed commands.

If a mature official mechanism exists now and Planner missed it, that is a blocker to the selected custom hardening route.

==================================================
3. ALTERNATIVES REVIEW
==================================================

Planner disposition:

A. prompt + better wording:
REJECT.

B. temporary exact task-scoped allow:
REJECT as the general product mechanism because:
- current rules are prefix-only;
- an exact command can be hardened with terminator/duplicate rejection,
  but there is no current zero-interaction trusted provisioning/lifecycle path
  that turns the Kickoff into the same-session rule.

C. one-shot local authorization artifact:
REJECT because a Codex-created artifact is forgeable unless a new trusted
token/signature/writer/auth framework is introduced.

D. permanent allow for CURRENT arbitrary task-bootstrap helper:
REJECT because caller still selects target/worktree/base.

D'. permanent allow only AFTER canonical-safe bootstrap hardening:
SELECT.

E. project rules / additional permissions:
REJECT for this current zero-interaction path unless current upstream proves a
mature task-bound mechanism.

Review each alternative against actual current source.
Do not prefer a more elaborate framework merely because it is theoretically cleaner.

==================================================
4. SELECTED D' — CANONICAL SAFE PROFILE
==================================================

The selected recovery changes ONLY first-bootstrap execution trust.

Before task bootstrap can become allowlisted, every mutating invocation must be
inside this safe class.

Target repo:
- real Git repo with Reviewed Handoff installed;
- expected_repo equals actual origin identity;
- no remote mutation.

Task:
- semantic task key;
- branch only reviewed/<task_key>;
- no caller branch;
- no existing local/remote task branch/metadata conflict.

Worktree:
- exact deterministic sibling only:

  <target-parent>/<target-repo-name>-<task_key>

- expected_worktree is equality assertion only;
- any other path fails before mutation;
- symlink/occupied/registered conflicts fail.

Base:
- expected_base_ref exactly origin/main;
- expected_base_commit exactly equals current local refs/remotes/origin/main OID;
- helper performs NO network fetch itself;
- any other base/ref/commit fails before mutation.

Effects:
- local branch/worktree/task-control creation only;
- no push;
- no remote branch;
- no upstream/remote mutation;
- no force.

Check whether this makes the standing allow a safe capability class rather than
“permanent arbitrary first bootstrap”.

==================================================
5. CHECKOUT EXECUTABLE FENCE
==================================================

This is critical because execpolicy allow can bypass the outer sandbox.

Git worktree add performs checkout and current Git docs say post-checkout runs
for worktree add. Checkout filters can invoke smudge/process executables.

The recovery therefore requires fail-closed BEFORE mutation if effective checkout
would execute at least:

- active post-checkout hook, including core.hooksPath;
- external smudge/process filter driver.

Selected semantics:

active checkout executable
-> BOOTSTRAP_CHECKOUT_EXECUTABLE_REQUIRES_SEPARATE_AUTHORIZATION
-> no mutation

Do not silently disable repo hooks/filters merely to pass.

Review whether the fence is sufficient for the new standing allow and whether
there is another obvious checkout-time executable path that would turn allow into
an arbitrary-code sandbox bypass.

Any new blocker here must identify the concrete Git mechanism/source.

==================================================
6. PREFIX / CLI ESCAPE FENCE
==================================================

Current execpolicy is prefix-based, not exact-end.

Recovery requires:

- duplicate safety-critical options rejected:
  --target
  --task-key
  --expected-repo
  --expected-worktree
  --expected-base-ref
  --expected-base-commit

- unknown trailing args rejected before mutation;
- option abbreviation disabled or otherwise proven unable to alter safety fields;
- no --branch;
- metadata-only options cannot change topology.

Review whether these are enough for a permanent allow prefix.

Do not treat match/not_match examples as runtime exact-end predicates.

==================================================
7. KICKOFF AUTHORIZATION SEMANTICS
==================================================

Planner explicitly stops claiming that Machine Policy can prove message provenance.

Revised split:

current-user Kickoff
= workflow authority selecting the task/effect Codex is permitted to attempt

CLI assertions
= equality checks against the helper-derived canonical effect

Machine Policy allow
= standing permission for the intrinsically bounded canonical bootstrap capability

Bridge helper
= machine enforcement that accepted execution cannot escape that capability class

AGENTS/repo files
!= current-user authorization

Review whether this is honest and compatible with the user's requirement.

Important:
If you judge this standing safe-class permission still too broad, say so as a
blocker and explain the minimum safe closure.
Do not suggest prompt+auto_review again unless current upstream provides a new
mechanical guarantee.

==================================================
8. SANDBOX
==================================================

Current official source says an explicitly allowed direct command can skip
approval and bypass the sandbox when all command segments are allowed.

The real recovery gate must prove:

task bootstrap -> allow
no approval request
real sibling path under /home/yuukias -> succeeds

If allow is still sandboxed on the real target and sibling write fails:

FB-G1-AT=FAIL
workflow-core remains paused
return Planner/Critic

No /tmp substitution and no global sandbox broadening.

Review whether this is the correct direct test of the previously unresolved
second layer.

==================================================
9. RESIDUAL THREAT
==================================================

After hardening, any allowed invocation can still choose a semantic task_key in
the current Reviewed repo, causing a deterministic local sibling worktree/branch
to be created from the current local origin/main OID.

Planner classifies this remaining effect as:
- local-only;
- reversible;
- no remote publication;
- no caller-selected path/base/branch;
- no checkout executable;
- bounded rollback.

Critic must explicitly judge whether this residual local-topology capability is
acceptable for permanent Machine Policy allow.

If NO, the Critic should also assess the feasibility conclusion:

with current Codex primitives, the simultaneous requirements
- no second user action,
- no standing safe-class permission,
- no trusted auth store/provisioner,
appear incompatible.

Do not invent a token/auth service as a cosmetic fix.

==================================================
10. NEW RECOVERY GATE
==================================================

Review:

FB-G1-AT — exact real sibling authorization/execution transport.

Real target:

repo:
YuukiAS/AI_Skills_Collection

task:
workflow-core--reviewed-first-bootstrap-normal-entry

branch:
reviewed/workflow-core--reviewed-first-bootstrap-normal-entry

worktree:
/home/yuukias/AI_Skills_Collection-workflow-core--reviewed-first-bootstrap-normal-entry

base:
exact approved/fetched origin/main OID

Required:
1. one Kickoff;
2. installed recovery candidate;
3. execpolicy task bootstrap = allow;
4. raw git worktree add = prompt;
5. canonical safe-profile checks pass;
6. no approval/Auto-review request;
7. real sibling creation succeeds;
8. REQUEST/CURRENT correct;
9. canonical main not polluted;
10. no raw fallback / alternate path / remote mutation.

Negative bank:
- noncanonical path;
- wrong base ref;
- wrong/stale base commit;
- duplicate safety-critical options;
- trailing args;
- symlink/occupied path;
- local/remote task branch conflict;
- post-checkout hook;
- external checkout filter.

A /tmp PASS cannot substitute.

workflow-core remains paused until this gate passes.

==================================================
11. SCOPE / SHOULD-NOT-CHANGE
==================================================

Do not reopen or modify:

- first-bootstrap lifecycle;
- remote-only resume;
- shared initializer;
- workflow-core owner split;
- candidate pair;
- 0.9.1 version target;
- Persistent Run;
- publisher;
- Notifications;
- plugin-replay;
- Reviewed state graph.

Future implementation scope from this recovery should be Bridge-only until
FB-G1-AT passes.

==================================================
12. OUTPUT
==================================================

Return:

CRITIC_RESULT=PASS | REVISE
REVIEW_STAGE=RECOVERY_DESIGN

PARENT_PLAN_VERSION=0.2
PARENT_PLAN_PATH=docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_plan_v0.2_2026-09-24.md
PARENT_PLAN_COMMIT=eac789a35c4a83ce015a7744c722527580e79dc8

RECOVERY_PLAN_PATH=docs/design/0.9.1_reviewed_handoff_first_bootstrap_authorization_transport_recovery_plan_v0.1_2026-09-24.md
RECOVERY_PLAN_VERSION=0.1
RECOVERY_PLAN_COMMIT=53e535347ba07fb2b234174f6a4cab64509c713e

FAILURE_ATTRIBUTION=PASS | REVISE
UPSTREAM_MECHANISM_CHECK=PASS | REVISE
ALTERNATIVES_ANALYSIS=PASS | REVISE
CANONICAL_SAFE_ALLOW=PASS | REVISE
CHECKOUT_EXECUTABLE_FENCE=PASS | REVISE
PREFIX_CLI_FENCE=PASS | REVISE
SANDBOX_CLOSURE=PASS | REVISE
RESIDUAL_RISK_ACCEPTABLE=YES | NO
FB_G1_AT_GATE=PASS | REVISE
WORKFLOW_CORE_REMAINS_PAUSED=YES | NO

BLOCKERS=
NON_BLOCKING_NOTES=
NEXT_HANDOFF=PLANNER

If REVISE:
each blocker must include stable id, requirement, direct current evidence,
causal user/product risk, and minimum closure.
Do not reopen already-PASS parent architecture unless this recovery directly
contradicts it.

If PASS:
state explicitly that PASS approves only this authorization-transport recovery design.
It does not authorize production implementation, Machine Policy modification,
AI_Skills worktree creation, workflow-core modification, paid API or rollout.

Hand back to Planner to prepare the bounded recovery execution package.

NEXT_HANDOFF=PLANNER
~~~
