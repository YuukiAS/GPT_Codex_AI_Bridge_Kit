# Bridge Kit 0.9.2 — Unattended Execution Refinement Execution-Ready Critic Handoff v0.2

Date: **2026-09-25**  
Task key: **bridge-core--unattended-execution-refinement**  
Review stage: **EXECUTION_READY_REVIEW / BLOCKER_1 RE-REVIEW**  
Current package commit: **8778c829deffe9ab71d4ec8e5a4215fa6372e5d1**

以下整段可直接复制给独立 Critic thread。

---

你是 AI Research Stack 的独立 Critic。

这是 **READ-ONLY execution-ready re-review**。

不要实现代码。  
不要修改 Git。  
不要启动 Codex Executor。  
不要启动 Persistent Run。  
不要修改 DII。  
不要修改 Machine Policy。  
不要调用付费 API。  
不要创建 automation。  
不要推进正式 release。

## Active Review Context

```text
target_repo:
YuukiAS/GPT_Codex_AI_Bridge_Kit

target_domain:
Bridge Kit / unattended authorization readiness + Persistent Run

design_topic_or_task_key:
bridge-core--unattended-execution-refinement

source_branch/ref:
main

review_stage:
EXECUTION_READY_REVIEW

previous_reviewed_main:
7132ec2929456dcea33970ba5ce0f7355251a059
```

## Previously approved architecture — do not reopen

Approved Plan remains unchanged:

```text
path:
docs/design/0.9.2_unattended_execution_persistent_run_refinement_plan_v0.1_2026-09-25.md

version:
v0.1

commit:
9c5c7f7ac2bb110dcba98deea2b0c0d8bb7ffa1f
```

The previous execution-ready Critic result was:

```text
RESULT=REVISE
BLOCKER_1=NORMAL_AUTHORING_SURFACES_MISSING_FROM_EXECUTION_SCOPE
```

Do not reopen items already accepted by the previous review:

```text
OPTION_B_PRESERVED=YES
G1_G2_NORMAL_SURFACE_CONTRACT=PASS
G4_TMUX_BOUNDARY=PASS
G5_PROGRESS_WITHOUT_TMUX=PASS
DII_922349_INTERPRETATION=PASS
AUTHORIZATION_BOUNDARY=PASS
MACHINE_POLICY_UNCHANGED=YES
FORMAL_RELEASE_EXCLUDED=YES
```

## Revised review object

Canonical Goal:

```text
path:
docs/design/0.9.2_unattended_execution_persistent_run_refinement_goal_v0.2_2026-09-25.md

version:
v0.2

first commit:
05c065d95f6f78694c62aec9405535e188a15c09
```

Kickoff Draft:

```text
path:
docs/design/0.9.2_unattended_execution_persistent_run_refinement_kickoff_v0.2_2026-09-25.md

version:
v0.2

first commit:
acdb8c1da0e4f0ded037c9a96c4acb35087e2bab

current binding commit:
0c5018db9b7af9dbda65400d19b9f4b1052d54a0
```

Execution Package:

```text
path:
docs/design/0.9.2_unattended_execution_persistent_run_refinement_execution_package_v0.2_2026-09-25.md

version:
v0.2

package commit:
8778c829deffe9ab71d4ec8e5a4215fa6372e5d1
```

## 0. Required reading

First read latest `YuukiAS/AI_Skills_Collection` main:

- `docs/workflows/PLANNER_ROLE_CONTRACT.md`
- `docs/workflows/CRITIC_ROLE_CONTRACT.md`
- `docs/workflows/PLUGIN_CAPABILITY_GATE_POLICY.md`

Then read Bridge latest main and exactly these package files:

- approved Plan v0.1;
- Goal v0.2;
- Kickoff v0.2;
- Execution Package v0.2;
- the two authoring sources named by BLOCKER_1:
  - `chatgpt/GITHUB_MCP_REPO_INSTRUCTIONS.md`
  - `templates/prompts/templates/TASK_TEMPLATE.md`

Read other current source only as necessary to check that this revision did not create a contradiction.

## 1. Only blocker under re-review

The prior execution package omitted two real normal-authoring entry points from its allowed implementation surface.

Current source evidence showed:

### `chatgpt/GITHUB_MCP_REPO_INSTRUCTIONS.md`

It still maps long/overnight/unattended/survive-disconnect intent in an installed Persistent Run repository toward:

```text
Persistent execution: REQUIRED
Backend: tmux
```

### `templates/prompts/templates/TASK_TEMPLATE.md`

It still instructs overnight/unattended/run-until-morning/multi-hour/survive-disconnect requests to replace the ordinary task section with the installed Persistent Run contract.

Those are normal authoring surfaces, so leaving them outside implementation scope could preserve the exact old duration-to-tmux behavior after the rest of the refinement.

## 2. What v0.2 changed

The revised Goal and Kickoff now explicitly add both files to the primary/allowed implementation surface:

```text
chatgpt/GITHUB_MCP_REPO_INSTRUCTIONS.md
templates/prompts/templates/TASK_TEMPLATE.md
```

The revised Goal also requires focused regression for **each** normal authoring entry proving:

1. duration / overnight / unattended does not automatically select tmux;
2. scheduler-owned batch does not select tmux solely because it is long/overnight;
3. terminal-owned survive-disconnect execution can still select Persistent Run/tmux.

The revised Package records this as the explicit closure of:

```text
BLOCKER_1=NORMAL_AUTHORING_SURFACES_MISSING_FROM_EXECUTION_SCOPE
```

## 3. Hard should-not-change boundary is unchanged

Still excluded from implementation:

```text
ai_bridge_kit/persistent_run.py
ai_bridge_kit/host.py
templates/host/rules/ai-bridge-global.rules
workflow schema/state machinery
release/ref machinery
YuukiAS/Distributed_Imaging_Inference
```

If the revised package silently changes any of these semantics, REVISE.

Otherwise do not ask for additional architecture work.

## 4. Previously-PASS gates and boundaries are unchanged

Do not reopen unless the v0.2 edit directly contradicts them:

- G1/G2 remain final-candidate normal Codex/Auto-review behavior gates;
- G4 remains the exact bounded tmux smoke;
- G5 remains no-tmux project-native progress evidence;
- DII 922349 remains scheduler-owned batch-holder reference evidence;
- Machine Policy remains unchanged;
- formal release remains excluded until later Gate/closure.

The only G5 addition is a non-blocking execution-isolation detail:

```text
--state-home /tmp/ai-bridge-092-unattended-gates/g5-state
```

This must be treated only as task-owned test-state isolation. It is not a new state architecture, progress owner, daemon or watcher.

## 5. Review question

Answer only:

> Does Goal/Kickoff/Package v0.2 close BLOCKER_1 completely and without regressing any previously-PASS execution boundary?

A PASS does not authorize implementation by itself; it makes the frozen Kickoff execution-ready, after which only the user sending the approved Kickoff becomes current-user execution authorization.

## 6. PASS / REVISE discipline

If REVISE, every blocker must satisfy the Critic contract and must arise from:

- BLOCKER_1 still not actually closed; or
- a direct regression introduced by this v0.2 revision.

Use:

```text
BLOCKER_N=
REQUIREMENT=
DIRECT_EVIDENCE=
CAUSAL_RISK=
MINIMAL_CLOSE_CONDITION=
```

Do not add unrelated architecture improvements or reopen already-PASS gates.

If PASS, this is an **execution-ready PASS**. Per Critic Role Contract:

1. state `READY_FOR_CODEX=YES`;
2. approve Goal v0.2 + Kickoff v0.2 + Package v0.2 as the exact execution package;
3. output the approved Kickoff **verbatim** from:
   `docs/design/0.9.2_unattended_execution_persistent_run_refinement_kickoff_v0.2_2026-09-25.md`;
4. do not rewrite or expand its authorization after PASS.

## 7. Expected final fields

```text
CRITIC_RESULT=PASS | REVISE
REVIEW_STAGE=EXECUTION_READY_REVIEW

REVIEWED_PLAN_VERSION=v0.1
REVIEWED_PLAN_COMMIT=9c5c7f7ac2bb110dcba98deea2b0c0d8bb7ffa1f

GOAL_PATH=docs/design/0.9.2_unattended_execution_persistent_run_refinement_goal_v0.2_2026-09-25.md
GOAL_VERSION=v0.2

KICKOFF_PATH=docs/design/0.9.2_unattended_execution_persistent_run_refinement_kickoff_v0.2_2026-09-25.md
KICKOFF_VERSION=v0.2

PACKAGE_PATH=docs/design/0.9.2_unattended_execution_persistent_run_refinement_execution_package_v0.2_2026-09-25.md
PACKAGE_VERSION=v0.2
PACKAGE_COMMIT=8778c829deffe9ab71d4ec8e5a4215fa6372e5d1

BLOCKER_1_NORMAL_AUTHORING_SURFACES=CLOSED | OPEN
GITHUB_MCP_REPO_INSTRUCTIONS_IN_SCOPE=YES | NO
TASK_TEMPLATE_IN_SCOPE=YES | NO
FOCUSED_REGRESSION_BOTH_SURFACES=PASS | REVISE
SHOULD_NOT_CHANGE_BOUNDARY=PASS | REVISE
PREVIOUSLY_PASS_GATES_REGRESSED=YES | NO
READY_FOR_CODEX=YES | NO

BLOCKERS=
NON_BLOCKING_NOTES=
NEXT_HANDOFF=CODEX | PLANNER
```
