# Bridge Kit 0.9.1 — First Bootstrap Authorization Transport Recovery Critic Handoff v0.5

Date: **2026-09-24**  
Parent task: **reviewed-handoff--first-bootstrap-normal-entry**  
Review stage: **RECOVERY_DESIGN_REVIEW**  
Recovery Plan: **docs/design/0.9.1_reviewed_handoff_first_bootstrap_authorization_transport_recovery_plan_v0.5_2026-09-24.md**  
Recovery Plan commit: **bdeda15529fe784515185165b18cfbac4531951c**

以下内容可完整复制到独立 Critic thread。

~~~text
你是 AI Research Stack 的独立 Critic。

target_repo:
YuukiAS/GPT_Codex_AI_Bridge_Kit

target_domain:
Reviewed Handoff / Bridge Kit core

design_topic_or_task_key:
reviewed-handoff--first-bootstrap-normal-entry

review_stage:
RECOVERY_DESIGN_REVIEW

source_branch/ref:
main

related consumer repo:
YuukiAS/AI_Skills_Collection

parent approved Plan:
docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_plan_v0.2_2026-09-24.md

parent Plan commit:
eac789a35c4a83ce015a7744c722527580e79dc8

implemented Bridge candidate:
44965d6707d27b48726a967fba46a9d836b7010c

previous recovery Plan:
docs/design/0.9.1_reviewed_handoff_first_bootstrap_authorization_transport_recovery_plan_v0.4_2026-09-24.md

previous recovery Plan commit:
d5f106be0c2fb5a9abff9905d59e39a67f21a743

reviewed recovery Plan:
docs/design/0.9.1_reviewed_handoff_first_bootstrap_authorization_transport_recovery_plan_v0.5_2026-09-24.md

reviewed recovery Plan version:
0.5

reviewed recovery Plan commit:
bdeda15529fe784515185165b18cfbac4531951c

previous Critic result:
REVISE

only blocker under re-review:
RL-B2-REMOTE-BRANCH-FRESHNESS

This is READ-ONLY recovery design re-review.

Do not:
- modify production;
- install/modify Machine Policy;
- create branch/worktree;
- implement workflow-core;
- launch Executor;
- call paid API;
- start automation;
- reopen RL-B1;
- reopen repo-local architecture;
- reopen standing-allow decision;
- reopen residual local topology permission.

==================================================
0. GOVERNANCE / SOURCE
==================================================

Read latest AI_Skills_Collection main:

- docs/workflows/PLANNER_ROLE_CONTRACT.md
- docs/workflows/CRITIC_ROLE_CONTRACT.md
- docs/workflows/PLUGIN_CAPABILITY_GATE_POLICY.md

Then read Bridge latest main:

- AGENTS.md
- parent Plan v0.2
- recovery Plan v0.4
- recovery Plan v0.5
- ai_bridge_kit/reviewed_handoff.py
- templates/host/rules/ai-bridge-global.rules
- tests/test_reviewed_handoff.py
- tests/test_host_policy.py
- results/reviewed-handoff--first-bootstrap-normal-entry/EVIDENCE.md

Independently recheck only the relevant official Git facts:

- remote.<name>.skipDefaultUpdate as deprecated synonym of skipFetchAll;
- git fetch --all behavior with multiple remotes;
- remote.<name>.fetch controlling fetched/mapped refs;
- negative refspec behavior;
- pruning following configured fetch mappings.

Do not perform broad architecture research.

==================================================
1. ACCEPTED PARTS — DO NOT REOPEN
==================================================

Already accepted and frozen:

- repo-local Codex owns its own repo;
- cross-repo low-friction mutation is not a 0.9.1 requirement;
- repo-local self-scoped task bootstrap;
- standing Machine Policy allow after hardening;
- RL-B1 reachable executable fence;
- RL-B1 output symlink fence;
- bootstrap is network-free;
- raw git worktree add remains prompt;
- deterministic sibling path;
- branch = reviewed/<task_key>;
- base = current locally-fetched origin/main exact OID;
- residual local topology permission acceptable;
- real FB-G1-AT must use actual AI_Skills sibling path;
- workflow-core remains paused until FB-G1-AT PASS;
- no temporary rules/auth DB/token/receipt/writable-root lifecycle/watcher/controller.

Do not re-review these.

==================================================
2. RL-B2 REMAINING FACTS
==================================================

Critic already accepted v0.4's core rule:
trusted bootstrap may treat refs/remotes/origin/* as remote truth only under a
canonical origin full-heads fetch profile.

v0.5 addresses only two missing direct Git configuration facts:

A. skipDefaultUpdate
remote.<name>.skipDefaultUpdate is still a deprecated synonym of
remote.<name>.skipFetchAll.

B. multiple remotes
git fetch --all --prune processes multiple configured remotes, and another
remote's refspec can write/prune custom destinations.

Therefore v0.5 chooses the strictest small safe class:
configured remotes exactly ["origin"].

Review only whether these two additions close RL-B2.

==================================================
3. FINAL CANONICAL REMOTE SAFE CLASS
==================================================

Before trusted low-friction bootstrap:

configured remotes:
- exactly origin
- no upstream/mirror/backup/second remote

remote.origin.skipFetchAll:
- unset or false

remote.origin.skipDefaultUpdate:
- unset or false

remote.origin.fetch:
- exactly one value
- exactly:
  +refs/heads/*:refs/remotes/origin/*

Reject:
- any second remote;
- skipDefaultUpdate=true;
- skipFetchAll=true;
- main-only refspec;
- multiple refspecs;
- negative refspec;
- custom destination;
- mirror/custom mapping;
- any ambiguous/invalid config.

Do not mutate config.
Do not attempt equivalence reasoning.
Noncanonical repos remain outside the trusted 0.9.1 path.

Review whether this strict class is sufficient and appropriately narrow.

==================================================
4. AUTHORITATIVE SYNC
==================================================

Only after the final safe class passes:

1. run existing allowed:
   git fetch --all --prune

2. capture post-sync:
   refs/remotes/origin/main

3. inspect post-sync:
   refs/remotes/origin/reviewed/<task_key>

4. if reviewed ref exists:
   reject first bootstrap / route existing-task resume

5. if absent:
   absence is authoritative for this safe class

6. expected-base-commit must equal post-sync origin/main OID

7. bootstrap re-checks the safe class and local refs immediately before mutation

8. bootstrap itself performs zero network operations

Review whether this now mechanically closes stale false-absence.

==================================================
5. WHY SECOND REMOTES ARE REJECTED
==================================================

v0.5 deliberately does not attempt to prove that another remote is harmless.

Reason:
another remote can have a custom fetch destination and fetch --all/prune acts on
all participating remotes.

Rather than build a generalized cross-remote refspec analyzer, the trusted
0.9.1 safe class simply requires origin-only.

Special/custom multi-remote repos can use ordinary/manual approval.

Do not ask for exotic multi-remote support unless current Git facts show
origin-only is still insufficient.

==================================================
6. DIRECT REGRESSIONS
==================================================

Require the minimal six direct cases:

1. canonical origin-only profile + remote reviewed branch:
   fetch sees it -> local reviewed remote-tracking ref exists -> bootstrap rejects.

2. reviewed branch deleted:
   fetch --all --prune removes local reviewed ref -> may continue if other checks pass.

3. remote.origin.skipDefaultUpdate=true:
   fail closed before trusted bootstrap.

4. second configured remote:
   fail closed before trusted bootstrap; no refspec analysis/config mutation.

5. stale expected base after sync:
   reject before mutation.

6. bootstrap network canary:
   zero fetch/ls-remote/provider/network calls inside helper.

Retain existing v0.4 regressions for:
- skipFetchAll=true;
- main-only/custom/multiple/negative origin refspec.

Check whether this coverage is enough to close the blocker.

==================================================
7. MECHANICAL ENFORCEMENT
==================================================

The bootstrap helper itself must re-check before mutation:

- configured remotes exactly ["origin"];
- skipFetchAll absent/false;
- skipDefaultUpdate absent/false;
- exact one canonical origin.fetch value;
- exact local origin/main OID;
- local origin/reviewed/<task_key> absence.

The workflow may preflight the same condition earlier, but prose alone is not enough.

No new CLI command/schema/policy object is introduced.

==================================================
8. COMPLEXITY BOUNDARY
==================================================

v0.5 adds only:

- deprecated skipDefaultUpdate check;
- exactly-one-remote=origin check.

No:
- generalized multi-remote reasoning;
- generalized refspec parser;
- new fetch wrapper;
- ls-remote inside bootstrap;
- provider API;
- Git config mutation;
- custom/mirror repo support;
- auth/workflow/controller infrastructure.

Do not expand scope beyond RL-B2.

==================================================
9. REAL FB-G1-AT IMPACT
==================================================

Real AI_Skills gate must record:

- configured remote names;
- skipFetchAll state;
- skipDefaultUpdate state;
- exact origin.fetch list;
- successful git fetch --all --prune;
- post-sync origin/main OID;
- post-sync reviewed task ref absence;
- zero bootstrap network;
- then the already-approved real sibling bootstrap.

If AI_Skills is noncanonical:
trusted low-friction FB-G1-AT does not PASS.
Do not auto-normalize.

==================================================
10. OUTPUT
==================================================

CRITIC_RESULT=PASS | REVISE
REVIEW_STAGE=RECOVERY_DESIGN

PARENT_PLAN_VERSION=0.2
PARENT_PLAN_PATH=docs/design/0.9.1_reviewed_handoff_first_bootstrap_normal_entry_plan_v0.2_2026-09-24.md
PARENT_PLAN_COMMIT=eac789a35c4a83ce015a7744c722527580e79dc8

RECOVERY_PLAN_VERSION=0.5
RECOVERY_PLAN_PATH=docs/design/0.9.1_reviewed_handoff_first_bootstrap_authorization_transport_recovery_plan_v0.5_2026-09-24.md
RECOVERY_PLAN_COMMIT=bdeda15529fe784515185165b18cfbac4531951c

RL_B2_REMOTE_BRANCH_FRESHNESS=CLOSED | OPEN
ORIGIN_ONLY_REMOTE_SET=PASS | REVISE
SKIP_FETCH_ALL_FENCE=PASS | REVISE
SKIP_DEFAULT_UPDATE_FENCE=PASS | REVISE
FULL_HEADS_REFSPEC_FENCE=PASS | REVISE
FETCH_ALL_PRUNE_AUTHORITY=PASS | REVISE
NETWORK_FREE_BOOTSTRAP=PASS | REVISE
REGRESSION_BANK=PASS | REVISE
COMPLEXITY_BOUNDARY=PASS | REVISE
RL_B1_REOPENED=NO
REPO_LOCAL_ARCHITECTURE_REOPENED=NO
STANDING_ALLOW_REOPENED=NO
WORKFLOW_CORE_REMAINS_PAUSED=YES | NO

NEW_BLOCKERS=
BLOCKERS=
NON_BLOCKING_NOTES=
NEXT_HANDOFF=PLANNER

If REVISE:
only block on RL-B2 not actually being closed, or a new direct Git fact that
makes the v0.5 freshness proof unsound.
Do not reopen RL-B1, repo-local architecture, or standing-allow.

If PASS:
state explicitly that recovery Plan v0.5 closes RL-B2 and approves design only.
It does not authorize production implementation, Machine Policy modification,
AI_Skills worktree creation, workflow-core modification, paid API or rollout.

Hand back to Planner for the minimal recovery execution package.

NEXT_HANDOFF=PLANNER
~~~
